use flake

# Same per-host build cache `./dev` wires, for the direnv path into the shell.
# Quiet: this runs on every `cd` into the tree, and a notice on each one is noise.
# See docs/dev/guides/build-cache.md.
eval "$("$PWD/scripts/build-cache-env.sh" --quiet)"
