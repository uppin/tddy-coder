# Validate Tests Report — `tddy-workflow-recipes`

**Date:** 2026-04-04

## Scope

Validation focused on the **tdd-small** workflow recipe work under `packages/tddy-workflow-recipes` (resolver/policy, `tdd_small` module, acceptance tests), per `plans/tdd-small-workflow-recipe/evaluation-report.md`.

## Command Run

```bash
cd /var/tddy/Code/tddy-coder/.worktrees/tdd-small-workflow-recipe && ./dev cargo test -p tddy-workflow-recipes 2>&1
```

## Summary

| Metric | Count |
|--------|------:|
| Passed | 85 |
| Failed | 0 |
| Total  | 85 |

Breakdown by test binary (all **ok**):

| Binary / suite | Passed |
|----------------|-------:|
| `lib` (unit tests) | 52 |
| `backend_invoke_no_tddy_tools_submit` | 5 |
| `free_prompting_activity_pane_hooks` | 1 |
| `free_prompting_agent_invoke` | 1 |
| `goals_contract` | 3 |
| `grill_me_repo_plan` | 8 |
| `proto_goal_files` | 2 |
| `proto_workflow_contracts` | 1 |
| `recipe_policy_red` | 5 |
| `tdd_small_acceptance` | 3 |
| `workflow_recipe_acceptance` | 4 |
| Doc-tests | 0 |

## Failing Tests

None.

## Full Workspace Tests (`./test`)

Running the full `./test` script was **not** executed for this report. Focused `cargo test -p tddy-workflow-recipes` is appropriate for validating this package’s recipe logic, proto/contracts, and integration tests. Full workspace tests add value mainly as a **regression net** for `tddy-core`, `tddy-coder`, and `tddy-tools` interactions; run them before merge if other workspace crates were touched or for release confidence.

## Coverage Gaps / Recommendations (`tdd_small`)

1. **`hooks.rs` (large surface)** — No dedicated tests exercise `TddSmallWorkflowHooks` end-to-end (task transitions, after-task handlers, merged submit wiring). Acceptance tests only assert graph shape, `merged_red_system_prompt()`, and parser round-trip. **Recommendation:** Add targeted integration tests similar in spirit to `free_prompting_activity_pane_hooks` / other recipe hook tests, using `StubBackend` and minimal workflow steps if the harness allows, to lock behavior and reduce drift vs classic `TddWorkflowHooks`.

2. **`parse_post_green_review_response`** — Covered by a minimal golden in `submit.rs` tests and duplicated golden usage in `tdd_small_acceptance.rs`. **Missing edge cases:** malformed JSON (expect `ParseError::Malformed`), wrong `goal` string (explicit error path), optional/trailing content, or empty required fields if those should be rejected. Add small unit tests for error messages and the `goal` guard.

3. **`post_green_review.rs`** — `system_prompt()` and `build_prompt()` are untested; low risk but cheap snapshot or substring tests would document PRD intent (single submit, no separate evaluate/validate).

4. **Policy / resolver** — `recipe_policy_red` and `workflow_recipe_acceptance` cover listing and `resolve_tdd_small_recipe`; no additional gap called out beyond broader hook coverage above.

---

*Generated by validate-tests subagent run.*
