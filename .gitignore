# Nix profile (GC root) - created by `./dev` or `nix develop --profile ./.nix-profile`
.nix-profile
.nix-profile-*

# Agent verification output (when terminal capture is broken)
.verify-result.txt

# Build artifacts
target/

# Web workspace
node_modules/
dist/
.storybook-static/
storybook-static/

# Cypress
cypress/screenshots/
cypress/videos/
cypress-debug.log

# macOS
.DS_Store
.worktrees

# tddy-coder session artifacts (when run without --output-dir, sessions go to ~/.tddy)
changeset.yaml
.vscode/settings.json

tmp/