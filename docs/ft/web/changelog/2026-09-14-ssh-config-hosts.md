# 2026-09-14 — SSH connections on the Hosts row

Each host row lists explicit OpenSSH `Host` aliases from that machine's `~/.ssh/config`, distinct
from ssh-agent key listing. Failed reads say "Could not check" rather than implying no destinations.
Product detail: [hosts-screen-tooling.md](../hosts-screen-tooling.md).
