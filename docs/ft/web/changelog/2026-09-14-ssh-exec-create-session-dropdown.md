# 2026-09-14 — Create-session SSH Host alias picker

Co-located managed sessions can select an OpenSSH Host alias from the session host's config
(`create-session-ssh-config-select`); empty selection keeps local exec tooling.
