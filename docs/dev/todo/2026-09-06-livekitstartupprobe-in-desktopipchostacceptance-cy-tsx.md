# 2026-09-06 — `LiveKitStartupProbe` in `DesktopIpcHostAcceptance.cy.tsx`

**Category:** Deferred from `optional-livekit` (#449)
**Source:** optional-livekit common-room switch, #449

grew an `enabled` field on its
inline `config` prop type. If a third spec needs the same shape, extract a named fixture rather
than widening the inline type again.
