# 2026-07-01 — tddy-core

**Category:** Future enhancement
**Source:** stdio-transport-for-grpc-binaries changeset, 2026-07-01

- **Migrate the toolcall listener to tddy-rpc/tddy-stdio** — `tddy-toolcall/src/toolcall/listener.rs` (in `tddy-core` until `#carve` 12/14, #522, which moved it unchanged; still reachable as `tddy_core::toolcall`) is a third bespoke newline-delimited-JSON protocol (`submit`/`ask`/`approve`/`list-actions`/`build`) between `tddy-coder` and the Claude Code CLI subprocess it spawns, distinct from the sandbox tool-IPC and gRPC-over-UDS relay this changeset migrates. Same category of problem, same fix would apply, deferred to keep this changeset scoped.

**Observed 2026-09-23** (`#carve` 12/14 wrap, not verified further): the listener already serves
each connection through `ToolcallRpcService` over `tddy_stdio::StdioEndpoint::from_duplex`
(`listener.rs:196`), with `tddy_rpc::RpcService` dispatch. If the `finish-stdio-ipc-migration`
work covered this item, the entry is resolved and can be deleted by whoever confirms it.
