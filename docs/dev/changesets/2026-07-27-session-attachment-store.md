# 2026-07-27 — **session-attachment-store

**Type:** Feature

user attachments under `artifacts/attachments/` with `kind`/`size_bytes` on `SessionEntry.context_docs`** — **tddy-workflow**: `SESSION_ATTACHMENTS_SUBDIR` / `session_attachments_root` / `canonical_attachment_write_path`. **tddy-core**: re-exports those helpers. **tddy-daemon**: `session_attachments` store (`list_session_attachments`, `copy_attachment_into_session` with segment + containment guards, exclusive create-new write); `session_context_docs` lists manifest rows then attachments (`ContextDocKind`, `size_bytes`); enrichment maps to proto. **tddy-service** / **tddy-web**: `SessionContextDocKind` + `kind`/`size_bytes` on `SessionContextDoc` (regen `connection_pb.ts`). Feature [session-attachments.md](../../ft/coder/session-attachments.md), [session-layout.md](../../ft/coder/session-layout.md), [pr-stacking.md § Context docs](../../ft/coder/pr-stacking.md#context-docs-in-proto). Package indexes [tddy-workflow](../../../packages/tddy-workflow/docs/changesets/), [tddy-core](../../../packages/tddy-core/docs/changesets/), [tddy-daemon](../../../packages/tddy-daemon/docs/changesets/), [tddy-service](../../../packages/tddy-service/docs/changesets/), [tddy-web](../../../packages/tddy-web/docs/changesets/). WIP source `docs/dev/1-WIP/changeset-session-attachment-store.md` removed after wrap. (tddy-workflow, tddy-core, tddy-daemon, tddy-service, tddy-web)
