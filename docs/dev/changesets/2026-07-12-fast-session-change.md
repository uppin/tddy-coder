# 2026-07-12 — **fast-session-change

**Type:** Feature

participant-bound sessions, background terminals, session metadata, shared `tddy-tool-engine`** — each attached LiveKit session owns a self-contained runtime (Room + `GhosttyTerminalLiveKit` + session-participant `ConnectionService` client + byte counters + `lastDataReceivedAt`) held in a `SessionRuntimeRegistry`; focus switch = CSS visibility, no unmount/reconnect/resize; background terminals keep streaming; explicit-disconnect eviction (no cap). Session-scoped RPCs route to `daemon-{instanceId}-{sessionId}`; `DeleteSession`/`SignalSession` + bootstrap/directory stay daemon-direct on `daemon-{instanceId}` (daemon owns teardown, reachable when coder is stuck). `tddy-coder` serves `ConnectionService` (tools/control) from its participant + publishes a `session` metadata block on transitions (shallow-merged with `owned_project_count`/`codex_oauth`); does NOT serve/relay delete/signal. New shared **`tddy-tool-engine`** crate (extracted from `tddy-daemon` `tool_engine.rs`+`tool_catalog.rs`) — `execute_tool`/`execute_tool_with_env`/`tool_catalog` (Read/Write/StrReplace/Delete/Grep/Glob/Shell/Await/ReadLints/SemanticSearch), path-contained against a `worktree_root`, backed by `tddy-task`; `CoderSessionToolExecutor` dispatches `ExecuteTool` against the session worktree (`ToolExecutor` seam `async`). `tddy-daemon`: re-exports the crate as `tool_engine` (call sites unchanged), sandbox-allowlist sync test moved to `tool_catalog_sync.rs`, `ListExecTools` maps shared `ToolDef`→proto, daemon-direct delete/signal contract test, `SessionEntry` gains `bytes_in`/`bytes_out`/`last_data_received_at` (proto 24/25/26) from the `GrpcSessionTerminal` meter. `tddy-livekit`: documents the `session` metadata key (merge helper already existed). `tddy-web`: registry + `sessionParticipantRpcClient` + `sessionParticipantMetadata` parser + `lastDataReceivedFormat` + inspector dual-source (live runtime | daemon `SessionEntry`) + `SessionManager` metadata overlay. **Deferred (FIXME `2026-07-12-fast-session-change`):** headless `--grpc` path `session` metadata tap (interactive path wired via `spawn_session_metadata_tap`); tool executor is wired on both paths. Features [session-drawer.md § Fast Session Change](../../ft/web/session-drawer.md#fast-session-change), [session-participant-rpc.md](../../ft/coder/session-participant-rpc.md), [terminal-sessions.md § Session-scoped RPC routing & daemon-direct lifecycle](../../ft/daemon/terminal-sessions.md#session-scoped-rpc-routing--daemon-direct-lifecycle), [web-terminal.md § Per-session LiveKit room](../../ft/web/web-terminal.md#per-session-livekit-room-sessions-drawer), [livekit-participant-owned-projects.md](../../ft/web/livekit-participant-owned-projects.md), [tddy-tool-engine README](../../../packages/tddy-tool-engine/README.md), [participant-metadata.md § `session` metadata key](../../../packages/tddy-livekit/docs/participant-metadata.md#session-metadata-key). Package indexes [tddy-web](../../../packages/tddy-web/docs/changesets/), [tddy-coder](../../../packages/tddy-coder/docs/changesets/), [tddy-tool-engine](../../../packages/tddy-tool-engine/docs/changesets/), [tddy-livekit](../../../packages/tddy-livekit/docs/changesets/), [tddy-daemon](../../../packages/tddy-daemon/docs/changesets/). PR [#297](https://github.com/uppin/tddy-coder/pull/297). WIP sources `docs/dev/1-WIP/2026-07-12-fast-session-change.md`, `docs/ft/web/1-WIP/PRD-2026-07-12-fast-session-change.md`, `docs/ft/daemon/1-WIP/PRD-2026-07-12-fast-session-change.md` removed after wrap. (tddy-web, tddy-coder, tddy-tool-engine, tddy-livekit, tddy-daemon, tddy-service)
