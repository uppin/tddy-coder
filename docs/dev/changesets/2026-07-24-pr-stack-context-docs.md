# 2026-07-24 — **pr-stack-context-docs

**Type:** Feature

pr-stack persists `exploration.md`, shares it to `orchestrate`, and the session's planning docs become a first-class `SessionEntry.context_docs` list** — **tddy-workflow-recipes**: `StackPlanOutput` gains an optional `exploration` field and `after_write_stack_plan` writes `artifacts/exploration.md` when non-blank (parity with tdd/bugfix); the `PrStackRecipe` manifest registers `exploration → exploration.md` and the `orchestrate` `before_task` prepends the `<context-reminder>` header listing the on-disk manifest docs; a new defaulted `SessionArtifactManifest::artifact_doc_descriptions()` (key→one-liner) is overridden by `PrStackRecipe` for all five known artifacts. **tddy-service**: `proto/connection.proto` adds `message SessionContextDoc { key, basename, path, description, exists }` + `repeated SessionEntry.context_docs = 27` (regenerated Rust). **tddy-daemon**: new `session_context_docs` module — `context_docs_for_session(recipe, session_dir)` lists the manifest's docs (absolute `artifacts/` path + on-disk existence + per-key description; blank/unknown recipe → empty), and `read_session_context_doc_utf8` reads an allowlisted doc with a canonicalize-and-contain guard rooted at `session_artifacts_root` (non-manifest / traversal basename → `PermissionDenied`, mirroring `session_workflow_files`); `ListSessions` enrichment populates `SessionEntry.context_docs` (new internal `tddy-workflow` dep). The wire `ReadSessionContextDoc` RPC + the web **Docs** tab and child "Start session" prompt references are a stacked follow-up. Tests: tddy-workflow-recipes A/B (`StackPlanOutput` exploration + `after_write_stack_plan` + manifest + orchestrate header), tddy-daemon `session_context_docs` 4 + `apply_context_docs_to_proto_lists_the_recipe_manifest_docs`; clippy `-D warnings` clean. Feature [pr-stacking.md](../../ft/coder/pr-stacking.md), [exploration-artifact.md](../../ft/coder/exploration-artifact.md). Package indexes [tddy-workflow-recipes](../../../packages/tddy-workflow-recipes/docs/changesets/), [tddy-service](../../../packages/tddy-service/docs/changesets/), [tddy-daemon](../../../packages/tddy-daemon/docs/changesets/). PR [#332](https://github.com/uppin/tddy-coder/pull/332). (tddy-workflow-recipes, tddy-service, tddy-daemon)
