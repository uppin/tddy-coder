# 2026-06-15 — RPC Playground

**Type:** Feature

gRPC ServerReflection backend (`reflection_service.rs`, vendored `reflection.proto`, embedded `FileDescriptorSet`); `MultiRpcService::service_names()`; `reflection_entry_from()` registered in daemon + all coder `MultiRpcService` sites; daemon LiveKit RPC participant in common room; `tddy-web` `/rpc-playground` screen (hash routing, participant picker, request editor, invoke); `tddy-livekit-web` reflection proto codegen; Cypress LiveKit auto-start testkit. Feature [rpc-playground.md](../../ft/daemon/rpc-playground.md); product [daemon/changelog/](../../ft/daemon/changelog/). (tddy-rpc, tddy-service, tddy-daemon, tddy-coder, tddy-livekit-web, tddy-web)
