# 2026-07-25 — **acp-replay-lazy-tool-bodies

**Type:** Feature

strip tool bodies from StreamAcpReplay, add GetAcpToolCallDetail** — `StreamAcpReplay`'s `SNAPSHOT_THEN_LIVE`/`LIVE_ONLY` tool frames drop `raw_input`/`raw_output` (keeping title/status/kind/id), fetched on demand via a new unary `GetAcpToolCallDetail`, so the stream's size tracks tool-call count not I/O volume. **tddy-service**: `proto/connection.proto` `GetAcpToolCallDetail` rpc + req/resp; `acp_replay` `strip_tool_body` + `tool_call_detail`/`ToolCallDetail`. **tddy-daemon**: strip in `acp_replay_frame` (snapshot + live `relay_acp_replay`) + `get_acp_tool_call_detail` (auth + `ExecuteTool`-style peer-forward + `NOT_FOUND`). **tddy-coder**: strip in `replay_frame_bytes` + `GetAcpToolCallDetail` arm. `COUNT_THEN_LIVE`/`StreamSessionActivity` unchanged; web adoption of the lookup is a follow-up. Tests: tddy-service acp_replay 5, tddy-daemon 4, tddy-coder 3; `clippy --tests -D warnings` + fmt clean. Feature [acp-replay-lazy-tool-bodies.md](../../ft/coder/acp-replay-lazy-tool-bodies.md). Package indexes [tddy-service](../../../packages/tddy-service/docs/changesets/), [tddy-daemon](../../../packages/tddy-daemon/docs/changesets/), [tddy-coder](../../../packages/tddy-coder/docs/changesets/). PR [#345](https://github.com/uppin/tddy-coder/pull/345). (tddy-service, tddy-daemon, tddy-coder)
