# Validate Tests Report

## Commands run

| Command | Exit status | Notes |
|--------|-------------|--------|
| `./dev cargo test --workspace --no-fail-fast` | **0** | Completed successfully (~193 s wall clock including compile). |

No fallback (`./verify`) was required: the full workspace run finished without timeout or failure.

Compiler emitted: `warning: Git tree '...' is dirty` (informational only).

## Summary

**All tests passed.** No failing test names to list.

Across the workspace, every `test result:` line reported `0 failed` for lib tests, integration tests, and doc tests (with expected ignored doc tests in a few crates).

## Coverage gaps (relative to integration-base / main-branch-ref feature)

Aligned with [evaluation-report.md](./evaluation-report.md):

1. **`main_branch_ref` not wired through daemon**  
   Current tests exercise storage, validation, and core worktree helpers. There is no automated proof that the daemon or TDD session path calls worktree setup with a per-project `main_branch_ref` from `projects.yaml` / `ProjectData`.

2. **Missing E2E: `projects.yaml` drives worktree HEAD**  
   `worktree_uses_configured_project_base_ref` in integration tests validates behavior against a main-only remote via heuristic resolution; it does not assert that a persisted project entry (with explicit integration base / main branch ref) is what selects the checked-out HEAD.

3. **Double-fetch performance not tested**  
   `setup_worktree_for_session` can run `git fetch origin` in default-branch resolution and again in `fetch_integration_base`. No test asserts fetch count, documents the tradeoff, or guards against accidental extra fetches in refactors.

4. **Proto/RPC/UI**  
   Beyond unit/integration scope here: connection paths still surface `main_branch_ref: None` where the PRD expects project-scoped wiring—no test currently closes that loop.

## Recommendations for additional tests

1. **Daemon/service integration** (when wiring lands): a test that creates `projects.yaml` with a non-default integration base, starts or simulates a session tied to that project, and asserts the worktree HEAD matches the configured ref (or the resolved effective ref).

2. **Explicit `ProjectData` roundtrip → worktree**: extend or add an integration test that writes `ProjectData` with `main_branch_ref` / integration base set, runs the same code path the product uses for worktree creation, and compares `git rev-parse HEAD` to the expected commit.

3. **Fetch accounting** (unit or integration with a scripted `git` wrapper): stub or spy on `git fetch` invocations for `resolve_default_integration_base_ref` + `fetch_integration_base` paths; assert at most one fetch per logical operation after any refactor, or document two fetches as intentional with a regression test if the contract changes.

4. **Regression for `origin/master` vs `origin/main`**: keep or add focused tests when both refs exist on the remote so default resolution order remains explicit and stable.

5. **Acceptance**: once `daemon_service` / hooks pass project context into `setup_worktree_for_session`, add an acceptance test in `tddy-daemon` or `tddy-integration-tests` that mirrors the real RPC → worktree flow.

---

*Generated by validate-tests subagent. Full suite: exit code 0.*
