# Changeset: tddy-vm — General-purpose VM build and lifecycle management

**Date:** 2026-06-21
**Status:** In development (red phase)
**Feature:** [docs/ft/vm/tddy-vm.md](../../ft/vm/tddy-vm.md)

## TODO

- [ ] Create/update PRD documentation — ✅ done (`docs/ft/vm/tddy-vm.md`)
- [ ] Create changeset — ✅ done (this file)
- [ ] Write failing acceptance tests (`packages/tddy-vm/tests/vm_registry_acceptance.rs`, `build_image_acceptance.rs`; `packages/tddy-daemon/tests/vm_service_acceptance.rs`; Cypress `VmsScreen.cy.tsx`)
- [ ] Write failing unit tests (`packages/tddy-vm/tests/qemu_args_unit.rs`, `registry_unit.rs`; web `appRoutes.test.ts`)
- [ ] Implement `VmManager` methods (full logic)
- [ ] Implement `build_vm_image` (full logic)
- [ ] Wire `VmsAppPage` to live `vm.VmService` RPC
- [ ] All tests passing

## Packages changed

### `packages/tddy-vm` (new)
- New crate: `Vm` trait, `QemuVm`, `MockVm`, `QemuVmArgs`, `VmManager` (stub), `VmSpec`, `VmState`, `build_vm_image` (stub), `VmServiceImpl` (stub).

### `packages/tddy-service`
- `proto/vm.proto`: new `VmService` with 7 RPCs.
- `build.rs`: codegen for `vm.proto` (TddyServiceGenerator + descriptor pass).
- `src/lib.rs`: `pub mod vm { include!(...) }`, `pub use proto::vm::VmServiceServer`.

### `packages/tddy-daemon`
- `Cargo.toml`: `tddy-vm` dep added.
- `src/main.rs`: `VmManager` + `VmServiceImpl` constructed; `vm.VmService` ServiceEntry pushed.

### `packages/tddy-demo-runner`
- `src/vm.rs`, `src/qemu.rs`, `src/mock.rs` deleted.
- `Cargo.toml`: `tddy-vm` dep added; `serde`, `serde_json` removed.
- `src/lib.rs`: re-exports from `tddy_vm` instead of own modules.
- `src/orchestrator.rs`: uses `tddy_vm::{Vm, VmError, RunningVm, PortForward}`.
- `tests/`: imports updated to use `tddy_vm` types (`QemuVm`, `MockVm`, `VmConfig`, `PortForward`).

### `packages/tddy-daemon` (connection_service.rs)
- `DemoVmHandle::Running.vm`: `tddy_vm::RunningVm`.
- `start_demo_vm` / `stop_demo_vm`: uses `tddy_vm::QemuVm` + `tddy_vm::Vm` + `tddy_vm::VmConfig`.

### `packages/tddy-web`
- `src/routing/appRoutes.ts`: `VMS_ROUTE`, `isVmsPath()`.
- `src/components/vms/VmsScreen.tsx`: presentational VM table component.
- `src/components/vms/VmsAppPage.tsx`: stub container (RPC wiring deferred).
- `src/index.tsx`: `/vms` routing branch.
- `src/components/shell/DaemonNavMenu.tsx`: "VMs" menu item.
- `src/gen/vm_pb.ts`: generated TypeScript client types for `VmService`.
