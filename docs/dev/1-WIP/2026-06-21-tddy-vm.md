# Changeset: tddy-vm — General-purpose VM build and lifecycle management

**Date:** 2026-06-21
**Status:** Implementation complete — follow-up items deferred
**Feature:** [docs/ft/vm/tddy-vm.md](../../ft/vm/tddy-vm.md)

## TODO

- [x] Create/update PRD documentation — `docs/ft/vm/tddy-vm.md`
- [x] Create changeset — this file
- [x] Write failing acceptance tests (`packages/tddy-vm/tests/vm_registry_acceptance.rs`, `build_image_acceptance.rs`; `packages/tddy-daemon/tests/vm_service_acceptance.rs`; Cypress `VmsScreen.cy.tsx`)
- [x] Write failing unit tests (`packages/tddy-vm/tests/qemu_args_unit.rs`, `registry_unit.rs`; web `appRoutes.test.ts`)
- [x] Implement `VmManager` methods (full logic + JSON persistence)
- [x] Implement `build_vm_image` (tddy-build executor integration)
- [x] Implement `VmServiceImpl` (all 7 RPC methods wired to VmManager)
- [x] All tests passing (24 Rust tests, 23 web tests)
- [x] Wire `VmsAppPage` to live `vm.VmService` RPC — ListVms/StartVm/StopVm/RemoveVm/DefineVm/BuildVmImage all wired
- [x] Add `DefineVmPanel` — build image + create VM form (Added: 2026-06-21)
- [ ] Redesign `DefineVmPanel`: buildroot spec textarea (replaces build-target text input); image dropdown (replaces single builtImagePath display) — in progress (Added: 2026-06-21)
- [ ] Add session token validation to `VmServiceImpl` — currently accepts any token; daemon wiring in `main.rs` does not yet pass a user resolver to VmService (security follow-up)

## Packages changed

### `packages/tddy-vm` (new)
- New crate: `Vm` trait, `QemuVm`, `MockVm`, `QemuVmArgs`, `VmManager` (full impl with JSON persistence), `VmSpec`, `VmState`, `build_vm_image`, `VmServiceImpl`.

### `packages/tddy-service`
- `proto/vm.proto`: new `VmService` with 7 RPCs.
- `build.rs`: codegen for `vm.proto` (TddyServiceGenerator + descriptor pass).
- `src/lib.rs`: `pub mod vm { include!(...) }`, `pub use proto::vm::VmServiceServer`.

### `packages/tddy-daemon`
- `Cargo.toml`: `tddy-vm` dep added.
- `src/main.rs`: `VmManager` + `VmServiceImpl` constructed; `vm.VmService` ServiceEntry pushed.

### `packages/tddy-demo-runner`
- `src/vm.rs`, `src/qemu.rs`, `src/mock.rs` deleted.
- `Cargo.toml`: `tddy-vm` dep added; `serde`, `serde_json` removed.
- `src/lib.rs`: re-exports from `tddy_vm` instead of own modules.
- `src/orchestrator.rs`: uses `tddy_vm::{Vm, VmError, RunningVm, PortForward}`.
- `tests/`: imports updated to use `tddy_vm` types (`QemuVm`, `MockVm`, `VmConfig`, `PortForward`).

### `packages/tddy-daemon` (connection_service.rs)
- `DemoVmHandle::Running.vm`: `tddy_vm::RunningVm`.
- `start_demo_vm` / `stop_demo_vm`: uses `tddy_vm::QemuVm` + `tddy_vm::Vm` + `tddy_vm::VmConfig`.

### `packages/tddy-web`
- `src/routing/appRoutes.ts`: `VMS_ROUTE`, `isVmsPath()`.
- `src/components/vms/VmsScreen.tsx`: presentational VM table component.
- `src/components/vms/VmsAppPage.tsx`: container — wires BuildVmImage/DefineVm/ListVms/StartVm/StopVm/RemoveVm RPCs; holds `building`, `availableImages`, `buildError` state.
- `src/components/vms/DefineVmPanel.tsx`: presentational — buildroot spec textarea + image dropdown + VM name + Create button. (Added: 2026-06-21; being redesigned — see TODO)
- `cypress/component/DefineVmPanel.cy.tsx`: component tests for DefineVmPanel (10 passing; being updated for redesign).
- `src/index.tsx`: `/vms` routing branch.
- `src/components/shell/DaemonNavMenu.tsx`: "VMs" menu item.
- `src/gen/vm_pb.ts`: generated TypeScript client types for `VmService`.
