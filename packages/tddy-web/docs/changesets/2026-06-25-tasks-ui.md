# 2026-06-25 — **Tasks UI

**Type:** Feature

real-time two-pane view** — `TasksDrawerScreen`: left drawer (280px, newest-first) + right output pane; `TaskDrawerItem`: status dot, kind, inline Cancel; `TaskOutputPane`: per-channel tabs, `TaskChannelOutput` auto-scroll; `useTaskListStream`: `Map<taskId,TaskInfo>` via `WatchTaskList` stream; `useTaskChannelStream`: UTF-8 via `WatchTask`; route `/tasks` now renders `TasksDrawerScreen` (replaced polling `TasksAppPage`, deleted). 7 Cypress component tests. Feature [tasks-ui-realtime.md](../../../../docs/ft/web/tasks-ui-realtime.md). (tddy-web)
