# 2026-06-21 — Session Drawer Screen

**Type:** Feature

`#/sessions` route; `SessionsDrawerScreen` with `SessionDrawer` (newest-first, derived label, status dot, tooltip) + `SessionDetailPane` (terminal when connected; Resume/metadata when disconnected); `useSessionAttachment` hook; shadcn `tooltip.tsx` + `scroll-area.tsx`; `sessionDrawerLabel`, `connectionStatusForSession`, `sortSessionsByCreation`. Feature [session-drawer.md](../../../../docs/ft/web/session-drawer.md). (tddy-web)
