# 2026-06-21 — **tddy-vm

**Type:** Feature

/vms web page: VmsScreen, VmsAppPage, DefineVmPanel, ListVmImages** — `appRoutes.ts`: `VMS_ROUTE`/`isVmsPath()`; `VmsScreen.tsx`: VM table (name/state/ssh-port/share-url/start/stop/remove); `VmsAppPage.tsx`: wires all 8 `VmService` RPCs; `DefineVmPanel.tsx`: Buildroot spec textarea + image dropdown + VM-name + Create; `loadImages()` fetches `listVmImages` on mount + after STAGE_DONE (dropdown survives reload); live build log via `for await...of` server stream; `DaemonNavMenu.tsx`: "VMs" nav item; `src/gen/vm_pb.ts` regenerated. Feature [vm/tddy-vm.md](../../../../../docs/ft/vm/tddy-vm.md). (tddy-web)
