# 2026-06-15 — RPC Playground screen

**Type:** Feature

`/rpc-playground` route (hash-based routing; no 404 on reload); participant picker (coder-role only); `RpcPlaygroundScreen` + `RpcPlaygroundAppPage`; request editor (builder ↔ raw JSON); streaming panel; `invoke.ts` with `sessionToken` auto-injection; `DaemonNavMenu` entry; `appRoutes.ts` `isRpcPlaygroundPath`. Feature [rpc-playground.md](../../../../../docs/ft/daemon/rpc-playground.md); product [daemon/changelog/](../../../../../docs/ft/daemon/changelog/). (tddy-web)
