# 2026-09-14 — Split create-session SSH host picker

**Type:** Feature

`sshConfigListDaemonId` and `CreateSessionSshConfigSelect` list OpenSSH `Host` aliases from the
codebase host when placement is split; co-located sessions still list the session host. Split
acceptance in `CreateSessionSplitSshAcceptance.cy.tsx`. See
[remote-managed-worktree.md](../../../../docs/ft/daemon/remote-managed-worktree.md) § Split SSH.
