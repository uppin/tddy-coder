# 2026-09-14 — Hosts row SSH connections cell

**Type:** Feature

`HostRowSshConfig` calls `ListSshConfigHosts` per host and renders explicit `Host` aliases, "No SSH
hosts", or "Could not check". Mounted from `HostRowTooling`. Documented in
[hosts-screen.md](../hosts-screen.md).
