# 2026-04-04 — URL routing (daemon terminal deep links)

**Type:** Feature

**`src/routing/appRoutes.ts`**; **`ConnectionScreen`** History API (push/replace, **popstate**, deep link); **`App`** standalone **`/terminal/*` → `/`**; **`web_bundle_acceptance`** SPA fallback for **`GET /terminal/...`**. Cypress **`AppRouting.cy.tsx`**, Bun **`appRoutes.test.ts`**, optional e2e deep-link spec. Feature doc: [web-terminal.md](../../../../../docs/ft/web/web-terminal.md). (tddy-web, tddy-coder)
