# Changesets Applied

Wrapped changeset history for tddy-web.

**Merge hygiene:** [Changelog merge hygiene](../../../docs/dev/guides/changelog-merge-hygiene.md) — prepend one single-line bullet; do not rewrite shipped lines.

- **2026-07-27** [Feature] session-agent: `SessionMainPane` gains an **"Add agent"** header button (`session-agents-add-btn`) that opens `CreateSessionPane` in **peer mode** to spawn a peer child session sharing the selected session's **worktree** via `StartSession.repo_path` (`stackParent` = current session, same `projectId`/`daemonInstanceId`, `repoPath` = current session's `repoPath`, separate `sessionType`/`agent`/`model`). In peer mode the branch selectors, PR-stack parent picker, **Project** and **Host** selectors are hidden (irrelevant/locked when reusing the orchestrator's worktree), and submit sends the frozen orchestrator `projectId`/`daemonInstanceId` (`effectiveProjectId`/`effectiveDaemonInstanceId`) so a peer always matches its worktree. New `SessionAgentsSection` (`src/components/sessions/SessionAgentsSection.tsx`) lists peers (children via `orchestratorSessionId`, derived by the pure `sessionPeers(sessions, currentSessionId)` in `src/utils/sessionPeers.ts`) with `agent`/`model`/`status` and a switch button (`session-agents-switch-<id>`) that selects the peer in the drawer; empty state when there are none. `SessionsDrawerScreen` wires peer spawn through the existing `onChildSessionStarted` optimistic overlay. A capture ref (`peerCreateCaptureRef`) keeps the overlay firing even if the drawer selection changes mid-flight; a keyed remount (`peer`/`standalone`) resets pane state on a peer→standalone transition so a standalone submit never carries a stale `stackParent`. Also fixes a latent `cursor-cli` `stackParent` hardcode so cursor-cli peers get their back-reference. Reuses `stack_parent`/`orchestratorSessionId` — no proto/daemon changes, no new deps. Tests: `sessionPeers` unit 5, Cypress `SessionAgentsSection` 4 / `SessionMainPanePeerSwitch` 4 / `SessionsDrawerPeerSpawn` 4 + `CreateSessionPane`/`CreateSessionCursorCli`/`CreateSessionCreateRemoteBranch`/`PrStackStartSessionModal` regression 34 = 51/51; `bun run build` clean; `tsc --noEmit` no new errors in edited source. Feature [session-drawer.md § Session Agents](../../../docs/ft/web/session-drawer.md#session-agents-peer-agent-sessions). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). PR [#357](https://github.com/uppin/tddy-coder/pull/357). (tddy-web) the PR-Stack Chat Screen stops dead-ending. New pure modules `prstack/isNodeOrphaned.ts` (`sessionId` set **and** the resolution has arrived **and** `session.exists === false` — an absent resolution is *unknown*, never orphaned, so the first render is deterministic) and, in `prstack/deriveStackBaseBranch.ts`, `resolveStackBase(node, nodes) -> StackBase` (`default-branch` | `ancestor-branch` | `no-ancestor-branch` — the discriminated form of the walk, because the flattened label collapses a root, an all-merged chain and a chain with no ancestor branch to the same string while only the last is unstartable; `deriveStackBaseBranch` is now a thin wrapper, unchanged in behaviour) plus `branchlessNonMergedParent(node, nodes)` (the flat direct-parent form of the daemon's own gate — `Stack::base_ref_for_spawn` refuses on **any** non-merged branchless parent while `resolveStackBase` walks past one to a sibling that owns a ref, so both are needed: the base names the dialog's label, this decides startability). `PlannedPrRow` renders the branch (`pr-stack-branch-<nodeId>`), the planned suggestion marked as planned (`pr-stack-planned-branch-<nodeId>`), `"Missing branch: <base>"` in place of the CTA (`pr-stack-missing-branch-<nodeId>`), and `"PR status unavailable"` with the reason as its tooltip (`pr-stack-pr-unavailable-<nodeId>`); the CTA slot is now mutually exclusive across button / blocked indicator / status chip. New `PlannedPrPanel` — always mounted, `data-state` ∈ `{closed, open}`, `absolute top-0 right-0 z-10`, 360px docked column vs full-screen overlay, header toggle + own close control (`pr-stack-planned-pr-panel{,-toggle,-close}`) — replaces the hardcoded `w-1/2` split; `relative` sits on the screen's **content row** rather than the root so the panel never covers the toggle that opens it, and width/closed-visibility are **inline styles driven by `isMobile`** rather than `md:` classes because component tests mount without the app stylesheet (the idiom `SessionDrawer` already establishes). `PrStackScreen` seeds the panel open on desktop / closed on mobile (`detectIsMobile` + `useIsMobile`), adds each node's **base** branch to the `useQueryBranch` poll set (startability is a property of the base, and an unspawned node owns no branch to query), and pre-fills `branchIntent: "work_on_selected_branch"` + the new `CreateSessionInitialValues.selectedBranch` when the node owns a branch — `new_branch_from_base` would fail on "branch already exists", which is exactly the orphaned node this path recovers. **`usePrStatus` is deleted** (no remaining caller) and `prStatusByBranch` / `prStatus` are off `PlannedPrList` / `PlannedPrRow`: both RPCs funnel into the same authenticated `GET /pulls`, so polling both was 2 × 5s × branches ≈ 1440 requests/hour/branch against a 5000/hour limit — exhausted within the hour on a five-node stack, after which every row read "PR status unavailable" permanently. Tests: `PrStackOrphanedNodeAcceptance` 5, `PrStackMissingBranchAcceptance` 6, `PrStackBranchAndPrVisibilityAcceptance` 6, `PrStackPlannedPrPanelAcceptance` 7, `PrStackLiveStatusAcceptance` re-driven through `QueryBranch` with every pinned behaviour unchanged — PR-stack Cypress 91/92 (`PrStackChatSystemMessagesAcceptance` fails on `master` too), bun unit 587/587 incl. `isNodeOrphaned` 6 and 5 added `resolveStackBase` cases. Feature [session-drawer.md § PR-Stack Chat Screen](../../../docs/ft/web/session-drawer.md#pr-stack-chat-screen). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-26** [Fix] pr-stack-repoint-dead-end: new pure `prstack/startBlockers.ts` — `startBlockers` returns **every** reason a node cannot be started, each carrying the operator-facing text (replacing a boolean + one branch name that could state a single reason and no message), and `resolveRepointTarget` returns the branch a repoint would land on, which both labels the control and is sent as `target_base_branch`. Both compose the existing `resolveStackBase` / `branchlessNonMergedParent` rather than re-walking the DAG, so the cycle and dangling-parent guards are inherited; `no-ancestor-branch` is reported only when no direct parent is the blocker, or the warning would state one fact twice. `PlannedPrRow` renders the planned PR's full information whatever its startability — plus a new `pr-stack-base-branch-<id>` line — with Start-session **disabled** (blockers joined into its `title`) beside `pr-stack-start-warning-<id>`; the `pr-stack-missing-branch-<id>` replacement indicator is **gone** (D16). `PrStackScreen` gains `defaultBranch` (threaded from `SessionMainPane`'s already-loaded `projects` via `WorkflowViewContext`, which also gives a root node's dialog a named base for the first time), sends `targetBaseBranch`, **catches** repoint failures into a per-node `pr-stack-repoint-error-<id>` (it previously `await`ed with no `catch`, so the new `invalid_argument` refusal would have left the row looking untouched), and guards re-entrancy with `repointingNodeIds` — a double-click had run two rebase + force-push sequences against one branch. `baseBranch` → `baseBranchLabel` (it holds prose such as "no predecessor branch yet"). **`test:unit` is now invoked by the `test` scripts**; it was reachable by nothing, so every unit test under `src/components/` (477) ran only when someone typed it. Tests: `startBlockers.test.ts` 14, `PrStackRepointDeadEndAcceptance.cy.tsx` 14, `PrStackMissingBranchAcceptance.cy.tsx` 6 + `PrStackOrphanedNodeAcceptance.cy.tsx` 5 migrated to the disabled-CTA contract. Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-26** [Fix] pr-stack-ux-recovery: the PR-Stack Chat Screen stops dead-ending. New pure modules `prstack/isNodeOrphaned.ts` (`sessionId` set **and** the resolution has arrived **and** `session.exists === false` — an absent resolution is *unknown*, never orphaned, so the first render is deterministic) and, in `prstack/deriveStackBaseBranch.ts`, `resolveStackBase(node, nodes) -> StackBase` (`default-branch` | `ancestor-branch` | `no-ancestor-branch` — the discriminated form of the walk, because the flattened label collapses a root, an all-merged chain and a chain with no ancestor branch to the same string while only the last is unstartable; `deriveStackBaseBranch` is now a thin wrapper, unchanged in behaviour) plus `branchlessNonMergedParent(node, nodes)` (the flat direct-parent form of the daemon's own gate — `Stack::base_ref_for_spawn` refuses on **any** non-merged branchless parent while `resolveStackBase` walks past one to a sibling that owns a ref, so both are needed: the base names the dialog's label, this decides startability). `PlannedPrRow` renders the branch (`pr-stack-branch-<nodeId>`), the planned suggestion marked as planned (`pr-stack-planned-branch-<nodeId>`), `"Missing branch: <base>"` in place of the CTA (`pr-stack-missing-branch-<nodeId>`), and `"PR status unavailable"` with the reason as its tooltip (`pr-stack-pr-unavailable-<nodeId>`); the CTA slot is now mutually exclusive across button / blocked indicator / status chip. New `PlannedPrPanel` — always mounted, `data-state` ∈ `{closed, open}`, `absolute top-0 right-0 z-10`, 360px docked column vs full-screen overlay, header toggle + own close control (`pr-stack-planned-pr-panel{,-toggle,-close}`) — replaces the hardcoded `w-1/2` split; `relative` sits on the screen's **content row** rather than the root so the panel never covers the toggle that opens it, and width/closed-visibility are **inline styles driven by `isMobile`** rather than `md:` classes because component tests mount without the app stylesheet (the idiom `SessionDrawer` already establishes). `PrStackScreen` seeds the panel open on desktop / closed on mobile (`detectIsMobile` + `useIsMobile`), adds each node's **base** branch to the `useQueryBranch` poll set (startability is a property of the base, and an unspawned node owns no branch to query), and pre-fills `branchIntent: "work_on_selected_branch"` + the new `CreateSessionInitialValues.selectedBranch` when the node owns a branch — `new_branch_from_base` would fail on "branch already exists", which is exactly the orphaned node this path recovers. **`usePrStatus` is deleted** (no remaining caller) and `prStatusByBranch` / `prStatus` are off `PlannedPrList` / `PlannedPrRow`: both RPCs funnel into the same authenticated `GET /pulls`, so polling both was 2 × 5s × branches ≈ 1440 requests/hour/branch against a 5000/hour limit — exhausted within the hour on a five-node stack, after which every row read "PR status unavailable" permanently. Tests: `PrStackOrphanedNodeAcceptance` 5, `PrStackMissingBranchAcceptance` 6, `PrStackBranchAndPrVisibilityAcceptance` 6, `PrStackPlannedPrPanelAcceptance` 7, `PrStackLiveStatusAcceptance` re-driven through `QueryBranch` with every pinned behaviour unchanged — PR-stack Cypress 91/92 (`PrStackChatSystemMessagesAcceptance` fails on `master` too), bun unit 587/587 incl. `isNodeOrphaned` 6 and 5 added `resolveStackBase` cases. Feature [session-drawer.md § PR-Stack Chat Screen](../../../docs/ft/web/session-drawer.md#pr-stack-chat-screen). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-26** [Feature] start-session-attachments: `src/gen/connection_pb.ts` regenerated (`bunx buf generate ../tddy-service/proto`, +453 lines) for `StartSessionRequest.attachments`, `SessionAttachment`/`StagedAttachmentRef`/`HostDocumentRef`/`StagedAttachmentEntry`, `HostDocumentScope`, and the three staging RPCs — **codegen only, no hand-written web code**: no picker, no host-document browser, nothing calls the new RPCs (the daemon returns `UNIMPLEMENTED`). `docs/terminal-file-upload.md` gains a "Reuse: start-session attachments" note recording that `chunkFile()`/`UPLOAD_CHUNK_SIZE` and the per-file chunk loop carry over unchanged (only the request builder differs) and that a staged batch is usable only by a session started on the host it was uploaded to. Verified by `bun run build`; `src/buildId.ts` deliberately not committed. Docs [terminal-file-upload.md](terminal-file-upload.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-26** [Feature] versionless-claude-aliases: `src/constants/claudeCliModels.ts` loses the hardcoded `CLAUDE_CLI_MODELS` array and its `ClaudeCliModel` interface (dead since `ConnectionScreen.tsx` was removed — only tests still imported them), keeping just the `isClaudeCliSession` / `isCursorCliSession` / `isCliTerminalSession` predicates; the web now holds no model list at all and every dropdown renders `ListAgentModels`' `models` preselecting its `default_model`. The testkit gains `DEFAULT_CLAUDE_CLI_MODELS` / `DEFAULT_CLAUDE_CLI_MODEL` in `cypress/support/rpc/responses.ts` (mirroring `tddy_core::backend::claude_cli_models`) wired into `connectionServiceBackend`'s `listAgentModels` fixture. Tests: `CreateSessionAcceptance` gains a preselection test and sources its dropdown expectation from the fixture; `claudeCliModels.test.ts` swaps the deleted-constant block for `isCursorCliSession`/`isCliTerminalSession` coverage — unit 318/318, `CreateSession*` Cypress 81/81. Feature [tool-session-model-selection.md](../../../docs/ft/web/tool-session-model-selection.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-26** [Fix] acp-tool-detail-explicit-states: reworks the lazy tool-body lookup landed by acp-replay-lazy-tool-bodies-web (#347), which left several "nothing to show" cases indistinguishable from each other. `useToolCallDetail` → new **`useAcpToolCallDetail`** (`components/chat/`): the cache is read in a **render-time `useMemo`** keyed on `(sessionId, toolCallId, cacheable)` rather than inside the effect (a cached row paints its JSON on the first frame, and a row switch cannot show the previous row's bodies); loading/error state is local to the hook instead of stored in the shared registry; an **empty `toolCallId` short-circuits to `missingId` with no request**; a `ConnectError` is classified `notFound` vs `failed` so `NOT_FOUND` keeps the signal the Rust side goes out of its way to send; resolutions are tagged `forSessionId`/`forToolCallId` so a stale answer reads as loading. `AgentActivityRegistry.setBody`/`getBody`/`ToolCallBodyState` → **`setToolDetail`** + `toolDetails: ReadonlyMap<string, ToolCallDetail>` (bodies only, no transport state; no-op when unchanged), and a **`running`** call is excluded from both the cache read and write — its partial body was previously cached for the life of the page. `AgentActivityDetailDialog` gains an `animate-pulse` **skeleton** (`agent-activity-detail-skeleton`, replacing the `agent-activity-detail-loading` text line), always renders the Output heading with an explicit `agent-activity-detail-no-output` note worded apart for running vs settled calls (it previously vanished), adds `agent-activity-detail-no-input` in place of an empty highlighted block, and renders **curated** error text per cause instead of the raw transport message. Tests: `AgentActivityDetailLazyBodyAcceptance` (6) → **`AgentActivityDetailLazyFetchAcceptance` (7)** + new **`AgentActivityDetailAbsentBodyAcceptance` (4)**; `AgentActivityDetailDialogAcceptance` 6; bun `agentActivityRegistry` 8; `test:unit` glob extended to `src/components/sessions`. Feature [agent-activity-pane.md](../../../docs/ft/web/agent-activity-pane.md#rendering-an-unanswered-lookup-updated-2026-07-26). (tddy-web)
- **2026-07-26** [Fix] pr-stack-branch-gated-spawn: `deriveStackBaseBranch` no longer previews `branch ?? branchSuggestion` — only a **created** `branch` names a ref, so a parent holding just a suggestion is passed over like an absent one and the Start-Session dialog can no longer promise a base the daemon's branch-gated spawn then refuses. Feature [session-drawer.md](../../../docs/ft/web/session-drawer.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-25** [Feature] acp-replay-lazy-tool-bodies-web: the Agent Activity detail dialog fetches a clicked tool call's `raw_input`/`raw_output` on demand via the new unary `GetAcpToolCallDetail` (regen `connection_pb.ts`) instead of reading them off the now body-less replay stream. `useAcpReplay` carries the frame's `tool_call_id` on the tool `ChatMessage` (dropping the dead `ChatMessage.rawInput`/`rawOutput` fields); new `useToolCallDetail` hook does fetch-once/cache/retry-on-error over an `AgentActivityRegistry` body cache keyed by `(sessionId, callId)` (stored separately so a body write leaves the transcript snapshot reference-stable); `AgentActivityDetailDialog` renders loading/error/loaded and `AgentActivityOverlay` threads `sessionId`/`sessionToken`/`client` to it. Tests: bun `agentActivityRegistry` 7, Cypress `AgentActivityDetailLazyBodyAcceptance` 6 + `AgentActivityDetailDialogAcceptance` 3 (Agent Activity regression 17); vite build clean. Web adoption of the [acp-replay-lazy-tool-bodies.md](../../../docs/ft/coder/acp-replay-lazy-tool-bodies.md) follow-up. Feature [agent-activity-pane.md § 4](../../../docs/ft/web/agent-activity-pane.md#4-lazy-tool-bodies--fetch-on-click-added-2026-07-25). PR [#347](https://github.com/uppin/tddy-coder/pull/347). (tddy-web)
- **2026-07-25** [Feature] branch-query-and-remote-branch: new `useQueryBranch(client, sessionToken, orchestratorId, branches)` hook (sibling of `usePrStatus`, per-branch polled → `Record<string, BranchResolution>`) threaded `PrStackScreen → PlannedPrList → PlannedPrRow` to render worktree (`pr-stack-worktree-<nodeId>`), in-progress session (`pr-stack-session-<nodeId>`), and PR link/state from `QueryBranch` (additive; `usePrStatus`/`resolveNodeSession` kept). `CreateSessionPane` gains a pre-checked "Create Remote Branch" checkbox (`create-session-create-remote-branch-toggle`, claude-cli/cursor-cli, new-branch mode) sending `createRemoteBranch` and a "New branch from base: `<baseBranchLabel>`" caption; `CreateSessionInitialValues` gains `createRemoteBranch`/`baseBranchLabel`; new pure `utils/deriveStackBaseBranch.ts`. Feature [pr-stack-live-status.md](../../../docs/ft/coder/pr-stack-live-status.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-25** [Feature] worktree-disk-usage-streaming: the Worktrees manager (`WorktreesAppPage`/`WorktreesScreen`) and the Session Inspector Worktree tab (`SessionWorktreeTab`) move onto `StreamWorktreeStats` via a new `useWorktreeStatsStream` hook (folded with `applyWorktreeStatsEvent`), showing a per-worktree `None/Calculating/Cached` status + last-calculated time with Recalculate-all / per-row Calculate (`CalculateWorktreeSize`); the inspector tab drops its 10-minute `ListWorktreesForProject` poll (removes `WORKTREE_STATS_REFRESH_MS`). New `lib/worktreeSize.ts` (`formatLastCalculated` + `applyWorktreeStatsEvent`). Tests: Cypress `WorktreesStreamingAcceptance` 4 / `WorktreesScreenDiskUsage` 6 / `SessionWorktreeTabAcceptance` 6, bun `worktreeSize` 8. Feature [worktree-disk-usage-streaming.md](../../../docs/ft/web/worktree-disk-usage-streaming.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-25** [Feature] session-files-inspector: `SessionFilesTab` (Inspector "files" tab via `InspectorTabs`) lists `ListSessionUploads` newest-first with per-row Insert / Copy path / two-step Delete (`DeleteSessionUpload` then reload); rows are `draggable` and set the host path under `application/x-tddy-host-path` (`lib/hostPathDrag.ts`), and `TerminalFileDropZone.handleDrop` inserts that path via `joinQuotedPaths` **without re-uploading** (OS `files` drop unchanged). Drag-start and Insert both close the Inspector; the click/tap route reaches the focused terminal through a new `SessionRuntimeRegistry.setInsertInput` hook threaded `SessionsDrawerScreen → SessionMainPane → SessionInspectorDrawer` (`onInsertPathIntoTerminal`), with `SessionRuntime`/`GhosttyTerminalLiveKit`/`GhosttyTerminalGrpc` registering their `insertInput`. New insecure-origin-safe `lib/clipboard.ts`. Two-step delete is keyed per row (`upload_id`+`file_name`) so siblings from one drop don't co-arm. Tests: Cypress `SessionFilesTabAcceptance` 6, `SessionInspectorFilesTab` 2, `TerminalDropInsertHostPath` 2. Feature [session-files-inspector.md](../../../docs/ft/web/session-files-inspector.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)

- **2026-07-25** [Feature] enqueued-input-overlay: the gRPC terminal now assigns each sent chunk a cumulative byte offset (`GrpcSessionTerminal` sends `input_offset = BigInt(enqueue(data))`) and consumes `SessionTerminalOutput.acked_input_offset`. New `lib/terminalInputQueue.ts` (`classifyInput` keyboard/mouse/control + `TerminalInputQueue` offset accounting, ack-trim, `overlayModel` coalescing/overflow), `components/sessions/useEnqueuedInput.ts` (owns the queue + 500 ms reveal timer), and `components/connection/EnqueuedInputOverlay.tsx` (single-line overlay: text inline, `🖱×n` mouse run, `⋯+n` overflow). Feature [enqueued-input-overlay.md](../../../docs/ft/web/enqueued-input-overlay.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-25** [Feature] pr-stack-live-status: the PR-Stack Chat Screen now shows live per-node status. `resolveNodeSession(node, sessions)` resolves the in-progress child session by matching `node.branch` against `SessionEntry.branch` (in-progress indicator); `usePrStatus` polls `GetPrStatus` every `POLL_INTERVAL_MS` (5s) per rendered branch to render the PR number as a link + its state (open/merged/closed/draft); a Repoint control (shown when a parent PR is merged) calls `RepointPlannedPr` and applies the returned stack. `SessionsDrawerScreen` treats a pr-stack orchestrator as active while a child it owns is live, so it stays reachable in the drawer mid-flight. Feature [pr-stack-live-status.md](../../../docs/ft/coder/pr-stack-live-status.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)

- **2026-07-24** [Fix] **terminal-file-drop-insecure-origin — the dropped file's path actually reaches the terminal** — two independent bugs both ended in "drop a file, nothing happens". (1) `uploadFiles` minted the per-drop `upload_id` with `crypto.randomUUID()`, a **secure-context-only** API, so on a plain-http LAN origin the drop handler threw before any chunk was sent (mobile Attach shares the hook, so it failed identically) — new `lib/randomId.ts` `randomUuid()` prefers the native call, falls back to `crypto.getRandomValues` (available on insecure origins), then `Math.random`. (2) `UPLOAD_CHUNK_SIZE` 256 KiB made each request ~5 LiveKit chunk frames, and one lost frame leaves the daemon's `ChunkReassembler` permanently incomplete, so the RPC was never answered and the upload stalled mid-file with no error — chunk size drops to **48 KiB** with a new `UPLOAD_REQUEST_ENVELOPE_HEADROOM` (8 KiB), pinned against `MAX_CHUNK_FRAME_BYTES` imported from `tddy-livekit-web`, and `useDaemonUploadChunk` now sends every chunk with `{ timeoutMs }` (`UPLOAD_CHUNK_TIMEOUT_MS` = 20 s) so a stalled chunk fails that one file via the existing skip-and-report path. Verified end-to-end on a LAN browser → dev daemon claude-cli session (2 MB PNG, 41 chunk RPCs, path typed into Claude Code's prompt). Docs [terminal-file-upload.md](terminal-file-upload.md), [insecure-origin-constraints.md](insecure-origin-constraints.md). Feature [web-terminal.md § File drop upload](../../../docs/ft/web/web-terminal.md#file-drop-upload). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). PR [#336](https://github.com/uppin/tddy-coder/pull/336). (tddy-web)
- **2026-07-24** [Feature] terminal-file-drop-upload: `TerminalFileDropZone` (dragover overlay + drop) wraps both terminals and a mobile `TerminalUploadButton` (native multi-file picker) sits in the Keyboard strip; `useSessionFileUpload` chunks each file (`chunkFile`/`UPLOAD_CHUNK_SIZE`), uploads via `useDaemonUploadChunk` under one per-drop UUID, advances a shared `UploadProgressStore` (provider + `useUploadProgressSnapshot`), and types the successful files' paths via `shellQuote`'s `joinQuotedPaths` (escaped, trailing space, no newline; failed files skipped). `UploadProgressIndicator` renders the aggregate bar in `HostStatsFooter`; `SessionsDrawerScreen` wraps the tree in `UploadProgressProvider`. `GhosttyTerminalGrpc`/`GhosttyTerminalLiveKit` thread `insertInput` (`sendInput`/`pushInput`); `SessionLiveKitTerminal` enables `showMobileKeyboard`. Tests: `shellQuote`/`fileUploadChunks`/`uploadProgress` units 18, Cypress `TerminalFileDropUpload`/`TerminalFileUploadProgressFooter`/`MobileTerminalUploadButton`/`TerminalFileUploadFailure` 10. Feature [web-terminal.md § File drop upload](../../../docs/ft/web/web-terminal.md#file-drop-upload). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-24** [Feature] **statusbar-session-traffic — footer byte counter aggregates all attached sessions** — the screen-level `StatusBar` (→ `SessionTrafficStrip`) data plane changes from the focused session's room meter (`meterRegistry.get(attachment.livekitRoom)`) to a new `useAttachedSessionTraffic(runtimes, runtimeRegistry)` hook that sums every mounted runtime's per-session byte-tap counters (from [[inspector-byte-counter]]) for the totals and folds their positive advances into one shared `TrafficMeter` for the aggregate rate — so focused AND backgrounded runtimes contribute and the total is not reset by focus switches (a disconnected runtime is pruned). `runtimes`/`runtimeRegistry` thread `SessionsDrawerScreen` → `HostStatsFooter` → `StatusBar`; the shared meter-subscribe/interval logic is extracted to `useMeterSnapshot.ts` (reused by the HTTP meter). Tests: Cypress `SessionTrafficStrip` +3 (StatusBar aggregation), `SessionMainPaneTraffic` +3 (screen wiring + focused/backgrounded aggregate). Feature [session-drawer.md § Session Traffic Strip](../../../docs/ft/web/session-drawer.md#session-traffic-strip), [host-stats-footer.md](../../../docs/ft/web/host-stats-footer.md). (tddy-web)
- **2026-07-24** [Feature] **sessions-drawer-active-separator — Active/Remaining collapsible partitions in the session drawer** — the open `SessionDrawer` splits its list into an **Active** partition (green/yellow dots: `connectionStatusForSession(s) !== "disconnected"`, i.e. `isActive || pendingElicitation`) and a **Remaining** partition (grey/disconnected), separated by collapsible `Active (N)` / `Remaining (M)` headers. New `SessionDrawerSeparator` is a `<button>` disclosure (body hidden via `display:none`) — deliberately not a nested `<details>`, so stack-group `<details>` counts stay intact. New `partitionSessionsByActivity()` filters by dot first, then runs `groupSessionsByStack` on each side independently, so a mixed-activity PR-stack splits by each session's own dot; the separators render only when both partitions are non-empty (else a single flat list, unchanged). Active expanded / Remaining collapsed by default; selection mode passes `forceOpen` to both so bulk delete still spans them. Extracted `SessionPartitionBody` (dedupes the groups+flat render); added `sessionsDrawerPage.expandRemaining()` and adapted two mixed-fixture specs (`SessionInspectorAcceptance`, `SessionStartResumeStillDaemonRouted`) to expand Remaining before clicking a now-collapsed disconnected row. Tests: unit `sessionStackGroups.test.ts` +8 (14 total), Cypress `SessionsDrawerActiveSeparator` 8. Feature [session-drawer.md § Active / Remaining Partition Separator](../../../docs/ft/web/session-drawer.md#active--remaining-partition-separator). PR [#333](https://github.com/uppin/tddy-coder/pull/333). (tddy-web)
- **2026-07-24** [Fix] **inspector-byte-counter — Session Inspector I/O meter ticks live** — the Details tab's bytes in/out were stuck at `0` because `SessionRuntimeRegistry.recordBytes` was never called in production. `GhosttyTerminalLiveKit` gains an `onBytes` prop (held in `onBytesRef`) fired from the `streamTerminalIO` loop (`bytesIn = output.data.length` per received chunk, `bytesOut = data.length` per batched input yield); it threads through `SessionLiveKitTerminal` → `SessionRuntime` (`onSessionBytes`, incl. child runtimes) → `SessionMainPane` → `SessionsDrawerScreen`, which folds it into the runtime registry via new `makeByteTap(registry, sessionId, now?)` + `ByteDelta`. `recordBytes` now stamps `lastDataReceivedAt` only for inbound bytes (sending input no longer resets the "Ns ago" clock); the inspector renders counts via `formatBytes`. Distinct from the footer's transport-level `SessionTrafficStrip` meter. Tests: unit `sessionRuntimeRegistry.test.ts` 5 (makeByteTap in/out + inbound-only timestamp), Cypress `SessionInspectorByteCountAndLastReceived` 2. Feature [session-drawer.md § Inspector I/O bytes](../../../docs/ft/web/session-drawer.md#inspector-io-bytes--last-data-received). (tddy-web)
- **2026-06-26** [Feature] **byte-traffic — ConnectRPC/LiveKit byte-traffic meters + session traffic strip** — `TrafficMeter`/`TrafficMeterRegistry` (accumulator + 2 s sliding-window B/s rate, subscribe/notify, keyed by scope) fed by an HTTP `Interceptor` (`createTrafficInterceptor`, sizing unary req/res via `toBinary` + wrapping streaming iterables) and the LiveKit `LiveKitTransport` (`meter.record("in"/"out", payload.length)`), both wired in `transportProvider.tsx`. Adds `readRoomRtt`/`useLiveKitPing` (WebRTC `getStats()` RTT), `useSessionLiveKitRoom`, `formatTraffic` (`formatBytes`/`formatRate`/`formatPing`), and the presentational `SessionTrafficStrip`. Later relocated from the header into the screen-level footer (host-stats-footer, 2026-07-21). Tests: `trafficMeter`/`httpTrafficInterceptor`/`livekitPing`/`formatTraffic` units + Cypress `SessionTrafficStrip`/`SessionMainPaneTraffic`. Feature [session-drawer.md § Session Traffic Strip](../../../docs/ft/web/session-drawer.md#session-traffic-strip). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-24** [Feature] activities-as-acp: `AgentActivityOverlay` body becomes `<AgentChatView readOnly>` fed by new `useAcpReplay` (decodes `StreamAcpReplay` `AcpReplayFrame` bytes → `AcpAgentMessage`, coalesces tool frames by `tool_call_id`). `AgentChatView` gains a `readOnly` mode (tool-call cards + `+Ns` timing badge from `timestamp_unix_ms`); the streaming agent-chunk merge is extracted to shared `acpAgentMerge.ts`; `ChatMessage.from` gains `"tool"` + optional `toolStatus`. Legacy `AgentActivityAcceptance.cy.tsx` removed (row-list UI gone); Cypress `AgentActivityAcpReplayAcceptance` 8. Feature [agent-activity-pane.md](../../../docs/ft/web/agent-activity-pane.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-23** [Feature] **Session Inspector Worktree tab** — new `"worktree"` tab (`InspectorTabs`/`SessionInspectorDrawer`) rendering `SessionWorktreeTab` for the selected session's own worktree (keyed off `SessionEntry.repoPath`/`projectId`). `useSessionWorktreeStats` loads `ListWorktreesForProject` cache-first (`refresh:false`) then polls `refresh:true` on a 10-min `setInterval` (cleared on unmount), matching the row by `repoPath` and deriving a `missing` state client-side. The tab shows size/branch/changed-files + Refresh, two-step **Clear** (`cleanWorktree`) / **Delete** (`removeWorktree`), and a missing-state **Restore** (`restoreSessionWorktree`). `formatDiskBytes` extracted to shared `worktreeStatsFormat.ts` (also used by `WorktreesAppPage`). Cypress `SessionWorktreeTabAcceptance` 5. Feature [session-worktree-inspector.md](../../../docs/ft/web/session-worktree-inspector.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)

- **2026-07-23** [Feature] session-activity-streaming-mode: `useSessionActivity` gains an optional `mode` (default `SNAPSHOT_THEN_LIVE`, always sent on the request; overlay keeps the default so history populates); the detail dialog renders the structured `input`/`result` `Value` via `toJson(ValueSchema, …)` (bare strings shown as-is, else pretty-printed JSON). Regenerated `connection_pb.ts`. Feature [agent-activity-pane.md](../../../docs/ft/web/agent-activity-pane.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-23** [Feature] **unify-web-layout — shared AppShell, sessions drawer as the default route, dedicated LiveKit screen, unified theme** — all daemon-mode screens now render inside one `AppShell` (`src/components/shell/AppShell.tsx`, `scroll` + `fullbleed` variants) owning the top chrome (top-left hamburger `DaemonNavMenu` + title + `DaemonSelectorConnected` + `UserAvatar`, sourced from `useAuthContext`), so no screen can ship without the nav menu; `SessionsDrawerScreen`, `TasksDrawerScreen`, `ProjectsAppPage`, `VmsAppPage`, `WorktreesAppPage`, and `RpcPlaygroundAppPage` were migrated onto it. The **sessions drawer is now the default route** — `index.tsx`'s routed switch renders `<SessionsDrawerScreen>` as the catch-all (`#/`, `#/sessions`, `#/sessions/:id`) and the legacy monolithic `ConnectionScreen` + its `#/terminal/:id` route are removed, along with `ConnectionScreen`-only helpers `connection/agentOptions` and `connection/multiSessionState` (kept `terminalPresentation`); `appRoutes.ts` adds `LIVEKIT_ROUTE`/`isLiveKitPath` and drops `isSessionListPath`/`terminalPathForSessionId`/`terminalDeepLinkSessionPath` (keeps `parseTerminalSessionIdFromPathname` for the standalone hash-strip). `DaemonNavMenu` collapses the old "Sessions"(`/`) + "Sessions (new)"(`/sessions`) into one **Sessions** entry and adds a **LiveKit** entry. New **`LiveKitAppPage`** (`#/livekit`) renders the "Connected participants" panel extracted from `ConnectionScreen` (`ParticipantList` fed by `useRoomParticipants(room)` + `useObservedCommonRoomStatus(room)` off `useSelectedDaemon().room`). Ported into the drawer: bulk row-select + delete (`session-row-select-<id>` checkboxes in `SessionDrawerItem` → `sessions-drawer-bulk-delete`, deletes in selection order) and an unknown-session not-found state (`terminal-route-unknown-session[-home]`) shown in the **detail pane only** (drawer stays visible) when a `#/sessions/:id` deep link matches no loaded session. Standalone `ConnectionForm`/`DaemonLoginScreen` re-themed from inline hex to shadcn tokens. Tests: unit `appRoutes.test.ts` (`isLiveKitPath`; 31/0); Cypress `AppShell` 3, `UnifiedLayoutAcceptance` 3, `LiveKitScreenAcceptance` 1, `SessionsDrawerBulkDeleteAcceptance` 1, `SessionsDrawerUnknownDeepLinkAcceptance` 1, `StandaloneFormThemeAcceptance` 1, regression `SessionsDrawerAcceptance` 23/23; e2e `app-routing-daemon` (real GitHub-stub auth: `#/` → drawer + hamburger) plus a harness fix adding `livekit.api_secret` to `startDaemonWithDemoTui`'s generated daemon.yaml (session-token signing). Removed obsolete `ConnectionScreen.cy.tsx` / `ConnectionScreenSharedCommonRoomAcceptance.cy.tsx` / `sharedCommonRoomPage.tsx` and the `/terminal`-era `AppRouting.cy.tsx` / `app-routing-deep-link.cy.ts`. Feature [app-shell.md](../../../docs/ft/web/app-shell.md), [session-drawer.md](../../../docs/ft/web/session-drawer.md). WIP source `docs/dev/1-WIP/unify-web-layout.md` removed after wrap. (tddy-web)

- **2026-07-23** [Feature] **Project default-branch dropdown** — each `ProjectCard` gains a default-branch `<select>` (`project-default-branch-select-<projectId>`) listing the project's remote branches (loaded via `listProjectBranches` for the project's first host); the stored `ProjectEntry.mainBranchRef` shows selected, else it pre-selects `origin/master` (then `origin/main`, then the first branch) via `defaultSelectedBranch`. Choosing a branch calls the new `setProjectDefaultBranch` RPC (wired in `ProjectsAppPage`'s `useProjectsRpc`, addressed to the first host) and refreshes the list. `ProjectsScreen`/`ProjectCard` gain `onSetDefaultBranch` + `loadProjectBranches` props; `ProjectGroup` carries `mainBranchRef`. Regenerated `connection_pb.ts`. Tests: `ProjectsScreenAcceptance` 12 (7 existing + 5 new). Feature [projects-screen-multi-host.md](../../../docs/ft/web/projects-screen-multi-host.md#default-branch). (tddy-web)
- **2026-07-23** [Feature] **Inspector docks as the main pane for disconnected sessions** — the Session Inspector renders as the full main pane (not a ~360px overlay drawer) when the selected session is `disconnected`, driven by a new `data-docked` attribute on `sessions-inspector-drawer` derived from a pure `isInspectorDocked(session)` helper (`inspectorState.ts`; docked iff `connectionStatusForSession === "disconnected"`, so `connected`/`needs-input` stay drawers). `SessionInspectorDrawer` gains a `docked?` prop that switches the open/expanded footprint to `left-0 right-0 w-full` while keeping all header controls; `SessionMainPane` computes `docked = isInspectorDocked(selectedSession)`, passes it down, and suppresses the focused runtime while docked (`focused={!docked && r.sessionId === focusedRuntimeId}`) — so a disconnected session renders no `sessions-detail-terminal-container` and no "Claim terminal" overlay, while the runtime layer stays mounted (background streaming preserved). Tests: unit `inspectorState.test.ts` `isInspectorDocked` (5), Cypress `SessionInspectorDockedDisconnected` 5/5 (regression: `SessionInspectorAcceptance` 13, `TerminalControlAcceptance` 4, `SessionMainPaneTraffic` 4, `SessionMainPaneUnscopedSession` 1). Feature [session-drawer.md](../../../docs/ft/web/session-drawer.md#docked-vs-drawer). (tddy-web)
- **2026-07-23** [Feature] **Semantic index toggle** — the managed-codebase section (claude-cli + cursor-cli) gains a "Semantic index" checkbox (`create-session-semantic-index-toggle`); `StartSession` sends `semantic_index` (true only when checked, reset when Managed codebase is off). Regenerated `connection_pb.ts` for the new field. Cypress `CreateSessionSemanticIndex` (5). Feature [semantic-index.md](../../../docs/ft/coder/semantic-index.md). (tddy-web)

- **2026-07-22** [Refactor] **Streamed host stats — one `StreamHostStats` subscription replaces the two poll hooks** — `useHostCpuStats`/`useHostDiskStats` (interval polling `GetHostCpuStats`/`GetHostDiskStats`) are replaced by a single `useHostStats(): { perCorePercent, disk }` that subscribes once to `client.streamHostStats(...)` via a `for await` loop over `useDaemonClient(ConnectionService)`, applying each server-pushed `HostStatsEvent` (cleanup mirrors `useSessionActivity`: a `cancelled` flag, AbortError swallowed). `HostStatsFooter` calls the single hook; `CPU_REFRESH_MS`/`DISK_REFRESH_MS` removed; `connectionServiceBackend` now serves `streamHostStats` (async generator, `hostStatsStreamCount`) instead of the two unary stubs. Cypress `HostStatsFooterAcceptance` extended to 6 (single-subscription + live-update). Feature [host-stats-footer.md](../../../docs/ft/web/host-stats-footer.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-06** [Feature] **On-demand model select in `CreateSessionPane`** — a Model `<select>` for **tool** (tddy-coder) sessions as well as **claude-cli**, populated on agent change via a new `rpc/useAgentModels.ts` hook (`ListAgentModels({agent})`, loading/error/stale-response guarding). Switching agent re-fetches and resets the selection to the response `default_model`; the picked `model` is sent in the tool `startSession` (previously hardcoded `""`); the claude-cli dropdown is now fed from the daemon instead of a hardcoded list. A failed probe renders an inline error with the Model select empty and Create disabled — no fallback. `constants/claudeCliModels.ts` `CLAUDE_CLI_MODELS` is dropped from `CreateSessionPane` but **[~] retained** for the out-of-scope legacy `ConnectionScreen` (its `isClaudeCliSession`/CLI-session helpers unchanged). Test support: `cypress/support/rpc/responses.ts` `listAgentModels(...)` builder + `connectionServiceBackend.ts` seeding. Tests: Cypress `CreateSessionModelSelectionAcceptance` 7/7, `CreateSessionPane` 29/29. Feature [tool-session-model-selection.md](../../../docs/ft/web/tool-session-model-selection.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-19** [Feature] **Child conversations rendered as session tabs** — new `components/sessions/useChildSessions.ts` derives `childSessions = sessions.filter(s => s.orchestratorSessionId === parentSessionId)` from the drawer session list (no new RPC — rides the existing `SessionEntry.orchestrator_session_id`). `SessionTerminalTabs.tsx` accepts `childSessions` + `onSelectChild` and renders a tab per child (testid `sessions-child-tab-<sessionId>`) after the bash tabs, before `+`. `SessionRuntime.tsx` extends the active-tab state to a discriminated union (`agent`/`bash`/`child`) and attaches + mounts the selected child session's runtime pane; `SessionMainPane.tsx`/`SessionsDrawerScreen.tsx` pass the enriched session list down. Test support: `sessionTerminalTabsPage` `childTab`/`childPane`, `testIds.sessionsChildTab`, `aSessionEntry` `orchestratorSessionId` override. Tests: Cypress `SessionChildTabsAcceptance` 3/3 (`SessionTerminalTabsAcceptance` 5/5, no regression). Feature [spawn-conversation.md](../../../docs/ft/coder/spawn-conversation.md), [session-terminal-tabs.md](../../../docs/ft/web/session-terminal-tabs.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-22** [Feature] **Code pane syntax highlighting — tokenized preview for recognized code files** — the `WorktreeCodePane` non-markdown preview branch now renders a new `CodeBlock` instead of a plain `<pre>`. `CodeBlock` derives a Prism language from the file path via `codeLanguageForPath` (`src/lib/codeLanguage.ts`; extension→language table, dotfiles/extensionless/`.md`→`null`) and highlights with `react-syntax-highlighter` `PrismLight` (registers only the ~15 shipped languages; theme objects, no external CSS), picking `oneLight`/`oneDark` from the document's `.dark` class. Unrecognized files fall back to the original plain monospace `<pre>` with no highlight container; markdown/error/empty branches and the `worktree-file-preview` wrapper are unchanged. New dep `react-syntax-highlighter` (+ `@types/…`). New testid `worktree-code-highlight` + `worktreeCodePanePage.highlight()`; unit `codeLanguage.test.ts` (8); Cypress `WorktreeCodePaneAcceptance` extended to 9. Feature [session-code-pane.md](../../../docs/ft/web/session-code-pane.md). PR [#314](https://github.com/uppin/tddy-coder/pull/314). (tddy-web)
- **2026-07-22** [Feature] **Agent Activity pane — live view of the agent's own tool calls** — new `useSessionActivity` hook (opens `StreamSessionActivity`, coalesces by `call_id`, exposes `records`/`hasActivity`/`unreadCount`/`markSeen()`) and `AgentActivityOverlay` (icon button hidden until `hasActivity`, unread badge, in-pane overlay list with `[running]`/`[error]` markers, full input/output detail dialog with Escape/backdrop close), wired into the `SessionMainPane` top bar beside the Inspector toggle; session-type-agnostic (incl. sandbox). New `agent-activity-*` testids + `agentActivityPage` page object; Cypress `AgentActivityAcceptance` (8). Feature [agent-activity-pane.md](../../../docs/ft/web/agent-activity-pane.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-22** [Feature] **Worktree Code pane — split-screen file tree + preview for every session type** — a **Code** toggle in `SessionMainPane` (beside Inspector, shown for every session type) opens a horizontal `react-resizable-panels` split (`^3.0.6`, classic `PanelGroup`/`PanelResizeHandle` API): the base view (terminal/chat/PR-Stack) stays in a stable always-mounted `Panel` (so toggling never re-mounts a live terminal or chat), and a `WorktreeCodePane` opens beside it. New `WorktreeFileTree` (recursive, lazy per-directory expand), `worktreeFilesApi` (adapter over `ConnectionService.listWorktreeDirectory`/`readWorktreeFile` keyed by `session.repoPath`), and a shared `renderSimpleMarkdown` (extracted from `SessionFilesPanel`; markdown → sanitized markup, else monospace). Load/read failures surface instead of hanging. New testids `sessions-code-toggle`/`worktree-code-pane`/`worktree-file-tree`/`worktree-file-preview`/`worktree-tree-node-<relPath>`; `worktreeCodePanePage` page object. Cypress `WorktreeCodePaneAcceptance` (7). Feature [session-code-pane.md](../../../docs/ft/web/session-code-pane.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-21** [Feature] **Host Stats Footer — disk + per-core CPU; byte-traffic relocated** — new screen-level bottom footer on `SessionsDrawerScreen`: `HostStatsFooter` combines the relocated `SessionTrafficStrip`/`StatusBar` with `DiskSpaceIndicator` (free space via `formatDiskFree`) and `CpuCoresIndicator` (one `cpu-core-bar-<i>` mini bar per logical core, `data-percent` = `clampCorePercent`). `useHostCpuStats` (5 s) / `useHostDiskStats` (60 s) poll `GetHostCpuStats`/`GetHostDiskStats` over `useDaemonClient(ConnectionService)`. The top header now holds only the daemon selector. New testids `host-stats-footer`/`disk-space-available`/`cpu-cores`/`cpu-core-bar-<i>`; `connectionServiceBackend` stubs both RPCs. Cypress `HostStatsFooterAcceptance` (4) + `SessionTrafficBarFooter` (5, replacing the retired `SessionTrafficBarTopbar` top-toolbar spec); `hostStatsFormat.test.ts` (6). Feature [host-stats-footer.md](../../../docs/ft/web/host-stats-footer.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). PR [#306](https://github.com/uppin/tddy-coder/pull/306). (tddy-web)
- **2026-07-21** [Feature] **Reusable Agent Chat + ACP transport + transcript export** — extracted the PR-Stack chat into a recipe-agnostic `AgentChat` / `useAgentChat` (`agent-chat-*` test ids, `agentChatPage` page object, `AgentChat.stories.tsx`: empty/streaming/select/multiSelect/error/connecting via an exported pure `AgentChatView`). `AgentChat` gains an `acp` prop selecting `useAcpSession` (generated `AcpService.Session` client over the same LiveKit room) vs the default `useAgentChat` (`TddyRemote.Stream`); both return the identical `UseAgentChatResult` and render through the shared `AgentChatView`. **`PrStackChat` uses `acp`**, at full behavior parity via tddy conventions (goal→`agent_thought_chunk`, non-tool activity→one-shot `tool_call`, clarification→`request_permission` with `:multi`/`title`/`raw_input` encoding, answers in the reply `option_id`). New "Export" button → `chatTranscript.buildChatTranscript` + `downloadTextFile`: a timestamped plain-text transcript merging messages and clarification (`ElicitationPoint`) points into one chronological timeline. Web codegen adds `src/gen/tddy/acp/v1/acp_pb.ts`. Cypress: `AgentChat*` (streaming/elicitation/reusable-config) + `AgentChatAcpStreamingAcceptance` + `AgentChatExportAcceptance`; pr-stack chat specs migrated to the shared `acpSession` mock helper — chat suite 44/44. Feature [session-drawer.md § Agent Chat](../../../docs/ft/web/session-drawer.md#agent-chat). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-16** [Feature] **Session terminal tabs** — `SessionTerminalTabs` (fixed non-closable Agent tab + closable bash tabs + `+`) and `useSessionTerminals` (lists open terminals via `ListTerminalSessions`; `open()`/`close(id)` drive `StartTerminalSession`/`StopTerminalSession` with optimistic tab add/remove; active-tab fallback to `main`). `GrpcSessionTerminal` gains a `terminalId` prop threaded into `streamTerminalOutput`/`sendTerminalInput`. `SessionRuntime` mounts one `sessions-terminal-pane-<id>` per terminal (active visible, others `hidden`, all kept streaming); a bash pane's `onDisconnect` drops only that tab while the Agent keeps the session-level disconnect. Agent renders the transport-appropriate view (gRPC main terminal / LiveKit VirtualTui); bash tabs use `GrpcSessionTerminal` over the daemon or session-scoped client. Cypress `SessionTerminalTabsAcceptance` (5, all green) + `sessionTerminalTabsPage` page object. Feature [session-terminal-tabs.md](../../../docs/ft/web/session-terminal-tabs.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). PR [#298](https://github.com/uppin/tddy-coder/pull/298). (tddy-web)
- **2026-07-12** [Feature] **Fast session change — per-session runtimes, session-participant RPC routing, participant metadata overlay, live inspector bytes** — `SessionRuntimeRegistry` (keyed by `sessionId`) replaces the single `useSessionAttachment` for LiveKit sessions; each `SessionRuntimeState` holds its own `Room` (joined `browser-{sessionId}-{ts}`), `GhosttyTerminalLiveKit`, session-participant `ConnectionService` client, traffic meter, byte counters, `lastDataReceivedAt`, control state. One `<SessionRuntime>` per attached session; focus switch = CSS visibility, no unmount/reconnect/resize; background terminals keep streaming; explicit-disconnect eviction (no cap). Session-scoped RPCs (`ExecuteTool`/`ListExecTools`/`ListSessionToolCalls`/`ClaimTerminalControl`/`WatchTerminalControl`/VNC/screen-sharing) route to `daemon-{instanceId}-{sessionId}` via `sessionParticipantRpcClient` (`liveKitFactory(room, sessionServerIdentity)`); `DeleteSession`/`SignalSession` + bootstrap/directory stay daemon-direct on `daemon-{instanceId}`. `useRoomParticipants` parses a `session` metadata field (`sessionParticipantMetadata`); `SessionManager.mergeActiveAndFetchedSessions` overlays it onto synthesized + fetched rows (no `ListSessions` fan-out for active rows). Inspector Details tab shows bytes in/out + "last data received: Ns ago" (`lastDataReceivedFormat`); dual-source — live runtime for attached sessions, daemon `SessionEntry` (`bytes_in`/`bytes_out`/`last_data_received_at`) fallback for sessions with no LiveKit participant. Cypress `SessionRuntimeRegistryBackgroundTerminals`/`SessionParticipantRpcRouting`/`SessionInspectorByteCountAndLastReceived`/`SessionInspectorInactiveDaemonSourced`/`SessionsListParticipantMetadata`/`SessionStartResumeStillDaemonRouted` (6 new, all green) + 96 bun units. Feature [session-drawer.md § Fast Session Change](../../../docs/ft/web/session-drawer.md#fast-session-change). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). PR [#297](https://github.com/uppin/tddy-coder/pull/297). (tddy-web)
- **2026-07-12** [Feature] **Session Inspector "Usage" tab** — live per-conversation token breakdown + summing TOTAL row, streamed over `TddyRemote.Stream` (`tokenUsageUpdated`). `useSessionUsage` (owned by `SessionUsageTab` so the stream opens only while the tab is viewed) keeps the latest `ConversationRecord` snapshot; `SessionUsageTab` renders the table; `formatTokens`/`sessionUsage` helpers; `"usage"` added to `InspectorTab` + `InspectorTabs`, rendered by `SessionInspectorDrawer` with `serverIdentity` threaded from `SessionMainPane`. Feature [session-usage-inspector.md](../../../docs/ft/web/session-usage-inspector.md). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). PR [#295](https://github.com/uppin/tddy-coder/pull/295). (tddy-web)
- **2026-07-12** [Feature] **Cross-host active sessions in the sessions drawer** — the drawer shows any session with a live LiveKit participant regardless of the selected host. Liveness is participant presence in the common room (no `ListSessions` fan-out, no backend signal, no polling — the SDK's connection keep-alive keeps a live session's `daemon-<instance>-<sessionId>` participant present). The list is the union of the selected host's `ListSessions` (active + inactive) and the live cross-host participants, with owner + session id parsed from the participant identity; a live session the selected host didn't return is a minimal synthesized row. New `SessionManager` (`components/sessions/sessionManager.ts`) encapsulates the merged list + refresh + change events as one `useSyncExternalStore`-backed observable store, with a window-bound refresh bridge (`lib/sessionsRefreshBridge.ts`, mirroring `terminalZoomBridge`). Cross-host rows show an owning-host badge and route attach/resume/delete/terminate to the owning daemon (`useDaemonClientFor`); create + no-host-switch preserved. Unit `crossHostSessions` 10/10 + `sessionManager` 5/5; Cypress `SessionsDrawerCrossHostAcceptance` 8/8 (seeds common-room participants via `aFakeCommonRoom`); `SessionsDrawerAcceptance`/`SessionTerminateRefetch`/`SessionDrawer` regression green. Feature [session-drawer.md § Cross-Host Active Sessions](../../../docs/ft/web/session-drawer.md#cross-host-active-sessions). PR [#294](https://github.com/uppin/tddy-coder/pull/294). (tddy-web)
- **2026-07-06** [Feature] **Cursor CLI sandbox + managed workflow UX** — `CreateSessionPane` sandbox toggle and managed-codebase section for `cursor-cli`; Cypress `CreateSessionCursorCliAcceptance` extended. Feature [cursor-cli-session.md](../../../docs/ft/daemon/cursor-cli-session.md). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). PR [#287](https://github.com/uppin/tddy-coder/pull/287). (tddy-web)
- **2026-07-05** [Feature] **Cursor CLI session type in CreateSessionPane** — third session-type toggle (`create-session-type-cursor-cli`); model from `ListAgentModels("cursor-cli")`; Cypress `CreateSessionCursorCliAcceptance.cy.tsx`. PRD [cursor-cli-session.md](../../../docs/ft/daemon/cursor-cli-session.md). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)

- **2026-07-04** [Feature] **Durable web session — single-flight token store + RPC auth gate** — new `sessionTokenStore.ts` (dual-token `localStorage`, client-side `exp`-decode, single-flight `ensureFreshAccessToken()`) and `authGateInterceptor.ts` (ConnectRPC interceptor wired into the HTTP transport that awaits a fresh token and rewrites each request's `sessionToken` field at send-time) replace sole reliance on `AuthProvider`'s 4-minute `setInterval`; a refresh is now triggered transparently on mount whenever the stored access token has expired but the refresh token is still valid, so waking a slept device/backgrounded tab no longer drops the user to the login screen. `AuthProvider` exposes `isRefreshing`; `UserAvatar` shows a top-bar "Refreshing…" indicator while a refresh is in flight. TS units 8/8; new `DurableSessionAcceptance` Cypress spec 5/5; `AuthProviderRefreshAcceptance` rewritten for the two-token contract, 4/4. Feature [session-auth.md § Durable sessions](../../../docs/ft/daemon/session-auth.md#durable-sessions-access--refresh-tokens). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). PR [#283](https://github.com/uppin/tddy-coder/pull/283). (tddy-web, tddy-github, tddy-service, tddy-daemon)
- **2026-07-04** [Feature] **Multi-daemon session host selection + reliable add-to-host + PR-stack create modal** — `CreateSessionPane` gains a Host `<select>` (from `useDaemons()`, shown only when the common room advertises a daemon) whose `daemonInstanceId` is threaded into `StartSession` and `ListProjectBranches`; no-provider/single-daemon usage unchanged. Projects "Add to host" now addresses the chosen target host **directly** (`useDaemonClientFor` → `daemon-{instanceId}` via the transport factory) instead of double-hopping through the selected daemon, and exposes an optional clone-location input (`user_relative_path`); each host shows its advertised base clone location (`DaemonHost.reposBasePath`, parsed from `repos_base_path`). PR-stack "Start session" opens the shared creation form pre-filled from the planned node (new `CreateSessionDialog` wrapping `CreateSessionPane` + `CreateSessionInitialValues`) instead of firing `StartSession` directly. Cypress `CreateSessionHostSelectionAcceptance` 3/3, `PrStackStartSessionModalAcceptance` 3/3 (replaces deleted `PrStackStartSessionAcceptance`), `ProjectsScreenAcceptance` +3, `CreateSessionPane` 28/28, `participantRole` +2. Feature [daemon-selector-livekit-rpc.md](../../../docs/ft/web/daemon-selector-livekit-rpc.md), [projects-screen-multi-host.md](../../../docs/ft/web/projects-screen-multi-host.md). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web, tddy-daemon)
- **2026-07-04** [Feature] **Top-right daemon selector + LiveKit-only daemon-level RPC** — a `DaemonSelector` (shadcn `Select`) on every daemon-mode screen's header lists the common-room daemon-role participants and re-targets all daemon-level RPC (`ConnectionService`, `TaskService`, `VmService`, etc.) at `daemon-{instanceId}` over LiveKit instead of same-origin HTTP, so an operator can route to a peer daemon without a page reload; new `SelectedDaemonProvider`/`useSelectedDaemon`/`useDaemons`/`useDaemonClient` (`src/rpc/selectedDaemon.tsx`) own the shared common-room connection and selection (defaults to the serving daemon via a new `daemon_instance_id` field on `/api/config`, persisted per-tab in `sessionStorage`). Migrated `ProjectsAppPage`, `WorktreesAppPage`, `VmsAppPage`, `TasksDrawerScreen`/`TaskDrawer`, `RpcPlaygroundAppPage`, `ConnectionScreen`, and `SessionsDrawerScreen` (+ `useTerminalControl`, `DemoVmControls`, `SessionWorkflowFilesModal`) off `useHttpClient` onto `useDaemonClient`; every call site null-guards the client, which is `null` until a daemon is selected and the shared room is connected. Per-session terminal/presenter communication is unchanged (stays on its own session room). Found and fixed two latent bugs surfaced by the migration: a zero-delay reconnect storm in `useTaskListStream.ts` (now floored at `MIN_RECONNECT_DELAY_MS`), and a missing test-injection seam for `App`'s internally-constructed `SelectedDaemonProvider` (new optional `testDaemonRoom`/`testDaemonHosts` props). `bun test` 422/422, Cypress component suite 400/400 (63 specs), `cargo test -p tddy-coder -p tddy-daemon` clean (one pre-existing, environment-flaky LiveKit peer-discovery test unrelated to this change). Feature [daemon-selector-livekit-rpc.md](../../../docs/ft/web/daemon-selector-livekit-rpc.md). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web, tddy-daemon, tddy-coder)
- **2026-07-03** [Feature] **PR-stack internal-status badge** — `parseStackPlan` (`stackPlan.ts`) parses the new `internal_status` field into `StackNode.internalStatus`; `PlannedPrRow.tsx` renders a colored action-needed badge (`data-testid="pr-stack-internal-status-badge-<nodeId>"`) next to the phase chip — amber `needs-repoint`, red `has-conflicts`, green `ready-to-merge`, muted otherwise, with the note as hover text — shown only when a status is present. Feature [pr-stacking.md § Internal PR status](../../../docs/ft/coder/pr-stacking.md#internal-pr-status). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-03** [Feature] **Dedicated Projects screen (`/projects`)** — new `ProjectsAppPage`/`ProjectsScreen` (route + `DaemonNavMenu` item) lists projects grouped by logical `projectId` with a row per hosting daemon, hosts an "Add to host" selector sourced from `listEligibleDaemons` (excluding hosts already hosting the project) calling `addProjectToHost`, and takes over the create-project form relocated from `ConnectionScreen`. [projects-screen.md](projects-screen.md); feature [projects-screen-multi-host.md](../../../docs/ft/web/projects-screen-multi-host.md). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web, tddy-service, tddy-daemon)
- **2026-07-03** [Feature] **Managed-codebase workflow controls** — `CreateSessionPane` replaces the implied managed flag with an explicit "Managed codebase" checkbox that, when enabled for a claude-cli session, reveals a workflow-recipe picker and the specialized-subagents multi-select and sends explicit `managed_codebase` + `recipe` (subagents gated on the toggle so a hidden selection can't leak). Feature [managed-codebase-workflow.md](../../../docs/ft/coder/managed-codebase-workflow.md). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-07-03** [Feature] **"New planned PR" form on the PR-Stack Chat Screen** — new `AddPlannedPrForm` (title/description/branch-suggestion + a multi-select ancestor picker over the orchestrator's existing planned-PR nodes) wired into `PrStackScreen` via `client.addPlannedPr(...)`; a `stackPlanOverride` state seam updates the visible list immediately from the RPC response without waiting on a parent session refetch. Feature [pr-stacking.md § Manually adding a planned PR](../../../docs/ft/coder/pr-stacking.md#manually-adding-a-planned-pr). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web, tddy-service)
- **2026-07-01** [Feature] **Per-workflow session views + PR-Stack Chat Screen** — new `resolveWorkflowView` registry (`workflowViews.tsx`) wired into `SessionMainPane` before the terminal branch; `recipe === "pr-stack"` renders `PrStackScreen` (planned-PR list parsed from `stackPlanJson`, "Start session" CTA reusing the existing `StartSession` `recipe`/`stackParent` fields, chat window over `TddyRemote.Stream` via `usePresenterChat`) instead of the terminal, regardless of attachment status. `CreateSessionPane`'s recipe dropdown now offers `pr-stack` in place of the two legacy entries; `stackParents.ts` recognizes it as a valid orchestrator recipe. Feature [session-drawer.md](../../../docs/ft/web/session-drawer.md#per-workflow-session-views). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)

- **2026-07-02** [Feature] **"Managed codebase" specialized-subagent picker in the new-session form** — `CreateSessionPane` gains a collapsible "Managed codebase" section (claude-cli sessions only, `data-testid="create-session-managed-codebase-toggle"`/`-section`) listing subagents from the new `listSubagents` RPC as checkboxes (`create-session-subagent-checkbox-<name>`); `managedCodebase` is derived from `selectedSubagents.length > 0` and both fields are threaded into the claude-cli `startSession` call. Component test `CreateSessionManagedCodebase.cy.tsx` (in-memory backend). Feature [specialized-subagents.md](../../../docs/ft/coder/specialized-subagents.md). (tddy-web, tddy-service, tddy-daemon)
- **2026-07-01** [Feature] **PR stack parent picker for Claude CLI sessions + recipe-based filter** — `CreateSessionPane` moves parent picker outside session-type branches so it renders for both tool and claude-cli sessions; filter updated from `!orchestratorSessionId` to `prStackOrchestrators()` (uses `recipe` field to identify real PR-stack orchestrators, including childless ones); `stackParent` threaded into claude-cli `startSession` call. Feature [session-drawer.md](../../../docs/ft/web/session-drawer.md). (tddy-service, tddy-daemon, tddy-web)
- **2026-06-28** [Feature] **Sandbox toggle in the new-session form** — `CreateSessionPane` gains a `sandbox` checkbox (`data-testid="create-session-sandbox-toggle"`) in the Claude CLI fields; submit sends `StartSessionRequest.sandbox`. Regenerated `connection_pb.ts` (adds the `sandbox` field). Component test `CreateSessionSandboxToggle.cy.tsx` (in-memory backend, asserts the typed request via `callsTo`). (tddy-web)
- **2026-06-26** [Fix] **PTY terminal width fix — GrpcSessionTerminal + GhosttyTerminalGrpc** — new `GrpcSessionTerminal` component: measures container via `getBoundingClientRect()`, sends `initial_cols`/`initial_rows` in `StreamTerminalOutput` request (8px/17px char-cell estimates); `GhosttyTerminalGrpc`: 200ms polling interval + hidden `data-testid="terminal-buffer-text"` div for Cypress visibility; 3 component tests (GrpcSessionTerminalResize.cy.tsx) + 4 e2e tests (terminal-rendering.cy.ts). Feature [web-terminal.md](../../../docs/ft/web/web-terminal.md). (tddy-web)

- **2026-06-26** [Feature] **Screen Sharing sessions — inspector tab, vault passphrase dialog, protocol selector (VNC/RDP)** — `InspectorTabs.tsx`: "Screen Sharing" tab with `data-testid="sessions-inspector-tab-screen-sharing"`; `SessionInspectorDrawer.tsx`: wires all 6 `ScreenSharingService` RPCs; new `SessionScreenSharingTab.tsx` (target list, Add form with VNC/RDP protocol selector, auto-port default, RDP-conditional username field, passphrase dialog flow, `vaultUnlocked` guard prevents re-prompting, inline `<p role="alert">` error display); new `ScreenSharingOverlay.tsx` (LiveKit video track, full-screen fixed); new `ScreenSharingPassphraseDialog.tsx`; new `screenSharingTabState.ts` (reducer: add/remove/open_overlay/close_overlay/set_stream_status); proto regen (`screen_sharing_pb.ts`, `screen_sharing_input_pb.ts`); 9 Cypress component acceptance tests + 6 state unit tests. Feature [screen-sharing-sessions.md](screen-sharing-sessions.md). (tddy-web)
- **2026-06-26** [Feature] **PR-stack session UI — recipe dropdown, parent picker, drawer grouping** — `CreateSessionPane.tsx`: recipe `<select>` with all 9 recipes (default "tdd"), parent-picker `<select>` (tool sessions only, populated via `listSessions`, filters non-orchestrator sessions), `stackParent` passed to `startSession`; `stackParents.ts`: `stackParentCandidates(sessions)` helper; `sessionStackGroups.ts`: `groupSessionsByStack(sessions) → { groups, flat }` (children with missing parent fall into flat); `SessionDrawer.tsx`: groups rendered as collapsible `<details>/<summary>` wrapping orchestrator + children; `SessionDrawerItem.tsx`: `depth?: number` prop with `data-depth` attribute. TS proto regenerated with `orchestratorSessionId`/`stackParent` fields. Tests: 13 bun unit, 7 SessionDrawer CT, 9 CreateSessionPane CT (recipe+picker). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-web)
- **2026-06-26** [Feature+Fix] **Browser DEBUG mask + fix GrpcSessionTerminal.send() errors** — `debugMask.ts` (`resolveDebugMask`/`applyDebugMaskFromConfig`/`applyDebugMaskFromUrl`/`tddyDebug`); `debug.d.ts` ambient types; `index.tsx` applies mask on boot and on `/api/config` response; `GhosttyTerminal`/`GhosttyTerminalGrpc` namespaced loggers (`tddy:term:{write,data,resize,grpc,life,mouse}`); `GrpcSessionTerminal`: `controlToken?` prop, internal ref pattern (no stream recreation), `.catch(()=>{})` on `sendTerminalInput`; `useTerminalControl` exposes `controlTokenRef`; `SessionsDrawerScreen` → `SessionMainPane` thread the ref. 6 bun unit + 2 Cypress CT tests. PR [#233](https://github.com/uppin/tddy-coder/pull/233). (tddy-web)
- **2026-06-26** [Feature] **VNC sessions — inspector VNC tab, VncOverlay, passphrase dialog** — `InspectorTabs`: "vnc" tab button; `SessionInspectorDrawer`: VncService client via `useHttpClient`, renders `SessionVncTab`; `SessionVncTab`: target list with per-row Start/Stop/Remove, Add form (label/host/port/password), passphrase-gated submit; `VncOverlay`: full-screen fixed overlay (`inset-0 z-50`), Escape/backdrop-click/close-button dismiss, `videoTrack.attach/detach` in useEffect; `VncPassphraseDialog`: passphrase input with confirm/cancel; `vncTabState.ts` reducer (add/remove/open_overlay/close_overlay/set_stream_status); `vncInput.ts` (scaleCoordinates, keyboardEventToKeysym, mouseButtonToRfbMask, wheelDeltaToRfbMask); 5 Cypress CT (SessionInspectorVncAcceptance) + 4 (VncOverlay) + 5 (SessionVncTargetRows); 5+15 bun unit tests. Feature [vnc-sessions.md](vnc-sessions.md). (tddy-web)

- **2026-06-26** [Fix] **PTY terminal width fix — GrpcSessionTerminal + GhosttyTerminalGrpc** — new `GrpcSessionTerminal` component: measures container via `getBoundingClientRect()`, sends `initial_cols`/`initial_rows` in `StreamTerminalOutput` request (8px/17px char-cell estimates); `GhosttyTerminalGrpc`: 200ms polling interval + hidden `data-testid="terminal-buffer-text"` div for Cypress visibility; 3 component tests (GrpcSessionTerminalResize.cy.tsx) + 4 e2e tests (terminal-rendering.cy.ts). Feature [web-terminal.md](../../../docs/ft/web/web-terminal.md). (tddy-web)

- **2026-06-26** [Feature] **Single-screen terminal control mutex: Claim terminal CTA** — `screenId.ts` (stable per-tab id via `sessionStorage`); `terminalControlState.ts` pure reducer (`applyTerminalControlEvent`, `TerminalControlState`, `initialTerminalControlState`); `useTerminalControl` hook (`runControlSession` extracted helper, claim-on-attach + reconnecting `for await` watch loop, `claim()`/`reset()`); `SessionMainPane`: `terminalControl` prop, absolute scrim overlay with holder id + "Claim terminal" button; `SessionsDrawerScreen` owns hook; page-object helpers + test ids added. 4 Cypress CT tests + 5+4 bun unit tests. Feature [session-drawer.md](../../../docs/ft/web/session-drawer.md). (tddy-web)
- **2026-06-25** [Feature] **Session inspector Tools tab — invoke panel + durable call log** — `InspectorTabs` (Details/Tools tab strip, `role="tab"`, `aria-selected`, `data-testid`); `SessionInspectorDrawer` restructured (tab state, Details scrolls existing metadata/controls, Tools renders `SessionToolsTab`); `SessionToolsTab`: invoke panel (`<select>` from `listExecTools`, `<textarea>` seeded by `defaultArgsFromSchema`, Invoke button → `executeTool`, result/error blocks) + call log (collapsible rows newest-first; expand shows Input/Output/stdio panels; Shell rows parse `stdout`/`stderr`/`exit_code` from `result_json`; empty-state message); `toolSchema.ts` (`defaultArgsFromSchema` walks JSON Schema `properties`/`required` → skeleton JSON); Cypress `SessionToolsTab.cy.tsx` (12 component tests) + `SessionInspectorAcceptance.cy.tsx` extended (5 tab-switching tests). Feature [session-drawer.md](../../../docs/ft/web/session-drawer.md). (tddy-web)
- **2026-06-25** [Feature] **Tasks UI — real-time two-pane view** — `TasksDrawerScreen`: left drawer (280px, newest-first) + right output pane; `TaskDrawerItem`: status dot, kind, inline Cancel; `TaskOutputPane`: per-channel tabs, `TaskChannelOutput` auto-scroll; `useTaskListStream`: `Map<taskId,TaskInfo>` via `WatchTaskList` stream; `useTaskChannelStream`: UTF-8 via `WatchTask`; route `/tasks` now renders `TasksDrawerScreen` (replaced polling `TasksAppPage`, deleted). 7 Cypress component tests. Feature [tasks-ui-realtime.md](../../../docs/ft/web/tasks-ui-realtime.md). (tddy-web)
- **2026-06-25** [Feature] **Create new session from sessions drawer** — `SessionsDrawerScreen` gains `mode: "list" | "creating"` state; `SessionDrawer` gets `+ New session` button (`onCreateSession?` prop); `SessionMainPane` renders `CreateSessionPane` when `isCreating`; new `CreateSessionPane` (tool/claude-cli toggle, project/agent/recipe/model/permission-mode/initial-prompt/branch-intent fields, `ListProjects`+`ListTools`+`ListAgents`+`ListProjectBranches` RPC calls, `StartSession` submit, auto-attach via `ConnectSession` on success); `interceptStartSession` uses middleware pattern for multi-handler intercept safety. Feature [session-drawer.md](../../../docs/ft/web/session-drawer.md). (tddy-web)
- **2026-06-25** [Feature] **Terminal mobile shortcut drawer — per-tool key preset buttons** — `ShortcutDrawer` floating drag-to-snap panel; `toolShortcuts.ts` (`ToolShortcutDef`, `TOOL_SHORTCUTS`, `keySequenceToBytes`, `resolveShortcutsForSession`); `GhosttyTerminalLiveKit` `mobileShortcuts`/`mobileShortcutsViewportHeight` props; `LiveKitConnectionParams.shortcuts` field; 5 `addSessionAttachment` call sites in `ConnectionScreen` resolve shortcuts at attach time; Cypress `ShortcutDrawer.cy.tsx` + 3 integration tests; flaky Disconnect test root-cause fix (stub alias shadowing). Feature [web-terminal.md](../../../../docs/ft/web/web-terminal.md). (tddy-web)
- **2026-06-24** [Feature] **Long-running background Tasks — /tasks web page** — `TasksAppPage.tsx`: 3-second polling via `ListTasks` RPC, colour-coded status badges (Pending/Running/Completed/Failed/Cancelled), Cancel button calls `CancelTask`; `appRoutes.ts`: `TASKS_ROUTE`/`isTasksPath()`; `DaemonNavMenu.tsx`: "Tasks" nav item; `src/gen/tasks_pb.ts` regenerated. Feature [daemon/background-tasks.md](../../../../docs/ft/daemon/background-tasks.md). (tddy-web)
- **2026-06-21** [Feature] **tddy-vm — /vms web page: VmsScreen, VmsAppPage, DefineVmPanel, ListVmImages** — `appRoutes.ts`: `VMS_ROUTE`/`isVmsPath()`; `VmsScreen.tsx`: VM table (name/state/ssh-port/share-url/start/stop/remove); `VmsAppPage.tsx`: wires all 8 `VmService` RPCs; `DefineVmPanel.tsx`: Buildroot spec textarea + image dropdown + VM-name + Create; `loadImages()` fetches `listVmImages` on mount + after STAGE_DONE (dropdown survives reload); live build log via `for await...of` server stream; `DaemonNavMenu.tsx`: "VMs" nav item; `src/gen/vm_pb.ts` regenerated. Feature [vm/tddy-vm.md](../../../../docs/ft/vm/tddy-vm.md). (tddy-web)
- **2026-06-21** [Feature] **Auth redirect — daemon pages require login with return-to** — `App` gates all daemon-mode routes on `useAuth()`; login passes current hash path as `returnTo`; `AuthCallback` redirects to `/#<returnTo>` after OAuth. (tddy-web)
- **2026-06-21** [Feature] **Session Inspector Drawer** — `SessionInspectorDrawer` overlay (open/expanded/closed states, metadata + controls); `inspectorState.ts` pure reducer; `SessionMainPane` (repurposed from `SessionDetailPane`) with inspector toggle; `useSessionAttachment` gains `deleteSession` + `signalSession`; five new proto fields (tool, sessionType, updatedAt, livekitRoom, previousSessionId) rendered; `pendingDelete` reset on session switch via `key` prop. Feature [session-drawer.md](../../../docs/ft/web/session-drawer.md). (tddy-web)
- **2026-06-21** [Feature] **Session Drawer Screen** — `#/sessions` route; `SessionsDrawerScreen` with `SessionDrawer` (newest-first, derived label, status dot, tooltip) + `SessionDetailPane` (terminal when connected; Resume/metadata when disconnected); `useSessionAttachment` hook; shadcn `tooltip.tsx` + `scroll-area.tsx`; `sessionDrawerLabel`, `connectionStatusForSession`, `sortSessionsByCreation`. Feature [session-drawer.md](../../../docs/ft/web/session-drawer.md). (tddy-web)
- **2026-06-21** [Feature] **Demo goal Phase 2 — DemoVmControls UI** — `DemoVmControls.tsx`: polls `GetDemoVmStatus` every 3 s; shows "Launch Demo VM" (stopped/unknown), "VM booting…" badge, "VM running" + "Open demo" share-URL link + "Stop VM" (running), "VM error" + "Retry" (error); wired into `ConnectionScreen.tsx` for sessions with `workflowGoal === "demo"` alongside the session token guard. Feature [coder/demo-goal.md](../../../../docs/ft/coder/demo-goal.md). (tddy-web)
- **2026-06-15** [Feature] **RPC Playground screen** — `/rpc-playground` route (hash-based routing; no 404 on reload); participant picker (coder-role only); `RpcPlaygroundScreen` + `RpcPlaygroundAppPage`; request editor (builder ↔ raw JSON); streaming panel; `invoke.ts` with `sessionToken` auto-injection; `DaemonNavMenu` entry; `appRoutes.ts` `isRpcPlaygroundPath`. Feature [rpc-playground.md](../../../../docs/ft/daemon/rpc-playground.md); product [daemon/changelog.md](../../../../docs/ft/daemon/changelog.md). (tddy-web)
- **2026-06-06** [Feature] **Claude Code CLI session type — web UI** — **`ConnectionScreen`**: session type selector + model dropdown; **`ConnectedClaudeCliTerminal`** bidi gRPC stream component; **`GhosttyTerminalGrpc`** (`GrpcStream` interface, output buffer before ready, OSC resize, optional chrome bar); **`constants/claudeCliModels.ts`** (`CLAUDE_CLI_MODELS`, `isClaudeCliSession`); `multiSessionState` extended with optional `claudeCli` discriminant. Tests: `claudeCliModels.test.ts`, `GhosttyTerminalGrpc.cy.tsx`. Feature [web-terminal.md](../../../../docs/ft/web/web-terminal.md); product [daemon/changelog.md](../../../../docs/ft/daemon/changelog.md). (tddy-web)
- **2026-04-11** [Feature] **Connection screen — multi-daemon project rows** — **`ConnectionScreen`**: one accordion + session table per **`ListProjects`** row; composite **`data-testid`** **`projectId__daemonInstanceId`** when **`daemon_instance_id`** is set; **`sessionProjectTable`** host-scoped helpers (**`connectionProjectRowKey`**, **`sessionBelongsToProjectHost`**, **`sortedSessionsForProjectHostTable`**, **`isSessionOrphan`**); Cypress **`ConnectionScreen.cy.tsx`**, Bun **`sessionProjectTableMultiHost.test.ts`**. Feature: [web-terminal.md](../../../../docs/ft/web/web-terminal.md). (tddy-web)
- **2026-04-11** [Feature] **ParticipantList Projects column + `owned_project_count`** — `parseOwnedProjectCount`, optional `ownedProjectCount` on `RoomParticipant`, `ParticipantList` **Projects** column (`participant-owned-project-count-*` test ids), Cypress `ParticipantList.cy.tsx`. Feature: [livekit-participant-owned-projects.md](../../../../docs/ft/web/livekit-participant-owned-projects.md). (tddy-web)
- **2026-04-10** [Feature] **ParticipantList Codex OAuth column + metadata parser** — `codexOauthMetadata.ts` (`parseCodexOAuthMetadata`); `ParticipantList` "Codex sign-in" column with `parseCodexOAuthPending`; `useRoomParticipants` `codexOauth` field; Cypress `ParticipantList.cy.tsx` OAuth assertions; Bun `codexOauthMetadata.test.ts`. Feature doc: [codex-oauth-web-relay.md](../../../../docs/ft/web/codex-oauth-web-relay.md). (tddy-web)
- **2026-04-06** [Feature] **Codex OAuth dialog (`CodexOAuthDialog`)** — Modal, iframe vs **`embeddingBlocked`** fallback, Cypress **`CodexOAuth*.cy.tsx`**. Feature doc: [codex-oauth-web-relay.md](../../../../docs/ft/web/codex-oauth-web-relay.md); dev: [codex-oauth-dialog.md](./codex-oauth-dialog.md). (tddy-web)
- **2026-04-05** [Feature] **Connection screen — concurrent terminal attachments** — **`ConnectionScreen`** **`sessionAttachments`** map; **`addSessionAttachment`** / **`removeSessionAttachment`** / **`focusedSessionIdFromPathname`** / **`connectionAttachedTerminalTestId`**; **`detachOthersWhenAddingSecondSession`** policy; stacked **`ConnectedTerminal`** instances under **`connection-attached-terminal-*`** test ids; Bun **`multiSessionState.test.ts`**, **`multiSessionPresentation.test.ts`**; Cypress **`ConnectionScreen.cy.tsx`**. Feature doc: [web-terminal.md](../../../../docs/ft/web/web-terminal.md); presentation reference: [terminal-presentation.md](./terminal-presentation.md). (tddy-web)
- **2026-04-05** [Feature] **Connection screen — workflow recipe default** — **`ConnectionScreen`**: **`recipe`** state defaults to **`free-prompting`**; **Workflow recipe** `<select>` includes **`tdd-small`**, **`review`**, **`merge-pr`**, and other shipped names. Feature doc: [web-terminal.md](../../../../docs/ft/web/web-terminal.md). (tddy-web)
- **2026-04-05** [Feature] **Session table bulk selection and delete** — **`src/utils/sessionSelection.ts`** (+ tests); **`ConnectionScreen`** per-table **`Set`** selection, header **`SessionTableSelectAllCheckbox`**, **Delete selected** toolbar, sequential **`DeleteSession`**, **`ListSessions`** refresh; error path refreshes list and prunes selection. Cypress **`ConnectionScreen.cy.tsx`** bulk suite; feature **[web-terminal.md](../../../../docs/ft/web/web-terminal.md)**. (tddy-web)
- **2026-04-04** [Feature] **Connection screen — pending elicitation** — **`ConnectionScreen`** session tables: **`data-pending-elicitation`**, **Input needed** badge, **`aria-label`**; consumes **`ListSessions`** **`pendingElicitation`**. Cypress **`ConnectionScreen.cy.tsx`**. Feature doc: [web-terminal.md](../../../../docs/ft/web/web-terminal.md). (tddy-web)
- **2026-04-04** [Feature] **URL routing (daemon terminal deep links)** — **`src/routing/appRoutes.ts`**; **`ConnectionScreen`** History API (push/replace, **popstate**, deep link); **`App`** standalone **`/terminal/*` → `/`**; **`web_bundle_acceptance`** SPA fallback for **`GET /terminal/...`**. Cypress **`AppRouting.cy.tsx`**, Bun **`appRoutes.test.ts`**, optional e2e deep-link spec. Feature doc: [web-terminal.md](../../../../docs/ft/web/web-terminal.md). (tddy-web, tddy-coder)
- **2026-04-04** [Feature] **Terminal font zoom** — **`terminalZoom`**, **`terminalZoomBridge`** (validated bridge + sync payloads), **`GhosttyTerminal`** **`applyFontSizePx`** / imperative **`setTerminalFontSize`** / keyboard + pinch + trackpad zoom, **`GhosttyTerminalLiveKit`** **`fontSize`** prop; Cypress **`TerminalZoomAcceptance.cy.tsx`**, Bun **`terminalZoom.test.ts`**, **`terminalZoomBridge.test.ts`**. Feature doc: [web-terminal.md](../../../../docs/ft/web/web-terminal.md). (tddy-web)
- **2026-04-04** [Documentation] **Terminal zoom — wrapped** — Dev reference **[`terminal-zoom.md`](./terminal-zoom.md)**; WIP changeset **`2026-04-04-terminal-zoom-pitch`** removed from **`docs/dev/1-WIP/`**. (tddy-web)
- **2026-04-04** [Documentation] **Terminal zoom — WIP reports removed** — Obsolete validation artifacts under **`docs/dev/1-WIP/terminal-zoom-pitch/`** (pre-toolbar-removal) deleted; Cypress prop-sync note added under **Testing** in **`terminal-zoom.md`**. (tddy-web)
- **2026-04-04** [Feature] **Terminal reconnect overlay** — **`src/components/connection/terminalPresentation.ts`**; **`ConnectionScreen`** presentation state (**`full`** / **`overlay`** / **`mini`**) and **`ConnectedTerminal`** compact layouts; **`appRoutes.ts`** **`terminalDeepLinkSessionPath`**; **`navigatePath`** + **`onNavigate`** for push; first **`ListSessions`** failure **`setError`**. Bun **`terminalPresentation.test.ts`**, **`ConnectionScreen.test.tsx`**; Cypress **`ConnectionScreen.cy.tsx`** (resume vs connect history). Feature doc: [web-terminal.md](../../../../docs/ft/web/web-terminal.md); dev: [terminal-presentation.md](./terminal-presentation.md). (tddy-web)
- **2026-04-05** [Feature] **Terminal connection status bar** — **`TerminalConnectionStatusBar`**, **`ConnectionTerminalChrome`** **`statusBar`** layout, **`connectionChromePlacement`** **`floating`** | **`none`**, **`terminalStatusBarLayout`** + tests, Cypress alignment with layout helpers, **`connectionTerminalChromeDotStyles`**. Feature: [web-terminal.md](../../../../docs/ft/web/web-terminal.md); dev: [terminal-connection-chrome.md](./terminal-connection-chrome.md). (tddy-web)
