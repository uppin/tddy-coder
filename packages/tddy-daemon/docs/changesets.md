# Changesets Applied

Wrapped changeset history for tddy-daemon.

**Merge hygiene:** [Changelog merge hygiene](../../../docs/dev/guides/changelog-merge-hygiene.md) — prepend one single-line bullet; do not rewrite shipped lines.

- **2026-08-02** [Feature] **activities-tail-first-autoscroll — the daemon serves the newest transcript page and the reverse cursor** — one `read_session_transcript` now feeds all three transcript modes through a `(first_seq, replayed_slice)` match: all of it (`SNAPSHOT_THEN_LIVE`), its newest page via `tail_page` (`TAIL_THEN_LIVE`), or none of it (`LIVE_ONLY`) — which still performs the read, because a live frame's `seq` is a position in the whole recording and claiming 0 against a transcript of hundreds is worse than one local file read; `COUNT_THEN_LIVE`, the mode billed as cheap, already pays exactly that read. New `get_acp_replay_page` handler mirrors `get_acp_tool_call_detail` for peer-forwarding, auth and the `strip_tool_body` seam, so a paged frame is not a back door to the tool bodies, with the passthrough wired in `connection_tonic_adapter`. **The live tail numbers by entry, not by delivery:** a tool call broadcasts twice (its `running` then its terminal record) but `latest_state_per_tool_call` coalesces both into one row, so incrementing per delivered record drifted one position ahead per completed call and gave a refinement a position of its own; the relay now carries a `tool_call_id → seq` map pre-seeded from the resolved transcript (positions, where the adjacent count relay's `seen_ids` needed only membership), so a refinement reuses the row's position and only a genuinely new entry advances the counter. One divergence is documented rather than papered over: coalescing keeps a call at its *last* record's slot, so a re-read orders tool rows by completion time while the live tail can only number by first-record time, and two interleaved calls get swapped positions — closing it would mean renumbering frames already sent, which the wire cannot express. Tests: 4 tail/cursor + 3 `seq`-dedup + 1 `LIVE_ONLY` positioning, each verified against the pre-fix numbering; suite at the documented 9-failure sandbox baseline. (tddy-daemon)
- **2026-08-02** [Feature] **VM service gains the tddy-host bake; `StartDemoVm` pinned back to its original launcher settings** — `vm.VmService`'s two new methods are served by the already-registered `VmServiceImpl`, so no `main.rs` change was required. `connection_service.rs`: the `StartDemoVm` `VmConfig` now pins `VmArch::X86_64` + `VmAccel::Tcg` explicitly. Widening `VmConfig` had briefly switched that path to *host* arch and accel, which is a regression rather than a feature — nothing on the demo path knows the image's architecture (`demo-plan.md` names a `tddy-build-qemu` target, which produces x86_64), so on an Apple Silicon host it ran an aarch64 emulator with `-machine virt` and no firmware against an x86_64 image, and on Linux it newly depended on the daemon user's access to `/dev/kvm`. New `tests/tddy_host_vm_rpc.rs` (9 tests) drives both methods through `RpcBridge`: the auth boundary, path-traversal rejection on RPC-supplied library names, the streaming terminal stage, and the create success path. Feature [vm/tddy-vm.md](../../../docs/ft/vm/tddy-vm.md) § Daemon-spawned tddy host VM; cross-package [changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)

- **2026-08-01** [Feature] **PR-stack panel — base-sync and dirty legs on `QueryBranch`, plus `ReorderPlannedPr` and `PullBaseIntoBranch`** — `query_branch` gains a fifth leg (`base_sync`, given the request's new `base_branch`) and reports worktree dirtiness, both **non-erroring** like the existing four: an unnamed base, an unknown checkout, a probe failure or a `spawn_blocking` timeout all degrade that leg alone. New `base_sync_cache` keyed on `(repo_root, base_ref, base_sha, head_ref, head_sha)` — no TTL because the answer is a pure function of the commits, errors cached so an unanswerable repo is not re-probed twelve times a minute, capped with oldest-insert eviction; the ref *names* are in the key because the cached value carries them and two branches at the same commit would otherwise render each other's identity. Every git subprocess in the handler (session scan, worktree, remote, base sync) moved onto `spawn_blocking_with_timeout` — the panel polls once per rendered row every five seconds. Two new mutating handlers carry the standard prologue ending in `require_pr_stack_orchestrator`, with `validate_session_id_segment` before any path is built; `pull_base_into_branch` recomputes the resolution **uncached** afterwards so the row repaints without waiting for a tick, and degrades rather than failing if that recompute times out (the pull already mutated the repo). Tests: `planned_pr_mutation_rpc_acceptance` (9, mutation-tested against deleted guards), `base_sync_cache_unit` (9), `query_branch_resolution_acceptance` extended to 13. (tddy-daemon)
- **2026-08-01** [Feature] **session-attach-ui — restart-cleared staging root, cross-host staged attachments, streaming peer-forward, and two latent forwarding bugs fixed** — the host half of the attach UI, plus two defects found while building it. **Bug 1, silent hang:** `forward_to_peer` addressed a peer by its **bare** instance id, but a daemon serves `connection.ConnectionService` on **`daemon-{instance_id}`** (`main.rs`); the bare identity belongs to the *discovery* participant, which runs no `ServerEngine` — and with no deadline on the forward the symptom was a call that never returned **and never errored**. Every fixture peer in the suite had served under the bare id, which is exactly what hid it; four fixtures (`forward_to_peer_shared_registry`, `staging_forwarding_acceptance`, `relay_e2e_acceptance`, `multi_host_acceptance`) now stand a peer up the way `main.rs` does — a discovery participant on the bare id plus a separate RPC participant on `daemon-{id}` — and `forward_to_peer` carries `PEER_FORWARD_TIMEOUT`. **Bug 2, silent 4 MiB ceiling:** a cross-host staged attachment routed through the *unary* forwarded `ReadHostDocument` while the same file staged locally had no such limit, so a large document attached on one host and failed across two; it now goes through `forward_stream_read_host_document_via_livekit`, re-checking `max_attachment_bytes` **while** accumulating. New `forward_server_stream_to_peer` (built on the pre-existing `RpcClient::call_server_stream`, so no transport change) relays decoded frames into `MpscResultStream<T>` with a per-frame idle deadline, so a peer-side error terminates the local stream as an error and a stream that stops can never surface as a clean end; its channel is **deliberately unbounded** because the transport's bounded channel is filled by the room's *shared* response loop, and a relay that stopped draining would head-of-line-block every other in-flight forwarded RPC. Staging moved from `{tddy_data_dir}/staging/` to `std::env::temp_dir()/tddy-staging` (`default_staging_base_dir`, injected via `ConnectionServiceImpl::with_staging_base_dir`), retiring the abandoned-batch leak; `host_documents` split into `resolve_host_document` (all guards, no bytes) / `read_host_document_bytes` so the unary read, the streaming read and materialization share one `.staged-complete` gate — which is what replaced the old same-host staged rule, since the marker checked on the *owning* host is what actually prevents a truncated attachment; `start_session`'s body extracted verbatim into `start_session_core(req, progress)` with an `AttachmentProgressSink` (discarding on the unary path, so its behaviour is unchanged) and `AttachmentMaterialization` grouping six carried args to remove two `#[allow(clippy::too_many_arguments)]`; `stream_start_session` resolves the caller **before** classifying the route, so an unauthenticated request cannot drive an outbound forward; a cross-host fetch reports progress **per frame**, not per completed attachment, because the relay's deadline is per frame and whole-file reporting made an ordinary PDF fail with `DEADLINE_EXCEEDED` mid-creation; `max_attachment_bytes` config key (64 MiB default) enforced host-side and published on `DaemonAdvertisement` (omitted when zero, so unadvertised stays distinguishable from a cap of none); `WorktreeDirEntry.size_bytes` populated from a real `stat`; a compile-time `const _: () = assert!(...)` pins `HOST_DOCUMENT_FRAME_BYTES` + 8 KiB headroom <= `MAX_CHUNK_FRAME_BYTES`, verified to fail the build at 64 KiB, because a lost chunk frame wedges a call permanently with no error. Tests: `session_attach_cross_host_acceptance` 8 (two daemons over the LiveKit testkit, including a 4.5 MiB transfer that crosses hosts *inside* a forwarded stream — the combination no earlier test covered), `session_attach_staging_scope_acceptance` 11, `staged_attachment_path_validation` 8, `daemon_advertisement_attachment_cap` 4, `staging_rpc_acceptance` 7, `worktree_files_rpc` 8 (incl. a test pinning that `ListWorktreeDirectory` lists a project's **primary** worktree, previously assumed), `--lib` 424; hermeticity re-verified under `TMPDIR=/tmp` across two consecutive runs; `clippy --all-targets -- -D warnings` and `cargo fmt --check` clean. Docs [connection-service.md § Start-session attachments](connection-service.md#start-session-attachments). Feature [session-attachments.md](../../../docs/ft/coder/session-attachments.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)

- **2026-07-30** [Feature] **session-branch-conflict — `StartSession` refuses a new branch another session already owns instead of silently suffixing it `-1`** — new `branch_owner::find_session_owning_branch(sessions_base, branch)` holds the single ownership rule (prefer active, tie-break most recent `updated_at`, skip unreadable changesets), lifted out of `query_branch`'s inline closure so the RPC guard, `QueryBranch` (which keeps its `require_pr_stack_orchestrator` gate) and the Telegram spawn flow cannot drift. New `owned_branch_conflict` is called from `start_session` after peer routing and before the session-type dispatch — one check for `tool`/`claude-cli`/`cursor-cli`/`workspace`, and nothing exists yet when it fires (no session dir, changeset, branch, worktree or remote push). Opt-in via `StartSessionRequest.on_branch_conflict` (field 30: `""` suffixes as before, `"reject"` refuses), answered as `StartSessionResponse.branch_conflict` (field 5, `BranchConflict { branch, BranchSession owner, suggested_branch_name }`) with an empty `session_id` — a response field, not an error, because `tddy_rpc::Status` has no details channel and `StartSession` crosses hosts over LiveKit. Fires only for an explicit `new_branch_from_base` with a non-empty `new_branch_name`; `work_on_selected_branch` is never refused, and a branch no session owns still suffixes. Telegram carries the same check in `handle_telegram_branch_callback` (claude-cli derivation only) with a three-button `tbc:` inline keyboard (`sw` enters the owning session, `na` rewrites to `WorkOnSelectedBranch`, `sg` takes the suggested name), wired into `telegram_bot.rs`. Tests: `session_branch_conflict_acceptance` 9, `branch_owner_unit` 5, `telegram_branch_conflict_acceptance` 7; `clippy --all-targets -- -D warnings` clean. Docs [connection-service.md § Branch-conflict guard on StartSession](connection-service.md#branch-conflict-guard-on-startsession). Feature [session-branch-conflict.md](../../../docs/ft/daemon/session-branch-conflict.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-30** [Fix] **session-attachment-store hardening applied to both write paths (merge of `master` into the start-materialization branch)** — the branch predated `master`'s store hardening and carried a `write_attachment_bytes` that re-introduced both defects it removed: an `if target.exists()` / `std::fs::write` pair (a TOCTOU window, and `fs::write` **truncates** an existing attachment instead of returning `FAILED_PRECONDITION`) and a discarded canonical dir (it wrote through the re-derived non-canonical path, so the containment check proved nothing about the path actually written). Both write entry points — `copy_attachment_into_session` and `write_attachment_bytes` — are rewritten on a shared `create_attachment_file_exclusively` helper (`OpenOptions::create_new(true)`: one atomic existence-check-plus-create; an existing regular file *or* a symlink planted in `artifacts/attachments/` is refused, never followed or truncated; a failed write removes the partial file), so neither path is the weaker gate. New `validate_attachment_basename` wraps the uploads path's `validate_segment` rule with a message naming the field the caller sent (`SessionAttachment.basename`) instead of `upload_id` / `file_name`; used by both store entry points and both `materialize_session_attachments` basename checks, while the staging RPCs keep the original message where those fields are real. Doc references repointed from the never-created `docs/ft/coder/amendments/session-attachments-start-materialization.md` to [session-attachments.md § Start-session materialization](../../../docs/ft/coder/session-attachments.md#start-session-materialization) (5 sites), the RPC-table row for the staging RPCs corrected (it still said `UNIMPLEMENTED` with a dangling anchor) and a `ReadHostDocument` row added, and `connection_pb.ts` regenerated from the merged proto. Verified on the merged tree: build + `clippy --all-targets -D warnings` + `cargo fmt` clean, daemon/workflow suites green apart from one pre-existing `master` failure tracked in [docs/dev/TODO.md](../../../docs/dev/TODO.md). (tddy-daemon)
- **2026-07-28** [Feature] **session-attachment-start-materialization — `StartSession` materializes both attachment sources into `{session_dir}/artifacts/attachments/` before the agent spawns; the staging RPCs and `ReadHostDocument` are live and forward across hosts** — new `session_attachment_staging` (per-OS-user root `{tddy_data_dir}/staging/{os_user}/{staging_id}/{file_name}`, ordered chunked writer mirroring `session_file_upload`, list newest-first, delete — `validate_segment` + `contained_canonical_dir` guards) and `host_documents::read_host_document_bytes` (resolves `HostDocumentScope` roots under the owning daemon's own `os_user`; POSIX `relative_path` no `.`/`..`/absolute, canonicalize-and-contained; binary read capped at `MAX_HOST_DOCUMENT_BYTES` = 4 MiB, refused over-cap not truncated). The three staging handlers + `read_host_document` replace the `TODO(start-session-attachments)` stubs and route by `daemon_instance_id` (empty/local = local, else forwarded over the LiveKit common room); four `forward_*_via_livekit` wrappers in `livekit_peer_discovery`. `materialize_session_attachments` runs in every `StartSession` session-type path after `session_dir` exists and before spawn: a `StagedAttachmentRef` naming a foreign `daemon_instance_id` is `INVALID_ARGUMENT` (no cross-host fetch, no silent empty attachment), `HostDocumentRef` is the only source that forwards (via `ReadHostDocument`), duplicate `basename`s within one request are rejected before any write, and a partial-materialization failure cleans up the request's `artifacts/attachments/` writes. The tool (tddy-coder) branch pre-creates `session_dir` so attachments land before the child spawns (the child's `create_session_dir_with_id` is idempotent on the existing dir). Tests: `tests/staging_rpc_acceptance.rs` 7 + `tests/staging_forwarding_acceptance.rs` 2 (two-daemon LiveKit testkit) + unit tests in `session_attachment_staging` (7) / `host_documents` (9) = 25; review hardening — `ReadHostDocument` canonicalizes the full file path (symlink-escape refusal) and checks file size before reading, forwarded bytes are re-capped on the session host, and staged materialization requires the `.staged-complete` marker; `cargo fmt --check` + `clippy --tests -D warnings` clean. Feature [session-attachments.md § Start-session materialization](../../../docs/ft/coder/session-attachments.md#start-session-materialization); [connection-service.md § Start-session attachments](./docs/connection-service.md#start-session-attachments) corrected and marked implemented. Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-27** [Feature] session-attachment-store: new `session_attachments` module (`list_session_attachments`, `copy_attachment_into_session` — segment + containment guards, exclusive create-new write with partial-file cleanup); `session_context_docs` lists manifest docs then attachments (`ContextDocKind`, `size_bytes`, recipe-independent attachments); enrichment maps `kind`/`size_bytes` onto `SessionEntry.context_docs`. Docs [connection-service.md § ListSessions workflow fields](connection-service.md#listsessions-workflow-fields). Feature [session-attachments.md](../../../docs/ft/coder/session-attachments.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-27** [Bug Fix] **remove-origin-hardcode — `project_storage` and `connection_service` resolve the effective remote instead of assuming `origin`** — `ProjectData` gains optional `remote_name: Option<String>` (serde, skip-if-none). New `project_storage::effective_remote_name_for_project(projects_dir, project_id, repo_root)` resolves the remote as main-worktree upstream (`tddy_core::worktree::detect_default_remote_name`) → stored `remote_name` → `origin` (last resort); `effective_integration_base_ref_for_project` calls `resolve_default_integration_base_ref_with_remote(repo_root, Some(&effective_remote_name_for_project(...)))` for legacy rows (stored `main_branch_ref` returned verbatim, validators now permissive). `connection_service.rs` threads the resolved remote through `push_new_branch_to_remote` (was `push_new_branch_to_origin`), `list_recent_remote_branches(repo_root, remote, limit)`, `validate_repoint_target` (compares via `local_branch_name_for_remote` on both sides), `effective_spawn_branch(..., remote)` (new `remote` parameter), and the telegram branch-list call. Tests: acceptance (`effective_spawn_branch`, `repoint_target_validation`, `project_default_branch_resolution`, `query_branch_resolution`, `set_project_default_branch`, `worktrees_rpc`, `unified_worktree_base`); `clippy -- -D warnings` + `cargo fmt --check` clean. Feature [git-integration-base-ref.md](../../../docs/ft/coder/git-integration-base-ref.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-27** [Feature] **planned-pr-base-branch-selection — the four web-facing spawn paths honor an explicit operator-chosen `selected_integration_base_ref` over the stack-parent-resolved chain base** — each of `spawn_claude_cli_session_inner`, `start_sandboxed_claude_cli_session`, `spawn_cursor_cli_session_inner`, `start_sandboxed_cursor_cli_session` computes `chain_base_ref` as before (still seeds `Changeset.workflow.selected_integration_base_ref` and node linking) then hands `tddy_core::session_chain::select_worktree_base_ref(selected_integration_base_ref, chain_base_ref)` to `setup_worktree_for_session_with_optional_chain_base`: a non-empty trimmed request override is used as the worktree base directly, an empty/whitespace one falls through to the stack-parent resolution (today's behavior). `resolve_chain_base_for_session_spawn` is unchanged — its `stack_parent → persisted → default` precedence still governs the Telegram / workflow-recipe callers that have no request field, completing the `unified-worktree-base-resolution` precedence at the spawn seam only. Tests: `tddy-core/tests/planned_pr_base_branch_selection.rs` 6 (precedence contract); `cargo clippy --workspace --all-targets -- -D warnings` + `cargo fmt --all --check` clean. Feature folded into [git-integration-base-ref.md § Operator-chosen base at the spawn seam](../../../docs/ft/coder/git-integration-base-ref.md#operator-chosen-base-at-the-spawn-seam-added-2026-07-27) and [pr-stacking.md § Operator-selectable base branch for a diamond node](../../../docs/ft/coder/pr-stacking.md#operator-selectable-base-branch-for-a-diamond-node-added-2026-07-27). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)

- **2026-07-27** [Architecture] **unified-worktree-base-resolution — chain base resolution moves to tddy-core; cursor-cli PR-stack children base off the planned node, not origin/master** — the cursor-cli spawn paths no longer fall back to `origin/master` for a PR-stack child. `cursor_cli_spawn.rs::spawn_cursor_cli_session_inner` and `connection_service.rs::start_sandboxed_cursor_cli_session` now accept `stack_parent`, set `Changeset.orchestrator_session_id`, and resolve the chain base via `tddy_core::resolve_chain_base_ref` instead of hardcoding `None`; the dispatch site forwards `req.stack_parent`. The moved `resolve_chain_base_ref` / `parent_is_pr_stack_orchestrator` / `pr_stack_node_for_spawn` methods and their inline `chain_base_resolution_tests` are removed (now in tddy-core); a thin `resolve_chain_base_ref_status` wrapper maps `String` → `Status::failed_precondition`. Telegram spawn paths route through `tddy_core::resolve_chain_base_for_session_spawn`, reading the persisted `worktree_integration_base_ref` the branch callback wrote (the prior `None` pass ignored it and always used the default base). Tests: `tests/unified_worktree_base_acceptance.rs` 3 (cursor-cli + claude-cli PR-stack child + no-stack default). Feature [git-integration-base-ref.md § Session chaining](../../../docs/ft/coder/git-integration-base-ref.md#session-chaining-parent-session--originbranch). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-27** [Fix] **cursor-cli-peer-spawn-repo-path — non-sandboxed cursor-cli "Add agent" peer spawn reuses the orchestrator's worktree and records the orchestrator link** — `spawn_cursor_cli_session_inner` (in `cursor_cli_spawn.rs`) now accepts `repo_path`/`stack_parent` and resolves the worktree via `session_worktree_source`, mirroring the contract the sandboxed claude-cli path already honors: a non-empty `repo_path` is canonicalized + `is_dir`-validated and used directly as the session worktree (no git worktree, no `cursor-cli/*` branch, not removed on session end), and `stack_parent` is recorded as `Changeset.orchestrator_session_id` (empty wire string → `None`). The call site in `connection_service.rs` forwards both fields; the sandboxed cursor-cli path is left unchanged with a `TODO` (tracked separately). Before this, a peer cursor-cli child became a standalone session on a fresh branch off `origin/master` with no orchestrator link — the daemon-side counterpart to the web "Add agent" feature (PR #357, which fixed a latent `cursor-cli` `stackParent` hardcode on the web side). `.cursor/hooks.json` is now gitignored (mirrors `**/.cursor/mcp.json`); it embeds a per-session `hook_token` secret written by `install_cursor_hooks_in_worktree`. Tests: 5 new fluent-style acceptance tests in `tests/cursor_cli_session_acceptance.rs` (worktree reuse, orchestrator link with/without `repo_path`, no new branch, invalid `repo_path` errors) + 2 helpers; `clippy -p tddy-daemon --tests -- -D warnings` clean; `./test -- cursor_cli` 9/10 (the 1 failure is pre-existing, environment-blocked: `cursor_cli_sandbox_start_succeeds_when_sandbox_backend_available` hits a host cgroups/userns EPERM, unmodified by this diff). Deferred: `os_user` impersonation for non-sandbox cursor-cli (`start_cursor` hardcodes `os_user: None`), and the sandboxed cursor-cli `repo_path`/`stack_parent` path. Feature [cursor-cli-session.md](../../../docs/ft/daemon/cursor-cli-session.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)

- **2026-07-27** [Fix] **cursor-cli-skip-keychain — sandboxed cursor-cli sessions no longer hang on macOS Keychain** — `start_sandboxed_cursor_cli_session` now calls the new pure helper `sandbox_session::build_sandboxed_cursor_runner_env` (base runner env + `cursor_runner_env_overlay`) instead of inline env assembly; the overlay was previously defined in `tddy-sandbox-recipes` but never called from the cursor spawn path (only the Claude overlay was wired, unconditionally, via `build_sandbox_runner_env`), so even `CURSOR_TMPDIR` was never set in cursor sandbox sessions before this fix. The helper is pure (no `&self` deps), so the Keychain-skip behavior is unit-testable without a Seatbelt/cgroups harness. Specialized-subagent / LSP / semantic-index extensions remain inline in the spawn path (they depend on `&self` / runtime state). Tests: 1 fluent-style unit test in `sandbox_session::tests` (`cursor_runner_env_skips_keychain_so_the_jailed_agent_never_probes_securityd` — asserts `CURSOR_SKIP_KEYCHAIN=1`, `CI=true`, `CURSOR_TMPDIR`). Feature [cursor-cli-session.md § Known follow-ups](../../../docs/ft/daemon/cursor-cli-session.md#known-follow-ups). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)

- **2026-07-26** [Fix] pr-stack-repoint-dead-end: new pure **`connection_service::validate_repoint_target(target, default_branch, parent_branches) -> Result<Option<String>, String>`**, in the same shape as `effective_spawn_branch` — empty/whitespace → `Ok(None)`, the default branch matched with `origin/` stripped from **both** sides (the resolver returns a remote-tracking ref while a node's `branch` and a GitHub PR base are plain names), a parent's branch accepted verbatim, anything else `invalid_argument`. Validation is not politeness: "no parent owns this branch" *is* the detach instruction, so an unvalidated target silently rewrites the plan. `repoint_planned_pr` collects the node's parents' branches off `changeset.stack`, **substitutes its own resolved `default_branch` for an empty wire target** (without which a project storing no `main_branch_ref` sent `""`, selected the recipe's drop-merged-parents rule, and returned success against an unchanged plan — no error, no change), then forwards `as_deref()`. Tests: `tests/repoint_target_validation_acceptance.rs` 8. Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-26** [Fix] pr-stack-ux-recovery: the daemon finally holds a GitHub credential and reports honestly when it does not. New `github_token_store.rs::FileGitHubTokenStore` is rooted at the previously-unread `auth_storage` config path (`github-tokens.json`, `O_CREAT` mode `0600` plus an explicit `set_permissions`, dir `0700`; `put` serialised on a process-wide mutex across its read-modify-write — two concurrent logins each read the same map and the second write dropped the first operator's token — and published via `github-tokens.json.tmp` + `fsync` + `rename`, because a truncated in-place write parses as an empty map and lost *every* operator's token with only a log line; `read_all` distinguishes `NotFound` from a real read failure, which is now logged, since an `EACCES` was the only clue an operator diagnosing "PR status unavailable" could have had). `auth.rs::build_auth_entries` returns `anyhow::Result` and **refuses to start** when a configured `auth_storage` fails `probe_writable` (create dir, write probe, remove) — retention is a hard login dependency, so an unwritable path fails every login one operator at a time for a fault entirely visible at boot; no fallback to a store-less service, and the probe runs for a stub provider too. New `github_pr_credentials::pr_lookup_for_caller(stub_mode, stored) -> PrLookup { Empty | Perform(token) | Unavailable(reason) }` plus `ConnectionServiceImpl::pr_status_for_caller`, which resolves the caller's token from their session-token login and returns a `PrStatusView` **value, never a `Status`** — an unresolvable `owner/repo` stays `exists = false` (a repo with no GitHub remote genuinely has no PRs), a stub/demo login short-circuits to an empty result (never *unavailable*), and both `.map_err(|e| Status::internal(..))?` sites are gone so `query_branch` keeps returning its session/worktree/remote legs when GitHub is unreachable. `query_branch` populates the new `remote` leg via `tddy_core::worktree::remote_branch_ref_sha`. New `effective_spawn_branch(intent, new_branch_name, selected_branch_to_work_on)` (`local_branch_name`-normalised selection under `work_on_selected_branch`, trimmed `new_branch_name` otherwise) feeds the **node-link** sites in both `stack_parent`-taking spawn paths, so a *resumed* spawn re-links its planned node instead of spawning another unlinked session on every click; `resolve_chain_base_ref` deliberately still keys on `new_branch_name`, since a resolved chain base would make worktree setup run a real `fetch_chain_pr_integration_base` for a resume that creates nothing. Tests: `tests/pr_lookup_credentials_acceptance.rs` 5, `tests/effective_spawn_branch_acceptance.rs` 6 (incl. a new-branch spawn ignoring the `selectedBranchToWorkOn` the dialog sends unconditionally — without it, "prefer whichever field is non-empty" passes every other case while being wrong). Feature [pr-stack-live-status.md](../../../docs/ft/coder/pr-stack-live-status.md#authenticated-pr-status-added-2026-07-26), [session-auth.md § GitHub access-token retention](../../../docs/ft/daemon/session-auth.md#github-access-token-retention-added-2026-07-26). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-26** [Feature] start-session-attachments: `connection_service.rs` and `connection_tonic_adapter.rs` gain the three new `ConnectionService` methods (`upload_staged_attachment_chunk`, `list_staged_attachments`, `delete_staged_attachment`) — the rpc impl returns **`Status::unimplemented`** under a `TODO(start-session-attachments)`, the tonic adapter delegates; the stubs are not optional, since adding an RPC makes both impls incomplete and nothing compiles without them. `start_session` **ignores** `StartSessionRequest.attachments` entirely, so a request carrying attachments starts a session with none and no error. No tests beyond compilation; the host side (staging root + chunked writer, materialization into `{session_dir}/attachments/`, `HostDocumentRef` scope resolution + cross-host fetch, staging GC) is unbuilt and recorded as known limitations. Docs [connection-service.md § Start-session attachments](connection-service.md#start-session-attachments-wire-contract-only). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-26** [Feature] versionless-claude-aliases: `telegram_session_control.rs` drops its two private `&[(&str, &str)]` model tables — `CLAUDE_CLI_MODELS` / `CURSOR_CLI_MODELS` are now `LazyLock<Vec<BackendModel>>` over `tddy_core::backend::claude_cli_models()` / `cursor_cli_models()`, so both keyboards render the shared catalog's `label` and index into it (fixing the Claude keyboard's stale labels and taking Cursor from 3 buttons to 5). `handle_telegram_claude_model_callback` / `handle_telegram_cursor_model_callback` resolve `model_idx` through `.get(..).map(|m| m.id.as_str())`, erroring on an out-of-range index instead of resolving to an arbitrary model. Tests: 3 added (alias ordering, plus Claude and Cursor keyboards asserted equal to the core catalogs) — `telegram_start_claude_acceptance` 7/7. Feature [telegram-session-control.md](../../../docs/ft/daemon/telegram-session-control.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-26** [Fix] pr-stack-branch-gated-spawn: `link_stack_parent_node_to_child` → **`link_stack_node_to_spawned_branch`** — its subject is the branch a spawn created, with the session recorded only as a fallback, and a new session claiming a branch a node already owns now **repoints** it (last writer wins) instead of failing `FAILED_PRECONDITION`, since restart and re-attach are normal. `pr_stack_node_for_spawn` matches by exact `branch`, else by `branch_suggestion` on a not-yet-materialized node, and reads the stack through `read_stack_with_resolved_branches` so the child-session fallback reaches the spawn gate itself (the hydrated copy is read-only; the forward link writes through the orchestrator's own session dir). Both spawn paths now **push the new branch before linking**, so a descendant basing onto `origin/<branch>` finds the ref. `StackChildSpawnHandler::spawn_child`'s duplicate guard keys on the node already owning a branch, and the branch to create comes from `branch_suggestion`. Docs [connection-service.md § Branch-gated spawn and the forward link](connection-service.md#branch-gated-spawn-and-the-forward-link). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-25** [Feature] branch-query-and-remote-branch: new `query_branch` handler (reuses the `get_pr_status` prologue + `get_pr_by_head` + branch→session scan + `tddy_core::worktree::worktree_path_for_branch`) resolves `{branch, session, worktree, pr}` for one head branch; `create_remote_branch` is threaded through the three session-start intent helpers so a set flag runs `push_new_branch_to_origin` after `create_worktree_with_retry` and sets `Changeset.remote_pushed = true` (a push failure fails `StartSession`, no fallback). Feature [pr-stack-live-status.md](../../../docs/ft/coder/pr-stack-live-status.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-25** [Feature] acp-replay-lazy-tool-bodies: `acp_replay_frame` strips tool bodies via `tddy_service::acp_replay::strip_tool_body` (covers the `StreamAcpReplay` snapshot loop + live `relay_acp_replay` tail; `SNAPSHOT_THEN_LIVE`/`LIVE_ONLY` body-less, `COUNT_THEN_LIVE` unchanged), and new unary `get_acp_tool_call_detail` returns one call's `raw_input`/`raw_output` from `tool_call_detail` (auth + `ExecuteTool`-style peer-forward, `NOT_FOUND` for unknown id). Tests: `stream_acp_replay_*_omit_tool_bodies` 2 + `get_acp_tool_call_detail_*` 2. Docs [connection-service.md](connection-service.md#agent-activity-log--stream). Cross-package [changeset](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-25** [Feature] worktree-disk-usage-streaming: new `WorktreeSizeCalculator` (daemon-global `tokio::sync::Semaphore` = 2, per-project `broadcast`, in-flight de-dup, separate `worktree_sizes.json` persistence; permit acquired inside each spawned walk) drives a lazy per-worktree `None/Calculating/Cached` size lifecycle; `stream_worktree_stats` emits a snapshot then per-worktree `Calculating→Cached` increments (mirrors `stream_host_stats`, `MpscWorktreeStatsStream`), `calculate_worktree_size` is membership-gated (like `remove_worktree`), and `list_worktrees_for_project` overlays the size status/timestamp while retaining its eager walk as a cache fallback. Tests: `stream_worktree_stats_rpc` 4 + `worktree_size_calculator_acceptance` 7. Docs [worktrees.md](worktrees.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-25** [Fix] terminal-capture-hardening: sandbox sessions join the mode-replay contract — `SandboxSessionState.capture` becomes `Arc<Mutex<TerminalCapture>>` (was a raw, **unbounded** `Vec<u8>` that grew for the session's life), the stdio bridge in `sandbox_session.rs` calls `append`, and the sandbox branch of `stream_terminal_output` replays via the new `sandbox_replay_frames` helper (`chunk_terminal_output(&capture.replay(), …)`) so a browser attaching to a long-running sandbox session gets the mouse-tracking prologue instead of silently dropping clicks. Docs [connection-service.md](connection-service.md#terminal-mode-replay-mouse-tracking). Tests: `sandbox_replay_tests` 1. Cross-package [changeset](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-25** [Fix] terminal-mode-replay: `stream_terminal_output` sends `capture.mode_prologue()` as its **own first frame**, before and independently of the replay branch — the replay is gated on `!has_initial_dims` and a browser always supplies dimensions, so the web terminal's path previously received no capture (and no mouse DECSETs) at all; frame order on attach is prologue → legacy replay → initial ACK → live bridge, and the legacy replay chunks `capture.buffered_bytes()`. `PtyHandle.capture` becomes `Arc<Mutex<TerminalCapture>>` and the LiveKit bidi attach replays `cap.replay()` (prologue ++ retained output). See [connection-service.md § Terminal mode replay](connection-service.md#terminal-mode-replay-mouse-tracking). Acceptance: `tests/terminal_mode_replay_acceptance.rs` (stub TUI enables tracking, ring is flooded until the DECSETs are provably gone from `buffered_bytes()`, then a dimensions-supplying browser-shaped client must get the prologue first). Known gap: `sandbox_session.rs` keeps its own unbounded raw capture and gets no prologue — follow-up. Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-25** [Feature] session-files-inspector: new `session_uploads` module — `list_uploads` walks `{session_dir}/uploads/{upload_id}/*` into a flat, newest-first `UploadEntry` list (absolute host path, size, mtime-ms; missing uploads root → empty, not an error), and `delete_upload` removes one file addressed by `upload_id`+`file_name`, reusing the writer's basename + canonicalize-and-contain guard (unsafe segment → `InvalidArgument`, removing nothing; missing file → `NotFound`) and pruning the emptied `upload_id` folder. Extracted the shared `session_file_upload::{validate_segment, contained_canonical_dir}` guard from `write_upload_chunk` (behavior unchanged). New `list_session_uploads`/`delete_session_upload` handlers (auth → `sessions_base` → `validate_session_id_segment`, via a shared `uploads_sessions_base` helper), delegated through the tonic adapter. Tests: `session_uploads_rpc` 13 (+ `session_file_upload_rpc` 9 regression). Feature [session-files-inspector.md](../../../docs/ft/web/session-files-inspector.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)

- **2026-07-25** [Feature] enqueued-input-overlay: terminal input-offset ACK. `PtyHandle::send_input(data, input_offset)` records the applied offset on the shared `tddy_task::TaskChannel` (not the per-RPC-rebuilt handle) and publishes it; `send_terminal_input` threads `req.input_offset`; `stream_terminal_output` subscribes to the channel's ack `watch` and interleaves `SessionTerminalOutput{data:[], acked_input_offset}` frames (initial offset emitted up front). Acceptance: `tests/terminal_input_ack_acceptance.rs`. See [connection-service.md § Input-offset acknowledgement](connection-service.md#input-offset-acknowledgement) and feature [enqueued-input-overlay.md](../../../docs/ft/web/enqueued-input-overlay.md). (tddy-daemon)
- **2026-07-25** [Feature] pr-stack-live-status: `resolve_chain_base_ref` now takes the new branch name and, for a pr-stack orchestrator parent, bases the child off its node's effective base via `Stack::base_ref_for_spawn` (propagating the ordering guard as `failed_precondition`) instead of the default branch — fixing both spawn paths (web Start-session + agent spawn-child) that previously ignored the DAG. New `get_pr_status` (owner/repo from the session's git remote → `get_pr_by_head`) and `repoint_planned_pr` (→ `repoint_planned_pr_node`) RPC handlers, network/git in `spawn_blocking`, token-less/no-PR → `exists=false`. `SessionEntry.branch` populated from `Changeset.branch` in `ListSessions` enrichment. Feature [pr-stack-live-status.md](../../../docs/ft/coder/pr-stack-live-status.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)

- **2026-07-24** [Feature] terminal-file-drop-upload: new `session_file_upload` module — `write_upload_chunk` basename-validates both `upload_id` and `file_name`, appends ordered chunks to `{session_dir}/uploads/<upload_id>/<file_name>`, and applies a canonicalize-and-contain guard rooted at the trusted `{session_dir}/uploads` root (traversal/separator segment → `InvalidArgument`), returning the absolute host path on the final chunk. `ConnectionService::upload_session_file_chunk` authenticates the session token and validates the session-id segment before any filesystem access; delegated through the tonic adapter. Tests: `session_file_upload_rpc` 9. Feature [web-terminal.md § File drop upload](../../../docs/ft/web/web-terminal.md#file-drop-upload). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-24** [Feature] pr-stack-context-docs: new `session_context_docs` module — `context_docs_for_session(recipe, session_dir)` lists the recipe manifest's planning docs (absolute `artifacts/` path + on-disk existence + per-key description via `SessionArtifactManifest::artifact_doc_descriptions()`; blank/unknown recipe → empty), and `read_session_context_doc_utf8` reads an allowlisted doc with a canonicalize-and-contain guard rooted at `session_artifacts_root` (non-manifest / traversal basename → `PermissionDenied`; mirrors `session_workflow_files`). `session_list_enrichment::apply_session_list_status_to_proto` populates `SessionEntry.context_docs` (field 27). New internal `tddy-workflow` dep. Tests: `session_context_docs` 4 + `apply_context_docs_to_proto_lists_the_recipe_manifest_docs`. Feature [pr-stacking.md](../../../docs/ft/coder/pr-stacking.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-24** [Feature] activities-as-acp: `ConnectionService` gains `stream_acp_replay` (+ `MpscAcpReplayStream`, `acp_replay_frame`, `relay_acp_replay`) — snapshot via `tddy_service::acp_replay::read_acp_transcript` then live `AgentActivityHub` tail (mapped to `AcpReplayFrame`), honouring `StreamMode` and mirroring `stream_session_activity` (Local-only; `PeerRoute::Forward` → `unimplemented`). Tonic adapter gains the `StreamAcpReplayStream` type + shim. Also fixed a pre-existing broken test target: `session_catalog_populate.rs` now calls `tddy_bsp::register_catalog_provider()` (was the non-existent `tddy_coder::catalog_provider::register()`). Feature [agent-activity-pane.md](../../../docs/ft/web/agent-activity-pane.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-23** [Feature] **Session worktree inspector actions** — `worktrees` module adds `CleanWorktreeError` + `clean_worktree_under_repo` (membership-gated `git clean -fdx`, refuses the primary row; mirrors `remove_worktree_under_repo`). `ConnectionService` gains `clean_worktree` and `restore_session_worktree` handlers (auth → arg validation → `main_repo_path_for_host` → `spawn_blocking` under `spawn_worker_request_timeout` → stats-cache invalidation → typed error mapping via `map_clean_worktree_error`); restore resolves the session dir (`sessions_base_for_user` + `unified_session_dir_path`) and reuses `tddy_core::resolve_persisted_worktree_integration_base_for_session` + `setup_worktree_for_session_with_integration_base`. Tonic adapter delegations added. Tests: `worktree_session_actions_acceptance` 3 + `worktrees_rpc` +6 (13 total). Feature [session-worktree-inspector.md](../../../docs/ft/web/session-worktree-inspector.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)

- **2026-07-23** [Feature] session-activity-streaming-mode: `stream_session_activity` honours `StreamMode` — `LIVE_ONLY` skips the snapshot replay (unknown/omitted → `SNAPSHOT_THEN_LIVE`); `report_agent_activity` parses the hook's `input_json`/`result_json` strings into structured `Value` (via `tddy_core::agent_activity::parse_activity_json`); the sandbox capture seam builds structured records; both map to the wire via `tddy_service::agent_activity_to_proto`. Feature [agent-activity-pane.md](../../../docs/ft/web/agent-activity-pane.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)

- **2026-07-23** [Feature] **Project default branch — unified resolution + `SetProjectDefaultBranch`** — `project_storage::effective_integration_base_ref_for_project` now returns a stored `main_branch_ref` verbatim, or for a legacy row resolves the default **live** via `tddy_core::resolve_default_integration_base_ref` (was the hardcoded `DOCUMENTED_DEFAULT_INTEGRATION_BASE_REF`); project-default validation relaxed to `validate_chain_pr_integration_base_ref` (any `origin/<path>`). New `set_project_default_branch` mutator + `SetProjectDefaultBranch` RPC handler (peer-forwarded via `forward_set_project_default_branch_via_livekit`, `classify_peer_route` like `AddProjectToHost`; `INVALID_ARGUMENT`/`NOT_FOUND` before any write) + tonic adapter; `main_branch_ref` populated in all `ProjectEntry` mappings. `StartSession` uses the project's stored default when the client sends no `selected_integration_base_ref` (claude-cli + both sandboxed helpers), legacy falls through to live resolution. Tests: `project_default_branch_resolution_acceptance` 4, `set_project_default_branch_acceptance` 4, `project_storage` unit 4. Feature [git-integration-base-ref.md](../../../docs/ft/coder/git-integration-base-ref.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-23** [Feature] **Semantic index orchestration** — new `semantic_index` module (`semantic_index_db_path`, `semantic_index_env` → `TDDY_SEMANTIC_INDEX_DB`, `run_semantic_index_blocking` → blocks on a `SemanticIndexTask`; `Err` aborts the start). Wired into all four managed start paths (claude+cursor × sandboxed+non-sandboxed): when `req.semantic_index`, runs `production_embedder(data_dir)?` + the blocking index before launch and injects the env pair. Non-default `local-model` passthrough feature. Feature [semantic-index.md](../../../docs/ft/coder/semantic-index.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)

- **2026-07-23** [Feature] **bsp-build-server — session-addressed `DaemonBspService`** — new `bsp_service.rs`: `DaemonBspService` (`SessionPathsResolver` + `tddy_data_dir`) implements the `bsp.BspService` trait by resolving each request's `(session_token, session_id)` to the session's `(session_dir, repo_root)` — token → os_user → sessions_base → `.session.yaml` `repo_path`, reusing the `ExecuteTool` preamble — and delegating to a per-request `tddy_bsp::BspServiceImpl`. `main.rs` repoints catalog-provider registration to `tddy_bsp::register_catalog_provider()`, builds the session-paths resolver, and registers a `bsp.BspService` entry in `rpc_entries`; `Cargo.toml` adds the `tddy-bsp` dep. Tests: bsp_service 2. Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). Feature [bsp-build-server.md](../../../docs/ft/coder/bsp-build-server.md). (tddy-daemon)

- **2026-07-22** [Refactor] **`stream_host_stats` replaces the unary host-stats handlers** — `connection_service.rs` drops `get_host_cpu_stats`/`get_host_disk_stats` (and their unit tests) for a single server-streaming `stream_host_stats`: authenticates the `session_token`, emits both CPU+disk immediately on subscribe, then drives two independent `interval_at` timers (`HOST_CPU_INTERVAL` 5 s / `HOST_DISK_INTERVAL` 60 s defaults, overridable via a new `with_host_stats_intervals` builder) in a `select!` loop, pushing a `HostStatsEvent` carrying the latest CPU+disk each tick over an mpsc-backed `MpscHostStatsStream` (task ends when the receiver drops). The `sysinfo`-backed `HostStats` provider is reused unchanged; `connection_tonic_adapter.rs` boxes the new stream type like `stream_session_activity`. Tests: `stream_host_stats_*` 4 (auth reject, immediate both-emit, independent CPU/disk cadence refresh via injected intervals). Feature [host-stats-footer.md](../../../docs/ft/web/host-stats-footer.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-06** [Feature] **`ListAgentModels` handler + tool-session `--model` threading** — `connection_service.rs` adds a `list_agent_models` handler that shells out to `tddy-tools list-models --agent <agent>` (via the configured tool path / spawn worker), parses the JSON with `parse_agent_models_json`, and returns `ListAgentModelsResponse`; results cached per (agent, daemon, **OS user**) with a short TTL (cursor/ACP catalogs are account-specific — keying by OS user prevents cross-user leakage on a multi-tenant daemon), the probe run via `run_capture_as_user` with `current_dir` = the user's **home** (daemon cwd may be unreadable after setuid; the ACP probe opens a session against the cwd). The `StartSession` tool branch threads `req.model` → `SpawnOptions.model` (`spawner.rs` appends `--model <m>` when non-empty) → `spawn_worker.rs` `SpawnOptions`/`SpawnRequest.model` + `build_spawn_request` passthrough. Tests: `build_spawn_request` carries `model` 1, `parse_agent_models_json` reads models/default + errors on malformed probe 2 (a failed probe is never an empty catalog). Feature [tool-session-model-selection.md](../../../docs/ft/web/tool-session-model-selection.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-19** [Feature] **`GrillMeConversationSpawnHandler` + stdio reverse-RPC host service** — `connection_service.rs` adds `GrillMeConversationSpawnHandler` (`impl ConversationSpawnHandler` → `spawn_claude_cli_session_inner(new_branch_from_base, stack_parent = Some(parent), managed_recipe = None)` + `conversation_branch_slug`), a `conversation_spawn_handler: Option<Arc<dyn ConversationSpawnHandler>>` parameter threaded through `spawn_claude_cli_session_inner` → `prepare_managed_workflow_inner`, and a `recipe_spawn_handlers(recipe, …)` helper selecting the `(child, conversation)` handler pair — bound for `grill-me` in `start_claude_cli_session` + `start_sandboxed_claude_cli_session` (mirrors the pr-stack child-handler block). `session_toolcall.rs` threads it through `set_up/resume/build_managed_workflow` + `start_session_toolcall_listener` (bound via `.with_conversation_spawn_handler`). Reverse-RPC (fe8ca30d): `host_session_service.rs` `HostSessionService` (JSON reverse RPC over `tddy_stdio`, reuses the grill-me handler); `spawner` pipes child stdio → `LocalChildStdio` (gated on `stdio_reverse`); `connection_service` bridges each grill-me child's fds via `StdioEndpoint::from_duplex` into a per-session `session_stdio` registry; `model_override` for tool sessions; `--grpc` retained. Deferred `TODO(stdio-relay)`: presenter-intent off `--grpc`, resume + remote/multi-host spawn. Tests: `recipe_enables_conversation_spawn` selection + `host_session_service` 3/3. Feature [spawn-conversation.md](../../../docs/ft/coder/spawn-conversation.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-11** [Feature] **`SandboxCgroupConfig` daemon section for the unprivileged Linux jail** — new optional `sandbox_cgroup:` config section (`SandboxCgroupConfig` + `DaemonConfig.sandbox_cgroup` + `sandbox_cgroup_config()` mapping onto `tddy_sandbox::CgroupConfig`) is threaded into `build_sandbox_plan` (`SandboxRunnerSpawn.cgroup`, set as `plan.cgroup`) at the 3 `connection_service.rs` spawn call sites, so a running-as-`tddy` daemon can override the runtime-derived delegated cgroup v2 base; absent → derive from `/proc/self/cgroup`. Pairs with `./install` flipping the systemd unit to `User=tddy`/`Delegate=yes`/`AppArmorProfile=tddy-daemon` (service-user creation, log/auth chown, AppArmor profile ship+auto-load). Config test 1. Architecture [tddy-sandbox § Linux cgroups jail](../../tddy-sandbox/docs/architecture.md#linux-cgroups-jail), install [systemd-install.md § Unprivileged service](../../../docs/ft/daemon/systemd-install.md#unprivileged-service-linux-cgroups-sandbox). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon, tddy-sandbox, tddy-sandbox-cgroups)
- **2026-07-22** [Feature] **Agent-activity hub, stream, and report handlers** — new **`AgentActivityHub`** (`Mutex<HashMap<sessionId, broadcast::Sender<AgentActivityRecord>>>`); `StreamSessionActivity` (snapshot via `read_agent_activity` then relay hub events with `Lagged` handling, mirroring `StreamTerminalOutput`; **Local-only**, `PeerRoute::Forward` → `unimplemented`); `ReportAgentActivity` (append + publish, auth like `report_session_status`, Pre→Post coalescing, bad-token rejection). `sandbox_session.rs`: `DaemonToolHandler::execute` threads `session_dir` + hub and appends running/terminal rows. `connection_tonic_adapter.rs` binds the new stream type. Doc [connection-service.md § Agent-activity log & stream](./connection-service.md#agent-activity-log--stream). Feature [agent-activity-pane.md](../../../docs/ft/web/agent-activity-pane.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)

- **2026-07-22** [Feature] reusable-lsp: register the `TddyLspExecutor` at startup sharing the daemon `TaskRegistry` + a 60s idle-reaper loop; `lsp_tools_env(worktree)` sets `TDDY_LSP_TOOLS` at the sandboxed-session env sites; `relay_idle::IdleTimeoutTracker` now re-exports `tddy_task::IdleTimeoutTracker`. Cross-package [changeset](../../../docs/dev/changesets.md). PR [#310](https://github.com/uppin/tddy-coder/pull/310).

- **2026-07-22** [Feature] **Worktree Code-pane file RPCs — `ListWorktreeDirectory` / `ReadWorktreeFile`** — new **`worktree_files`** module browses a session's git worktree (`SessionEntry.repo_path`): `list_worktree_directory_entries` lists one directory level via `git ls-files --cached --others --exclude-standard -z` (dirs-first, `.git`-excluded, `.gitignore`-aware; a linked worktree's private `<gitdir>/info/exclude` is fed in via `--exclude-from`), and `read_worktree_file_utf8` reads a listed file with `..`/absolute rejection + canonicalize-and-contain and a 1 MiB cap (`truncated`/`byte_size`). Handlers reuse the `RemoveWorktree` preamble and gate on the new shared **`worktrees::worktree_path_is_listed`** (extracted from `remove_worktree_under_repo`); git/fs work runs in `spawn_blocking`. Tonic adapter delegates both. Units 8 (`worktree_files`) + integration 6 (`worktree_files_rpc`); `worktrees` regression 3/3, clippy clean. Doc [connection-service.md § Worktree Code pane file RPCs](./connection-service.md#worktree-code-pane-file-rpcs). Feature [session-code-pane.md](../../../docs/ft/web/session-code-pane.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-21** [Feature] **Host stats provider + `ConnectionService` handlers** — new `host_stats.rs`: `sysinfo`-backed `SysinfoHostStats` (long-lived per-core CPU sampler; `select_mount_for_path` longest-path-component-prefix disk resolution against `$HOME/<repos_base_path_or_default>`, largest-mount fallback) behind a `HostStats` trait. `ConnectionServiceImpl` gains an `Arc<dyn HostStats>` field (default `SysinfoHostStats`) + a `with_host_stats` test seam, and token-validating `get_host_cpu_stats`/`get_host_disk_stats` handlers (tonic adapter delegates both). Units: 4 `select_mount_for_path` + 4 handler token/mapping tests. Doc [connection-service.md § Host stats](./connection-service.md#host-stats-host-stats-footer). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). PR [#306](https://github.com/uppin/tddy-coder/pull/306). (tddy-daemon)
- **2026-07-16** [Refactor] **PTY runtime extracted to shared `tddy-pty`; Bash tool uses the passwd login shell** — `pty_runtime.rs`/`pty_registry.rs` now delegate to the new `tddy-pty` crate (`PtyRuntime`/`open_pty_and_pump`/`PtyRegistry`/`PtyControl` moved out); the daemon keeps OS-user impersonation (`resolve_pty_os_user`/`wrap_argv_for_privilege_drop`/`pty_user_env_overrides`) and pre-computes the final argv/env via `resolve_final_argv_env` before calling the shared spawner (`FailedSpawnBody` preserves the unresolvable-user path). `StartTerminalSession` resolves the Bash tool's shell from the target user's passwd `pw_shell` (`login_shell_for_os_user`), falling back to `$SHELL` then `/bin/bash`, so a nix/systemd `$SHELL` no longer leaks in. Daemon PTY unit + `terminal_session`/`terminal_control` acceptance tests unchanged and green. Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). PR [#298](https://github.com/uppin/tddy-coder/pull/298). (tddy-daemon, tddy-pty)
- **2026-07-12** [Refactor] **`tool_engine` + `tool_catalog` extracted into shared `tddy-tool-engine`** — `src/tool_engine.rs` + `src/tool_catalog.rs` moved to the new `tddy-tool-engine` crate (shared with `tddy-coder`); `lib.rs` re-exports it as `tool_engine` so internal `tool_engine::execute_tool` / `execute_tool_with_env` call sites are unchanged; the sandbox-allowlist sync test moved to `src/tool_catalog_sync.rs` (`workspace_exec_tool_names_match_tool_catalog`). `connection_service.rs` `ListExecTools` maps `tddy_tool_engine::ToolDef` → proto `ToolDef` at the RPC boundary; `DeleteSession`/`SignalSession` remain daemon-direct (no behaviour change) with a new contract test `delete_session_unit_accepts_daemon_direct_caller_with_valid_token`. `SessionEntry` gains `bytes_in`/`bytes_out`/`last_data_received_at` (proto fields 24/25/26) populated from the `GrpcSessionTerminal` traffic meter for claude-cli/cursor-cli/workspace sessions and zero/empty for stopped tddy-coder sessions. Feature [terminal-sessions.md § Session-scoped RPC routing & daemon-direct lifecycle](../../../docs/ft/daemon/terminal-sessions.md#session-scoped-rpc-routing--daemon-direct-lifecycle). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). PR [#297](https://github.com/uppin/tddy-coder/pull/297). (tddy-daemon, tddy-tool-engine, tddy-service)
- **2026-07-12** [Refactor] **`usage_watcher` moved to `tddy-core`** — the `SessionUsageEmitter` / `spawn_usage_watcher` module moved out of `tddy-daemon` (to `tddy-core`, so `tddy-coder`'s `run_daemon` can call it); the `usage_watcher` integration test now imports from `tddy_core`. Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). PR [#295](https://github.com/uppin/tddy-coder/pull/295). (tddy-daemon)
- **2026-07-12** [Feature] **Specialized-agent warm-up gate** — `start_sandboxed_claude_cli_session` and `start_sandboxed_cursor_cli_session` call `tddy_discovery::warmup::warm_up_agents` right after resolving the specialized-agent defs and before spawning the jail; a cold/unreachable agent endpoint fails the start with `FAILED_PRECONDITION` (naming the agent/endpoint/model) instead of stalling the main agent's first `subagent_prompt` — no fallback. `502`/`5xx`/`429`/connection-errors retry to a 120s budget, `404` fails fast; resume reuses the start path so it is gated too. Architecture [connection-service.md § Sandboxed Claude Code CLI sessions](connection-service.md#sandboxed-claude-code-cli-sessions). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). PR [#296](https://github.com/uppin/tddy-coder/pull/296). (tddy-daemon, tddy-discovery, tddy-sandbox-app)
- **2026-07-11** [Feature] **Local peer-trust UDS + daemon-assisted sandbox for the Linux app** — a UDS `tonic` server serves `ConnectionService` via a hand-written adapter over the existing impl (shared via codegen `from_arc`); `MintLocalToken` mints a session token from the SO_PEERCRED peer uid (`local_token_login_for_uid`); `start_sandboxed_claude_cli_session` honors client `repo_path` (used directly, never auto-removed — `.worktrees` guard) + `claude_args`. Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). PR [#291](https://github.com/uppin/tddy-coder/pull/291) (draft; **not run end-to-end**; stacks on #290). (tddy-daemon, tddy-service, tddy-sandbox-app)

- **2026-07-06** [Feature] **Sandboxed cursor-cli sessions** — `start_sandboxed_cursor_cli_session`; `managed_codebase`/`recipe`/`specialized_agents` for cursor-cli; managed orchestration via `.cursor/rules/tddy-managed-workflow.mdc`. Feature [cursor-cli-session.md](../../../docs/ft/daemon/cursor-cli-session.md); architecture [connection-service.md](./connection-service.md#sandboxed-cursor-agent-cli-sessions). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). PR [#287](https://github.com/uppin/tddy-coder/pull/287). (tddy-daemon, tddy-sandbox-recipes, tddy-sandbox-runner)
- **2026-07-05** [Feature] **Cursor Agent CLI session** — `cursor_cli` config block; `cursor_cli_spawn` + `install_cursor_hooks_in_worktree`; `connection_service` Start/Resume/Connect/`ReportSessionStatus` for `cursor-cli`; Telegram `/start-cursor` + `tcur:` model keyboard; `cli_session_manager` module (shim `claude_cli_session`). PRD [cursor-cli-session.md](../../../docs/ft/daemon/cursor-cli-session.md). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-07-04** [Feature] **Durable web session — refresh-kind token enforcement** — the per-RPC `SessionUserResolver` (`auth.rs`) now additionally requires `claims.kind == TokenKind::Access`, rejecting a `refresh`-kind token even if it signature-verifies — the long-lived credential minted by `RefreshSession` must never authenticate a normal RPC. Complements `tddy-github`'s new two-token (access + refresh) model. Feature [session-auth.md § Durable sessions](../../../docs/ft/daemon/session-auth.md#durable-sessions-access--refresh-tokens). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). PR [#283](https://github.com/uppin/tddy-coder/pull/283). (tddy-daemon, tddy-github, tddy-service, tddy-web)
- **2026-07-04** [Fix] **Sandboxed claude-cli: real binary resolution + persistent jail `$HOME`** — `config::resolve_claude_binary_path` resolves the real host `claude` to an absolute path (prefers `~/.local/bin/claude`, skips Superset wrapper-shim dirs; `TDDY_CLAUDE_BINARY` / `claude_cli.binary_path` overrides) — a bare name emitted `(subpath "")` and made `sandbox-exec` reject the profile (exit 65 → `deadline_exceeded`). Adds a single daemon-wide **persistent jail `$HOME`** (`config::resolve_claude_home_dir`: `TDDY_SANDBOX_CLAUDE_HOME` > `claude_cli.claude_home_dir` > `$HOME/.tddy/sandbox-claude-home`), mounted read-write and seeded non-clobbering (`prepare_persistent_claude_home`, spawn passes `host_home: None`) so refreshed OAuth tokens/history/settings persist across sessions. `config::resolve_sandbox_config_path` resolves a per-OS `claude-sandbox.<os>.yaml` (loader is a follow-up). Architecture [connection-service.md § Sandboxed Claude Code CLI sessions](./connection-service.md#sandboxed-claude-code-cli-sessions). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). PR [#281](https://github.com/uppin/tddy-coder/pull/281). (tddy-daemon, tddy-sandbox, tddy-sandbox-recipes, tddy-sandbox-app)
- **2026-07-04** [Feature] **Cross-daemon verifiable session tokens** — `auth::build_auth_entries` builds a `tddy_github::SessionTokenSigner` from `livekit.api_secret` and wires `AuthServiceImpl::new_signed` (stub + real providers via a new generic `auth_service_entry` helper); the `SessionUserResolver` now verifies a token's HMAC signature + expiry and extracts the login instead of a per-daemon `HashMap` lookup, so any daemon holding the shared secret accepts a token any other daemon minted (fixes daemon-switch `invalid or expired session` and the peer `ListProjects`/`StartSession`/`AddProjectToHost` forwarding paths). No `api_secret` ⇒ resolver rejects every token (fail-closed); the `tddy_data_dir` param and `auth-sessions.json` persistence were dropped. `cargo test -p tddy-daemon` lib 236/0; cross-daemon acceptance 2/2 + resolver 3/3 green. Feature [../../../docs/ft/daemon/session-auth.md](../../../docs/ft/daemon/session-auth.md). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon, tddy-github, tddy-service, tddy-web)
- **2026-07-04** [Feature] **Auto-provision project on session start + advertise base clone location** — `project_provision::ensure_project_available_locally` (injected `cloner` + `peer_lookup` for testability) is wired into `start_session` via `ensure_project_available_for_start` on the local branch (covers tool/claude-cli/sandboxed/workspace): clones a registered-but-missing checkout from the stored `git_url`, peer-discovers `(name, git_url)` for an unregistered project (same fan-out as aggregated `ListProjects`) and clones into `repos_base_for_user(os_user, repos_base_path)/<name>` + `add_or_get_project`, else `NotFound`. Real cloner mirrors `AddProjectToHost` (`SpawnClient::clone_repo` when a spawn worker is set, else `spawner::clone_as_user`), runs via `spawn_blocking` + `timeout`, surfaces clone errors (no masking) and preserves `NotFound`. `DaemonAdvertisement` gains `repos_base_path` (published from `repos_base_path_or_default()`, parsed by `parse_daemon_advertisement_json`; the web reads the same key). `cargo test -p tddy-daemon` 230/1 (1 pre-existing env). [connection-service.md § Auto-provisioning on session start](connection-service.md#auto-provisioning-on-session-start). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon, tddy-web)
- **2026-07-04** [Refactor] **Non-blocking peer `ListProjects` fan-out** — `EligibleDaemonSource::peer_project_entries` is now an `async` trait method (`#[async_trait]`); `merge_listed_projects_with_peers` and the `ListProjects` handler await it directly, deleting the `block_in_place`+`Handle::block_on` (and `Handle::try_current`) bridge in `LiveKitEligibleDaemonSource` — no worker thread is parked and a current-thread runtime now suffices. `aggregate_peer_project_entries` fans out to peers concurrently (`futures_util::future::join_all`) so the aggregate is bounded by the slowest responsive peer, not the serial sum; timeout/error skip semantics unchanged. New acceptance `non_blocking_peer_fanout_acceptance`; existing peer sources migrated to the async trait. [connection-service.md § Multi-host projects](connection-service.md#multi-host-projects). (tddy-daemon)
- **2026-07-03** [Feature] **PR-stack child-spawn relay + managed-orchestrator env fix** — `pr_spawn_child` is served by a per-session `StackChildSpawnHandler` (bound in `start_claude_cli_session` only when `managed_recipe.name() == "pr-stack"`, so an orchestrator can spawn only its own stack's children) that reads the orchestrator changeset for the planned node and calls the shared `spawn_claude_cli_session_inner` — extracted verbatim from `start_claude_cli_session` (the RPC path is now a thin wrapper, behavior unchanged) — with `stack_parent` set, `managed_recipe = None`, `new_branch_from_base`, inheriting the orchestrator's `SessionMetadata.model`. `session_toolcall.rs` threads the handler to the toolcall listener (`with_child_spawn_handler`). Also fixes a latent runtime bug: `prepare_managed_workflow` now exports `TDDY_SESSION_DIR` + `TDDY_REPO_DIR` (previously only `TDDY_SOCKET` + `PATH`), without which the PR-management tools had no session/repo in scope. Feature [pr-stacking.md § PR-management tools](../../../docs/ft/coder/pr-stacking.md#pr-management-tools); [connection-service.md](connection-service.md#pr-stack-child-spawn-relay). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon, tddy-core, tddy-tools)
- **2026-07-03** [Feature] **`AddProjectToHost` handler + multi-host project aggregation** — `ConnectionServiceImpl::add_project_to_host` makes an existing project available on another host reusing its `project_id`: routes local-or-forward (`classify_peer_route` + new `forward_add_project_to_host_via_livekit`, mirroring `StartSession`), clones the repo and persists via the new idempotent `project_storage::add_or_get_project`. `list_projects` honors a new `local_only` flag (skips peer merge); `LiveKitEligibleDaemonSource::peer_project_entries` now fans out to peers' `ListProjects` (`local_only=true`, tagged by `daemon_instance_id`, bridging sync→async via `block_in_place`+`Handle::block_on`). [connection-service.md § Multi-host projects](connection-service.md#multi-host-projects). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon, tddy-service, tddy-web)
- **2026-07-03** [Feature] **Managed-codebase workflow wiring** — `ConnectionServiceImpl::prepare_managed_workflow` (shared across start/resume × sandbox) seeds `changeset.yaml` at the recipe start goal, builds a per-session `WorkflowController` + toolcall listener (new `session_toolcall.rs`), injects the recipe's orchestration prompt (`--append-system-prompt-file`) and a per-session `TDDY_SOCKET`/`PATH`, and persists `recipe` for resume; `tool_engine`/`pty_runtime`/`claude_cli_session`/`sandbox_session` thread the per-session env so host-side `tddy-tools transition` advances the controller; resume re-wires at the persisted goal; unknown recipe → `INVALID_ARGUMENT`. Feature [managed-codebase-workflow.md](../../../docs/ft/coder/managed-codebase-workflow.md); [connection-service.md](connection-service.md#managed-codebase-workflow-workflow-aware-claude-cli). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon, tddy-core, tddy-sandbox-runner, tddy-web)
- **2026-07-03** [Feature] **`AddPlannedPr` handler — manually add a planned PR to a pr-stack orchestrator** — `ConnectionServiceImpl::add_planned_pr` validates the session is a `"pr-stack"` orchestrator (`require_pr_stack_orchestrator`, resolves legacy aliases too) before delegating to `tddy_workflow_recipes::pr_stack::add_planned_pr_node`; `session_list_enrichment::stack_plan_json_for_changeset` narrowed to `pub(crate)` for reuse in the response. Feature [pr-stacking.md § Manually adding a planned PR](../../../docs/ft/coder/pr-stacking.md#manually-adding-a-planned-pr). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon, tddy-service, tddy-workflow-recipes)
- **2026-07-01** [Feature] **`SessionEntry.stack_plan_json` enrichment** — `session_list_status_from_session_dir` serializes `Changeset.stack` to JSON (empty string when absent) via `stack_plan_json_for_changeset`; threaded through `SessionListStatusDisplay` into proto field 23. Powers the web PR-Stack Chat Screen's planned-PR list. Feature [session-drawer.md](../../../docs/ft/web/session-drawer.md#pr-stack-chat-screen). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon, tddy-service)

- **2026-07-02** [Feature] **Daemon-hosted sandboxed sessions gain discovery-subagent + tool-replacement parity** — new `SubagentSpawnConfig`/`subagent_env_overlay`/`prepare_context_dir_with_subagent` (`sandbox_session.rs`); `start_sandboxed_claude_cli_session`, `relaunch_sandboxed_runner`, `resume_sandboxed_claude_cli_session`, and the `StartSession` dispatch all wired so a daemon-hosted session gets the same `TDDY_SUBAGENT_*`/allowlist-filtering/appendix behavior `tddy-sandbox-app` already had. Feature [managed-codebase-subagents.md § Tool replacement](../../../docs/ft/coder/managed-codebase-subagents.md#tool-replacement-subagent-declared) (AC18). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon, tddy-discovery, tddy-core, tddy-service)
- **2026-07-02** [Feature] **Sandboxed-session lifecycle switched to stdio** — `connection_service.rs`'s spawn/dial orchestration and `sandbox_session::dial_and_bridge` now dial `tddy-sandbox-runner` exclusively over its stdio-served `SessionChannel` (`bridge_sandbox_stdio` → `StdioSandboxClient` → `run_host_relay`), wiring in the primitive the previous changeset proved but didn't use; `--grpc-socket`/`--grpc-listen-port`/`--grpc-uds` argv and `pick_free_loopback_port`'s control-port allocation deleted for this call site (no dual-path fallback); `connect_sandbox_client`/`connect_sandbox_session_client` are kept (still used by `sandbox_action.rs`'s separate generic-action-execution flow). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon, tddy-sandbox-runner)
- **2026-07-02** [Feature] **VM service repointed at the VM & Image Library** — `main.rs:437-459`: VM lifecycle service construction now builds a `tddy_vm::VmLibrary` at the same resolved data root every other per-user store here uses (`user_sessions_path::tddy_data_root_matching_child`), logs (does not crash) on `init()` failure, and constructs `VmManager::from_library` instead of the old `vm-registry.json`-backed `VmManager::new`. Additive at the type level (`VmManager::new` still exists); the daemon's own VM service is the only call site moved. Feature [vm/tddy-vm.md](../../../docs/ft/vm/tddy-vm.md) § VM & Image Library; cross-package [changesets.md](../../../docs/dev/changesets.md). (tddy-daemon, tddy-vm)

- **2026-07-02** [Feature] **`ListSubagents` RPC + specialized-agent env wiring for sandboxed claude-cli sessions** — new `ListSubagents` RPC returns resolved `SpecializedAgentDef`s (builtin + `<tddyhome>/agents`) as `SubagentInfo` rows; `StartSessionRequest.managed_codebase`/`.specialized_agents` accepted for `session_type == "claude-cli"` sandboxed sessions — new `ConnectionServiceImpl::specialized_subagent_env` resolves named agents (an unresolvable name is `INVALID_ARGUMENT`) into `TDDY_SUBAGENT`/`TDDY_SUBAGENTS_JSON` jail env pairs; the sandboxed path already never mounts the repo, so `managed_codebase` is accepted for request-shape clarity rather than toggling mounts. Feature [specialized-subagents.md](../../../docs/ft/coder/specialized-subagents.md); technical [connection-service.md](connection-service.md#sandboxed-claude-code-cli-sessions). (tddy-daemon, tddy-discovery, tddy-service)
- **2026-07-01** [Feature] **stdio bridge primitive for sandboxed sessions (not yet wired into real call sites)** — new `sandbox_session::bridge_sandbox_stdio` converts a jail-spawned `SandboxHandle`'s piped (blocking) stdio into an async RPC endpoint via `tokio::net::unix::pipe` + a new `tddy_stdio::StdioEndpoint::from_duplex` constructor; proven end-to-end through a real Seatbelt jail, including a full `run_host_relay` tool-call round trip via the new `StdioSandboxClient` (`tddy-sandbox-runner`). **`connection_service.rs`'s spawn/dial orchestration and `dial_and_bridge` are unchanged** — real sessions still spawn with `--grpc-uds`/`--grpc-listen-port` and dial the tonic client; wiring the daemon's actual call sites onto this primitive is tracked in `docs/dev/TODO.md`. Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). PR [#253](https://github.com/uppin/tddy-coder/pull/253). (tddy-daemon, tddy-sandbox, tddy-sandbox-darwin, tddy-sandbox-runner, tddy-stdio)
- **2026-07-01** [Fix] **Sandboxed-action PTY session-end deadlock + ordering race** — `sandbox_action.rs`/`sandbox_plan_builder.rs`/`sandbox_session.rs`: session-end signaling now always defers `SessionEnded` to the next `HostPoll` (shared `tddy-sandbox-runner` fix) instead of pushing it immediately on the raw outbound stream, which could stall a not-yet-attached host indefinitely or race ahead of queued terminal output. Architecture [tddy-sandbox architecture](../../../packages/tddy-sandbox/docs/architecture.md). (tddy-daemon, tddy-sandbox-runner)
- **2026-07-01** [Feature] **PR stack support for Claude CLI sessions** — `session_list_enrichment` surfaces `recipe` from `changeset.yaml` into `SessionListStatusDisplay` and proto `SessionEntry` field 22; `start_claude_cli_session` + `start_sandboxed_claude_cli_session` accept `stack_parent` — sets `orchestrator_session_id` on child changeset and resolves git chain base via `resolve_chain_integration_base_ref_from_parent_session`; `resolve_chain_base_ref` private helper deduplicates both paths. Feature [session-drawer.md](../../../docs/ft/web/session-drawer.md). (tddy-service, tddy-daemon, tddy-web)
- **2026-06-29** [Feature] **Unified actions + sandbox action execution** — `ActionServiceImpl` (`ListActionKinds`/`StartAction`/`GetAction`); `sandbox_plan_builder` + `sandbox_action` (confined process + runner-PTY via `tddy-sandbox-runner --pty-command`); `PtyRuntime`/`ProcessRuntime` integration; fast tools + session jobs + PTY terminals share `TaskRegistry`; acceptance: `action_sandbox_acceptance`, `action_service_acceptance`. Feature [background-tasks.md](../../../docs/ft/daemon/background-tasks.md). PR [#244](https://github.com/uppin/tddy-coder/pull/244). (tddy-daemon, tddy-actions, tddy-sandbox-recipes, tddy-sandbox-runner)
- **2026-06-28** [Feature] **Linux cgroups sandbox wiring + host-relay collapse** — `spawn_sandbox_runner` dispatches darwin/cgroups by target OS; `dial_and_bridge` collapsed onto shared `tddy_sandbox_runner::run_host_relay` (`DaemonToolHandler`), dials in-jail gRPC over AF_UNIX (`connect_sandbox_client_uds`) on Linux; runner argv gains `--grpc-uds`; dep `tddy-sandbox-cgroups` (linux). Acceptance: `sandboxed_claude_cli_starts_on_linux_with_the_cgroups_backend`. Architecture [tddy-sandbox](../../../packages/tddy-sandbox/docs/architecture.md). (tddy-daemon, tddy-sandbox-runner, tddy-sandbox-cgroups)
- **2026-06-27** [Feature] **Darwin-sandboxed Claude CLI sessions (local gRPC)** — `start_sandboxed_claude_cli_session`, `SandboxSessionManager`, `dial_and_bridge` on `SessionChannel`; `ResumeSession`/`DeleteSession` stop `SandboxHandle`; `.session.yaml` `sandbox: true`; deps `tddy-sandbox`, `tddy-sandbox-darwin`; acceptance: sandbox_behavior, sandboxed_claude_cli, sandboxed_session_lifecycle. Feature [claude-cli-session.md](../../../docs/ft/daemon/claude-cli-session.md); technical [connection-service.md](./connection-service.md#sandboxed-claude-code-cli-sessions). (tddy-daemon, tddy-sandbox, tddy-sandbox-darwin, tddy-service, tddy-tools)

- **2026-06-26** [Fix] **PTY terminal width fix** — `stream_terminal_output`: reads `initial_cols`/`initial_rows` from request, resizes PTY via `PtyHandle::resize`, drains stale broadcast messages (`try_recv` loop), calls `trigger_redraw()`, skips capture replay when dims provided; `PtyHandle::send_input`: strips `\x1b]resize;{cols};{rows}\x07` escape sequences before forwarding stdin; `kill_all()`: SIGTERM + 5s wait + SIGKILL for all registered PIYs, clears registry; CAPTURE_LIMIT_BYTES raised 64KB→512KB; 4 kill_all unit tests, 3 component acceptance test updates.

- **2026-06-26** [Feature] **Screen sharing — generalize VNC → ScreenSharing + RDP support** — renamed `vnc_service.rs` → `screen_sharing_service.rs` (`ScreenSharingServiceImpl`, protocol-aware vault lookup, `start_stream` dispatches to VNC or RDP bridge binary, `build_bridge_spawn_config` extracted, magic-constant named); renamed `vnc_vault.rs` → `screen_sharing_vault.rs` (`ScreenSharingVault`, domain `ScreenSharingTarget.{protocol,username}`, `.screen-sharing.yaml` vault file, `create_dir_all` parent dir on first write); `config.rs`: `ScreenSharingConfig` (`vnc_binary_path` + `rdp_binary_path`), `resolve_binary_path_for_protocol`; `main.rs`: `ScreenSharingServiceServer` mounted; `lib.rs`: `screen_sharing_service` + `screen_sharing_vault` modules; username threaded from proto → vault → service → bridge config; 5 service acceptance tests + 5 vault acceptance tests. Feature [screen-sharing-sessions.md](../../../docs/ft/web/screen-sharing-sessions.md). (tddy-daemon)
- **2026-06-26** [Feature] **PR-stack session UI — proto plumbing + stack_parent spawn threading** — `connection.proto`: `orchestrator_session_id = 21` on `SessionEntry`, `stack_parent = 15` on `StartSessionRequest`; `session_list_enrichment.rs`: `orchestrator_session_id` on `SessionListStatusDisplay`, read from `changeset.orchestrator_session_id`, applied to proto in `apply_session_list_status_to_proto`; `spawner.rs`: `stack_parent: Option<&str>` on `SpawnOptions`, passes `--stack-parent` arg to child; `spawn_worker.rs`: `stack_parent` field on `SpawnRequest` (fixes silent drop in worker path), threaded through `build_spawn_request` + `spawn_worker_main`; `connection_service.rs`: `stack_parent_for_spawn` derived from request, passed to both direct+worker `SpawnOptions`. Tests: 3 enrichment unit tests. Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-daemon)
- **2026-06-26** [Feature] **Browser DEBUG mask from daemon config** — `DaemonConfig.debug: Option<String>` (`#[serde(default)]`); `run_server` gains `debug` param, set on `ClientConfig.debug`; `main.rs` reads `config.debug` → `web_debug` → `run_server`; `dev.daemon.yaml` ships `debug: "tddy:term:*"`; 3 unit tests (default/absent-yaml/parse). PR [#233](https://github.com/uppin/tddy-coder/pull/233). (tddy-daemon)
- **2026-06-26** [Feature] **VNC sessions — VncVault + VncServiceImpl control plane** — new `vnc_vault.rs`: `VncVault` (Argon2id KDF + ChaCha20-Poly1305 AEAD; `.vnc.yaml` mode 0600; create/unlock/add_target/list_targets/remove_target/decrypt_password); new `vnc_service.rs`: `VncServiceImpl` (6 RPCs — ListVncTargets/AddVncTarget/RemoveVncTarget/UnlockVncVault/StartVncStream/StopVncStream; per-session vault-key cache; bridge-spawn FIXME); `config.rs` gains `VncConfig` with `binary_path` resolution; 7 vault acceptance tests + 5 service acceptance tests. Feature [vnc-sessions.md](../../../docs/ft/web/vnc-sessions.md). (tddy-daemon)

- **2026-06-26** [Fix] **PTY terminal width fix** — `stream_terminal_output`: reads `initial_cols`/`initial_rows` from request, resizes PTY via `PtyHandle::resize`, drains stale broadcast messages (`try_recv` loop), calls `trigger_redraw()`, skips capture replay when dims provided; `PtyHandle::send_input`: strips `\x1b]resize;{cols};{rows}\x07` escape sequences before forwarding stdin; `kill_all()`: SIGTERM + 5s wait + SIGKILL for all registered PIYs, clears registry; CAPTURE_LIMIT_BYTES raised 64KB→512KB; 4 kill_all unit tests, 3 component acceptance test updates.

- **2026-06-26** [Feature] **Single-screen terminal control mutex** — `ClaudeCliSessionManager`: `ControlLeaseInfo`/`ControlChangeEvent` types, `control: RwLock<HashMap<session_id, ControlLeaseInfo>>` + `control_tx: broadcast::Sender`, `claim_control`/`verify_control`/`current_control`/`subscribe_control`; `relay_control_events` free fn (extracted to reduce nesting); `claim_terminal_control`/`watch_terminal_control` handlers (auth, snapshot-then-delta, `ClaimOutcome` → proto response); `send_terminal_input` + `stream_session_terminal_io` enforcement gate (FAILED_PRECONDITION on wrong token). 10 acceptance tests. Feature [daemon/terminal-sessions.md](../../../docs/ft/daemon/terminal-sessions.md). (tddy-daemon)
- **2026-06-25** [Feature] **Session inspector Tools tab — durable tool-call log + ListSessionToolCalls handler** — new `tool_call_log.rs` module: `append_tool_call(session_dir, &record)` (JSONL append, `create_dir_all`), `read_tool_calls(session_dir)` (skip malformed lines, 500-entry tail cap); `execute_tool` handler appends a `ToolCallRecord` after every invocation (non-fatal); new `list_session_tool_calls` handler (auth → `validate_session_id_segment` → optional peer forward → `read_tool_calls` → map to `ToolCallInfo`); 5 unit tests + 6 acceptance tests. Feature [session-drawer.md](../../../docs/ft/web/session-drawer.md). (tddy-daemon)
- **2026-06-25** [Feature] **WatchTaskList RPC — real-time task list streaming** — `TaskServiceImpl.watch_task_list`: authenticates, rejects remote `daemon_instance_id`; `registry.list_and_subscribe()` → snapshot as `is_snapshot=true` events → live `TaskRegistryEvent`→`TaskListEvent` (Added/Updated/Removed) until 100ms idle; `registry_event_to_list_event` helper extracted. 5 acceptance tests. Feature [tasks-ui-realtime.md](../../../docs/ft/web/tasks-ui-realtime.md). (tddy-daemon)
- **2026-06-25** [Feature] **Multiple tools per session (Bash tool)** — `ClaudeCliSessionManager` two-level registry `session_id→(terminal_id→PtyHandle)`; `start_terminal` (Bash tool: `$SHELL` in worktree, kind `"bash"`, uuidv7 id, no inputs), `get_terminal`/`list_terminals`/`stop_terminal` (SIGTERM→SIGKILL, idempotent with PTY exit-monitor); `spawn_in_pty` generalized over a prebuilt argv (`spawn_tool` shared by claude + bash); `start_terminal_session`/`stop_terminal_session`/`list_terminal_sessions` handlers (auth, `"main"` stop guard → `INVALID_ARGUMENT`, worktree from main handle → `FAILED_PRECONDITION`, `$SHELL` resolve); 3 terminal I/O handlers route by `terminal_id` (empty ⇒ `"main"`, unknown ⇒ `NOT_FOUND`); `session_deletion::signal_pid` `pub(crate)`. 12 acceptance tests (`terminal_session_acceptance`). Feature [daemon/terminal-sessions.md](../../../docs/ft/daemon/terminal-sessions.md). (tddy-daemon)
- **2026-06-24** [Feature] **Long-running background Tasks — daemon wiring** — new `TaskServiceImpl` (5 RPCs: ListTasks, GetTask, WatchTask with subscribe-first + replay-then-live + `tokio::select!` terminal guard, CancelTask, SendInput); shared `TaskRegistry` across `ConnectionServiceImpl`, `VmServiceImpl`, `TaskServiceImpl` via register-then-expose pattern in `main.rs`; `tool_engine.rs`: `ShellTaskBody` (background Shell as task), `register_sync_task` (sync tools as terminal tasks), `Await` reads `TaskRegistry`; `shell_job_registry.rs` deleted; `tasks.TaskService` registered in `main.rs`. Tests: 11 task_service acceptance, 5 tool_engine acceptance (every-action-is-a-task invariant). Feature [daemon/background-tasks.md](../../../docs/ft/daemon/background-tasks.md). (tddy-daemon)
- **2026-06-21** [Feature] **tddy-vm — daemon wiring + session token validation** — `VmManager` + `VmServiceImpl` (with `SessionUserResolver`) registered inside `if let Some(user_resolver)` block (gated on auth config, same as `ConnectionService`); resolver cloned before being moved into `ConnectionServiceImpl`; `vm.VmService` ServiceEntry pushed via `rpc_entries`. Acceptance tests updated: resolver required in `VmServiceImpl::new`, 5 new tests covering `ListVmImages`, auth rejection for `ListVms`/`DefineVm`/`ListVmImages`. Feature [vm/tddy-vm.md](../../../docs/ft/vm/tddy-vm.md). (tddy-daemon)
- **2026-06-21** [Feature] **Session Inspector Drawer — surface extra session.yaml fields** — `session_reader.rs` `SessionEntry` struct gains `tool`, `session_type`, `updated_at`, `livekit_room`, `previous_session_id` fields populated from `SessionMetadata`; `connection_service.rs` `list_sessions` maps them to the five new proto fields. Feature [session-drawer.md](../../../docs/ft/web/session-drawer.md). (tddy-daemon)
- **2026-06-21** [Feature] **Demo goal Phase 2 — daemon VM lifecycle RPCs** — `DemoVmHandle` enum (`Booting`, `Running { vm: RunningVm, share_url }`, `Error(String)`) as per-session state; `demo_vm_state: Arc<Mutex<HashMap<String, DemoVmHandle>>>` field in `ConnectionServiceImpl`; `StartDemoVm` reads session's `demo-plan.md`, builds `DemoVmConfig`, spawns `boot()` background task; `StopDemoVm` removes handle and calls `shutdown()`; `GetDemoVmStatus` returns `DemoVmState + ssh_host_port + share_url`; `tddy-demo-runner` dep added. Feature [coder/demo-goal.md](../../../docs/ft/coder/demo-goal.md). (tddy-daemon)
- **2026-06-15** [Feature] **RPC Playground — daemon registration + LiveKit participant** — `reflection_entry_from()` registered in `main.rs` after `rpc_entries` assembly; daemon spawns a dedicated `LiveKitParticipant` in the common room (identity `daemon-{id}`) serving the full RPC set so the playground can discover and invoke via data channel. Feature [rpc-playground.md](../../../docs/ft/daemon/rpc-playground.md); product [daemon/changelog.md](../../../docs/ft/daemon/changelog.md). (tddy-daemon)
- **2026-06-14** [Feature] **Remote-codebase mode** — workspace sessions (`start_workspace_session`, `resolve_worktree_root_for_session`); `tool_engine.rs` (10 cursor tools, `contain_path` security); `tool_catalog.rs`; `shell_job_registry.rs` (background shell + Await); `execute_tool`/`list_exec_tools` handlers; relay mode (`--relay`, `startup_config_check`, `RelayConfig`); `IdleTimeoutTracker`, `with_idle_tracker`, `record_rpc_activity`; `forward_to_peer` + per-peer `RpcClient` cache; `classify_peer_route`; external shutdown channel in `run_server`; idle monitor task in `main.rs`. Feature [remote-codebase-mode.md](../../../docs/ft/daemon/remote-codebase-mode.md); product [daemon/changelog.md](../../../docs/ft/daemon/changelog.md). (tddy-daemon)
- **2026-06-13** [Feature] **Claude Code CLI permission mode selection** — `build_claude_argv` gains `permission_mode: Option<&str>` (5th param); `ClaudeCliSessionManager::start()` gains `permission_mode: Option<&str>` (6th param); `--permission-mode <mode>` appended to PTY argv (default: `auto`; empty/whitespace normalised to `auto`); `StartSessionRequest.permission_mode` (proto field 14) wired through `connection_service::start_session → start_claude_cli_session → manager.start → build_claude_argv`; `tddy-tools pty-relay --permission-mode` optional CLI arg. Tests: `claude_cli_permission_mode_acceptance` (16 tests). Feature [claude-cli-permission-mode.md](../../../docs/ft/daemon/claude-cli-permission-mode.md). (tddy-service, tddy-daemon, tddy-tools)
- **2026-06-13** [Feature] **Telegram alerts for claude-cli session elicitations** — **`telegram_notifier`**: `TelegramSessionWatcher::on_claude_cli_activity_status_changed` — alerts tracked chats on `WaitingForInput` / `Done` transitions (dedupe on same-status repeat); `last_activity_status` HashMap field; **`telegram_tracked_session`**: `chats_tracking_session` reverse lookup; **`connection_service`**: `report_session_status` calls `on_claude_cli_activity_status_changed` when Telegram configured. Tests: `telegram_claude_cli_activity_alert_acceptance` (5 new), inline unit tests for `chats_tracking_session` (2 new) and `on_claude_cli_activity_status_changed` (6 new). Feature [telegram-notifications.md § Claude Code CLI session activity alerts](../../../docs/ft/daemon/telegram-notifications.md#claude-code-cli-session-activity-alerts). (tddy-daemon, docs)
- **2026-06-13** [Feature] **Per-worktree hooks — claude-cli session activity status** — **`connection_service`**: **`report_session_status`** RPC handler (path guard, `os_user` sessions_base, constant-time `hook_token`, `update_activity_status`); hook wiring in `start_claude_cli_session` (UUID token, writes `.claude/settings.local.json`); **`ClaudeCliSessionManager`** injected as constructor param; `build_claude_argv`/`PtyHandle::resize()`; **`session_list_enrichment`** surfaces `activity_status`; **`config`**: `tddy_tools_path`/`daemon_url` on `ClaudeCliConfig`. Feature [claude-cli-session.md](../../../docs/ft/daemon/claude-cli-session.md#session-activity-status-via-per-worktree-hooks). (tddy-daemon)
- **2026-06-13** [Feature] **`/start-claude` Telegram command — Claude Code CLI sessions seeded with first prompt** — **`claude_cli_session`**: `start(…, initial_prompt: Option<&str>)` passes positional arg to `claude`; `build_claude_argv` pure helper; `resume` never replays prompt; **`StartSessionRequest.initial_prompt`** (field 13) wired through RPC; **`ClaudeCliSessionManager`** now injected into `ConnectionServiceImpl::new` (shared `Arc`); **`TelegramWorkflowSpawn.claude_cli_manager`** field; **`telegram_session_control`**: `handle_start_claude`, `handle_telegram_claude_model_callback`, `spawn_telegram_claude_cli`, `claude_cli_branch_name_from_changeset`, model keyboard (`tcm:` callbacks, 3 models); branch callback routes to model keyboard for `session_type == "claude-cli"`, sets `new_branch_name` from `cs.name` before worktree setup; **`telegram_bot`**: `/start-claude` dispatch + `tcm:` callback. Tests: `claude_cli_session_acceptance` (6 new, 12 total), `telegram_start_claude_acceptance` (4 new). Feature [claude-cli-session.md § Seeding a first prompt](../../../docs/ft/daemon/claude-cli-session.md), [telegram-session-control.md § /start-claude flow](../../../docs/ft/daemon/telegram-session-control.md). (tddy-service, tddy-daemon, docs)
- **2026-06-06** [Fix] **Stable chain parent session id in `tcp:` callback** — format changed from `tcp:<idx>|s:<child>` to `tcp:p:<parent_tail8>|s:<child>` (last 8 chars of parent id); `handle_chain_parent_callback` scans candidates by tail instead of index — immune to list churn. `parse_chain_workflow_prompt` unit tests added. (tddy-daemon)
- **2026-06-06** [Feature] **Claude Code CLI session type** — **`claude_cli_session`**: **`ClaudeCliSessionManager`** (tokio-channel subprocess registry; `start()` spawns `claude --model <m> --session-id <id>`; broadcast stdout, mpsc stdin; exit monitor removes from registry); **`connection_service`**: `start_session` claude-cli branch, `connect_session` early return, `stream_session_terminal_io` bidi handler, `delete_session` worktree cleanup; **`session_list_enrichment`** populates `agent = "claude-cli"` and `model`; **`config`** optional `claude_cli.binary_path`. Tests: `claude_cli_session_acceptance`. Feature [claude-cli-session.md](../../../docs/ft/daemon/claude-cli-session.md); technical [connection-service.md](./connection-service.md); product [daemon/changelog.md](../../../docs/ft/daemon/changelog.md). (tddy-service, tddy-core, tddy-daemon, tddy-web, docs)
- **2026-05-02** [Documentation] **Session chaining Phase 2 (live `tcp:`, chain-base merge on blocking pool, tests)** — **`telegram_bot`**: **`maybe_dispatch_tcp_chain_parent_callback`**, **`workflow_callback_gate_authorized`**; **`telegram_session_control`**: **`parent_candidates_page_for_chain_picker`**, **`handle_chain_parent_callback`** validates **`tcp:`** child ids; **`spawn_telegram_workflow`** runs **`merge_chain_integration_base_with_explicit_operator_overrides`** in **`tokio::task::spawn_blocking`**. Tests: **`session_chaining_phase2_acceptance`**, **`session_chaining_phase2_unit`**, integration **`parent_candidates_page_for_chain_picker_*`**, **`telegram_chain_parent_callback_rejects_invalid_child_session_id_segment`**. Feature **[telegram-session-control.md](../../../docs/ft/daemon/telegram-session-control.md)**; optional product follow-up **[2026-05-02-changeset-session-chaining.md](../../../docs/dev/1-WIP/2026-05-02-changeset-session-chaining.md)**. (tddy-daemon, docs)
- **2026-05-02** [Feature] **Session chaining (Telegram harness + `/chain-workflow` message)** — **`telegram_session_control`**: **`handle_chain_workflow`**, **`handle_chain_parent_callback`**, **`parse_telegram_chain_parent_callback`**, **`parse_chain_workflow_prompt`**; **`telegram_bot`**: **`/chain-workflow`** message dispatch. Tests: **`telegram_session_control_integration`**, **`telegram_chain_workflow_dispatch_acceptance`**. Feature **[telegram-session-control.md](../../../docs/ft/daemon/telegram-session-control.md)**. (tddy-daemon, docs)
- **2026-05-02** [Documentation] **Telegram notifier logging** — Removed obsolete **`marker_json`** trace-marker description; documented **`tddy_daemon::telegram_bot::message-body`** and **start-workflow** telegram-tracked binding in [telegram-notifier.md](./telegram-notifier.md). (tddy-daemon)
- **2026-05-02** [Feature] **Session chaining (Telegram harness + `/chain-workflow` message)** — **`telegram_session_control`**: **`handle_chain_workflow`**, **`handle_chain_parent_callback`**, **`parse_telegram_chain_parent_callback`**, **`parse_chain_workflow_prompt`**; **`telegram_bot`**: **`/chain-workflow`** message dispatch. Tests: **`telegram_session_control_integration`**, **`telegram_chain_workflow_dispatch_acceptance`**. Feature **[telegram-session-control.md](../../../docs/ft/daemon/telegram-session-control.md)**; live **`tcp:`** callback dispatch tracked in **[2026-05-02-changeset-session-chaining.md](../../../docs/dev/1-WIP/2026-05-02-changeset-session-chaining.md)**. (tddy-daemon, docs)
- **2026-05-02** [Feature] **Telegram tracked session gate + chat traffic logs** — **`telegram_tracked_session`**: **`SharedTelegramTrackedSessionCoordinator`**, **`should_suppress_workflow_keyboards_for_session`**, **Enter** bind + elicitation replay, **QueuePromotionReplay** bypass; structured **`telegram_traffic`** / **`telegram_bot`** logs. **`send_mode_changed_*`**, **`telegram_session_control`** replay bridge. Tests: **`telegram_tracked_session_acceptance`**, harness binds in concurrent + multi-select suites. Feature docs: [telegram-notifications.md](../../../docs/ft/daemon/telegram-notifications.md), [telegram-session-control.md](../../../docs/ft/daemon/telegram-session-control.md), [daemon changelog](../../../docs/ft/daemon/changelog.md). Technical: [telegram-notifier.md](./telegram-notifier.md). (tddy-daemon, docs)
- **2026-05-02** [Feature] **Telegram MultiSelect shortcuts** — **`telegram_multi_select_shortcuts`**, **`eli:mn:`** / **`eli:mr:`** (≤64-byte **`callback_data`**); **`MultiSelectShortcutElicitationMeta`** in **`TelegramSessionWatcher`**; **`telegram_bot`** gate + **`handle_elicitation_multi_select_shortcut`** → **`PresenterIntent::AnswerClarificationMultiSelect`**. Tests: **`telegram_multi_select_acceptance`**, **`telegram_concurrent_elicitation_integration`**. Feature docs: [telegram-session-control.md](../../../docs/ft/daemon/telegram-session-control.md), [telegram-notifications.md](../../../docs/ft/daemon/telegram-notifications.md), [daemon changelog](../../../docs/ft/daemon/changelog.md). Technical: [telegram-notifier.md](./telegram-notifier.md). (tddy-daemon, tddy-core, tddy-service, docs)
- **2026-04-11** [Architecture] **OAuth loopback TCP in daemon** — **`oauth_loopback_tunnel`**, **`codex_oauth_participant_metadata`**, **`run_oauth_tunnel_supervisor_follow_room_slot`** + **`livekit_peer_discovery`** **`Room`** slot; replaces desktop **`Bun.listen`** / **`@livekit/rtc-node`** operator path. Dev: [oauth-loopback-tunnel.md](./oauth-loopback-tunnel.md). Feature: [codex-oauth-relay.md](../../../docs/ft/daemon/codex-oauth-relay.md), [tddy-desktop-electrobun.md](../../../docs/ft/desktop/tddy-desktop-electrobun.md). (tddy-daemon, tddy-desktop, docs)
- **2026-04-11** [Feature] **`list_projects` registry + peer row hook** — **`connection_service`**: merges local **`projects.yaml`** with **`EligibleDaemonSource::peer_project_entries`** (default source: empty peer list); integration test **`list_projects_multi_daemon_aggregation`**. Feature: [web-terminal.md](../../../docs/ft/web/web-terminal.md), [daemon changelog](../../../docs/ft/daemon/changelog.md). Technical: [connection-service.md](./connection-service.md). (tddy-daemon)
- **2026-04-06** [Feature] **Codex OAuth relay (`codex_oauth_relay`)** — **`validate_codex_oauth_authorize_url`**, **`dispatch_browser_open_capture`**, **`relay_oauth_callback_to_registered_listener`**; **`url`** dependency; tests **`codex_oauth_relay::tests`**, integration **`codex_oauth_web_relay_acceptance`**. Feature docs: [codex-oauth-relay.md](../../../docs/ft/daemon/codex-oauth-relay.md), [codex-oauth-web-relay.md](../../../docs/ft/web/codex-oauth-web-relay.md); technical: [codex-oauth-relay.md](./codex-oauth-relay.md). (tddy-daemon, tddy-integration-tests)
- **2026-04-06** [Feature] **Telegram ↔ GitHub user linking (library)** — **`telegram_github_link`**: **`TelegramOAuthStateSigner`**, **`TelegramGithubMappingStore`**, **`resolved_os_user_for_telegram_workflow`**, **`complete_telegram_link_via_stub_exchange`**; **`TelegramSessionControlHarness::with_telegram_github_link`**, **`handle_start_workflow`** mapping gate. Tests: **`telegram_github_link`**. Feature doc: [telegram-session-control.md](../../../docs/ft/daemon/telegram-session-control.md). Technical: [telegram-github-link.md](./telegram-github-link.md). (tddy-daemon, docs)
- **2026-04-06** [Feature] **Telegram concurrent elicitation** — **`active_elicitation`**: per-chat FIFO queue and **`advance_after_elicitation_completion`**; **`telegram_notifier`** registers **`ModeChanged`** surfaces and defers non-primary **`eli:s:`** keyboards; **`telegram_session_control`** shares **`SharedActiveElicitationCoordinator`** with the watcher, advances on completion paths, gates **`/answer-text`** / **`/answer-multi`**; **`telegram_bot`** centralizes authorize + **`elicitation_callback_permitted`** for **`eli:s:`** / **`eli:o:`** / **`doc:`**. Integration tests: **`telegram_concurrent_elicitation_integration`**. Feature docs: [telegram-session-control.md](../../../docs/ft/daemon/telegram-session-control.md), [telegram-notifications.md](../../../docs/ft/daemon/telegram-notifications.md), [daemon changelog](../../../docs/ft/daemon/changelog.md). Technical: [telegram-notifier.md](./telegram-notifier.md). (tddy-daemon)
- **2026-04-06** [Feature] **Telegram `/start-workflow`: branch/worktree intent** — **`telegram_session_control`**: **`CB_TELEGRAM_INTENT`**, **`parse_telegram_intent_callback`**, **`send_intent_pick_keyboard`**, **`handle_telegram_intent_callback`**; **`send_project_pick_keyboard`** (renamed from **`send_project_pick_after_recipe`**); optional **`TelegramWorkflowSpawn::projects_dir_override`** for tests. **`telegram_bot`**: intent dispatch before project pick; after recipe, intent keyboard before project list. Integration tests **`telegram_intent_*`**. Feature doc: [telegram-session-control.md](../../../docs/ft/daemon/telegram-session-control.md). (tddy-daemon, docs)
- **2026-04-05** [Feature] **Telegram inbound session control + PresenterIntent** — **`telegram_bot`**, **`telegram_session_control`** (commands, callbacks, **`TelegramWorkflowSpawn`**), **`presenter_intent_client`**, shared **`elicitation_select_options`** cache with **`TelegramSessionWatcher`** for select confirmations. **`telegram_notifier`**: document body + keyboards for review/viewer; **`Select`** extended listing + numeric buttons. Feature docs: [telegram-session-control.md](../../../docs/ft/daemon/telegram-session-control.md), [telegram-notifications.md](../../../docs/ft/daemon/telegram-notifications.md), [daemon changelog](../../../docs/ft/daemon/changelog.md). Technical: [telegram-notifier.md](./telegram-notifier.md). (tddy-service, tddy-core, tddy-coder, tddy-daemon, tddy-web)
- **2026-04-05** [Feature] **Telegram recipe list: `review`** — **`telegram_session_control`**: **`RECIPE_MORE_PAGE`** includes **`review`** (CLI-normalized). Integration test **`telegram_session_control_integration`**. Feature doc: [workflow-recipes.md](../../../docs/ft/coder/workflow-recipes.md). (tddy-daemon)
- **2026-04-04** [Feature] **Session elicitation indicators** — **`elicitation`**: **`pending_elicitation_for_session_dir`**, **`telegram_elicitation_line_for_mode_changed`**, **`elicitation_signature_for_mode_changed`**. **`TelegramSessionWatcher`**: **`ModeChanged`** path + **`last_elicitation_signature`**. **`session_list_enrichment`**: proto **`pending_elicitation`**. Feature docs: [telegram-notifications.md](../../../docs/ft/daemon/telegram-notifications.md), [web-terminal.md](../../../docs/ft/web/web-terminal.md), daemon [changelog.md](../../../docs/ft/daemon/changelog.md). Technical: [telegram-notifier.md](./telegram-notifier.md). (tddy-core, tddy-service, tddy-daemon, tddy-web)
- **2026-04-04** [Feature] Project **`main_branch_ref`** — **`ProjectData`** optional **`main_branch_ref`**; **`effective_integration_base_ref_for_project`**; **`add_project`** validates integration base strings via **`tddy_core::validate_integration_base_ref`** before YAML write. Feature doc: [git-integration-base-ref.md](../../../docs/ft/coder/git-integration-base-ref.md); [project-concept.md](../../../docs/ft/daemon/project-concept.md). (tddy-daemon)
- **2026-04-04** [Feature] **Worktrees RPCs + web** — **`connection.proto`**: **`ListWorktreesForProject`**, **`RemoveWorktree`**. **`ConnectionService`**: **`WorktreeStatsCache`** per daemon, **`main_repo_path_for_host`**. **`tddy-web`**: **`WorktreesAppPage`** wired to Connect. Tests: **`worktrees_rpc`**. Feature docs: [worktrees.md](../../../docs/ft/web/worktrees.md), [daemon changelog](../../../docs/ft/daemon/changelog.md). Technical: [worktrees.md](./worktrees.md), [connection-service.md](./connection-service.md). (tddy-service, tddy-daemon, tddy-web)
- **2026-04-04** [Feature] **Worktrees library** — **`worktrees`**: **`git worktree list`** parsing, **`WorktreeStatsCache`** (JSON under **`TDDY_PROJECTS_STATS_ROOT`**), lexical path policy, **`remove_worktree_under_repo`**. Feature docs: [worktrees.md](../../../docs/ft/web/worktrees.md), [daemon changelog](../../../docs/ft/daemon/changelog.md). Technical: [worktrees.md](./worktrees.md). (tddy-daemon)
- **2026-04-03** [Feature] Telegram notifier (teloxide) — Optional **`telegram`** YAML (**`enabled`**, **`bot_token`**, **`chat_ids`**); **`telegram_notifier`**: **`TelegramSessionWatcher`**, **`TelegramSender`**, **`send_telegram_via_teloxide`**, two-segment session labels, token masking; unit and integration tests with mock sender. Feature doc **[telegram-notifications.md](../../../docs/ft/daemon/telegram-notifications.md)**; package **[telegram-notifier.md](./telegram-notifier.md)**. (tddy-daemon)
- **2026-04-03** [Feature] Session workflow files + connection path alignment — **`ListSessionWorkflowFiles`** / **`ReadSessionWorkflowFile`** (**`session_workflow_files`**, **`session_workflow_files_rpc`**). **`sessions_base_for_user`** resolves the Tddy data root (**`~/.tddy`**) so **`unified_session_dir_path`** matches **`tddy-coder`** session trees. **`DeleteSession`** terminates a live recorded PID (SIGTERM/SIGKILL; Linux zombie handling), removes directories missing **`.session.yaml`** when safe, and exposes delete on active sessions from the web client. Web: **`SessionWorkflowFilesModal`**, **`SessionMoreActionsMenu`**, project/worktree matching (**`sessionProjectTable`**). Feature docs: [web-terminal.md](../../../docs/ft/web/web-terminal.md), [daemon changelog](../../../docs/ft/daemon/changelog.md), [connection-service.md](./connection-service.md). (tddy-daemon, tddy-service, tddy-web)
- **2026-03-29** [Feature] **`ListAgents`** and **`allowed_agents`** — Connection API lists allowed backends from YAML; **`StartSession`** rejects unknown **`agent`** when the allowlist is non-empty; label rules shared via **`agent_list_mapping`**. Feature docs: [web-terminal.md](../../../docs/ft/web/web-terminal.md), [local-web-dev.md](../../../docs/ft/web/local-web-dev.md), [systemd-install.md](../../../docs/ft/daemon/systemd-install.md). [connection-service.md](./connection-service.md). (tddy-daemon, tddy-service, tddy-web)
- **2026-03-28** [Feature] Session id validation on connection RPCs — **`ConnectSession`**, **`ResumeSession`**, and **`SignalSession`** call **`validate_session_id_segment`** before **`unified_session_dir_path`**; aligns with **`session_deletion`** for **`DeleteSession`**. [connection-service.md](./connection-service.md), [session-layout.md](../../../docs/ft/coder/session-layout.md). (tddy-daemon)
- **2026-03-28** [Feature] Multi-host daemon selection + ListSessions workflow enrichment — `ConnectionService`: `ListEligibleDaemons` via `EligibleDaemonSource`; `ListSessions` populates `daemon_instance_id` and enriched workflow fields (`session_list_enrichment`); `StartSession` honors local vs non-local `daemon_instance_id` (non-local returns unimplemented until peer routing); `spawn_blocking_with_timeout` for read + enrichment. Integration tests: `list_sessions_enriched`, multi-host. Feature docs: [connection-service.md](./connection-service.md), [docs/ft/web/web-terminal.md](../../../docs/ft/web/web-terminal.md). (tddy-daemon, tddy-service, tddy-web)
- **2026-03-23** [Feature] Root `./install --systemd` — Copies release binaries, optional web bundle, production config when absent, generated systemd unit; ENV path overrides and `INSTALL_NO_SYSTEMCTL` for tests. Documented in [docs/ft/daemon/systemd-install.md](../../../docs/ft/daemon/systemd-install.md). (tddy-daemon, tddy-e2e, repo root)
- **2026-03-21** [Feature] StartSession `agent` — `StartSession` passes `agent` into spawn; `spawner` / `spawn_worker` add `--agent` when set for `tddy-coder`. (tddy-daemon)
- **2026-03-19** [Feature] tddy-daemon Binary Extraction — New binary crate. DaemonConfig (listen, livekit, github, users, allowed_tools). AuthService from config. ConnectionService: ListTools, ListSessions, StartSession, ConnectSession, ResumeSession. ProcessSpawner with fork+setuid/setgid, LiveKit credential passing. Session reader from ~user/.tddy/sessions. TokenService when LiveKit configured. serve_web_bundle via tddy-coder. (tddy-daemon)

- **2026-03-21** [Feature] Project concept — `repos_base_path` config; `project_storage` (`~/.tddy/projects/projects.yaml`); `ListProjects`, `CreateProject` (optional `user_relative_path`); `StartSession` by `project_id`; `clone_as_user` and spawn-worker `clone` requests; `SessionMetadata`/`SessionEntry` carry `project_id`. See [connection-service.md](./connection-service.md). (tddy-daemon)
- **2026-03-24** [Feature] DeleteSession — `DeleteSession` removes an inactive session directory under the caller’s sessions tree; `session_deletion` validates ids and path containment; generic internal error on `remove_dir_all` failure with full detail in logs. (tddy-daemon)
