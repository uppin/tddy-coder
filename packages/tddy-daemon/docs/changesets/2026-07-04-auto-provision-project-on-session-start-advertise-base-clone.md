# 2026-07-04 — Auto-provision project on session start + advertise base clone location

**Type:** Feature

`project_provision::ensure_project_available_locally` (injected `cloner` + `peer_lookup` for testability) is wired into `start_session` via `ensure_project_available_for_start` on the local branch (covers tool/claude-cli/sandboxed/workspace): clones a registered-but-missing checkout from the stored `git_url`, peer-discovers `(name, git_url)` for an unregistered project (same fan-out as aggregated `ListProjects`) and clones into `repos_base_for_user(os_user, repos_base_path)/<name>` + `add_or_get_project`, else `NotFound`. Real cloner mirrors `AddProjectToHost` (`SpawnClient::clone_repo` when a spawn worker is set, else `spawner::clone_as_user`), runs via `spawn_blocking` + `timeout`, surfaces clone errors (no masking) and preserves `NotFound`. `DaemonAdvertisement` gains `repos_base_path` (published from `repos_base_path_or_default()`, parsed by `parse_daemon_advertisement_json`; the web reads the same key). `cargo test -p tddy-daemon` 230/1 (1 pre-existing env). [connection-service.md § Auto-provisioning on session start](../connection-service.md#auto-provisioning-on-session-start). Cross-package: [docs/dev/changesets/](../../../../docs/dev/changesets/). (tddy-daemon, tddy-web)
