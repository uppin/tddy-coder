# 2026-07-05 — Cursor Agent CLI session

**Type:** Feature

`cursor_cli` config block; `cursor_cli_spawn` + `install_cursor_hooks_in_worktree`; `connection_service` Start/Resume/Connect/`ReportSessionStatus` for `cursor-cli`; Telegram `/start-cursor` + `tcur:` model keyboard; `cli_session_manager` module (shim `claude_cli_session`). PRD [cursor-cli-session.md](../../../../docs/ft/daemon/cursor-cli-session.md). Cross-package: [docs/dev/changesets/](../../../../docs/dev/changesets/). (tddy-daemon)
