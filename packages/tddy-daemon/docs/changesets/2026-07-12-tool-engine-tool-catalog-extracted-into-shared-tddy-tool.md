# 2026-07-12 — `tool_engine` + `tool_catalog` extracted into shared `tddy-tool-engine`

**Type:** Refactor

`src/tool_engine.rs` + `src/tool_catalog.rs` moved to the new `tddy-tool-engine` crate (shared with `tddy-coder`); `lib.rs` re-exports it as `tool_engine` so internal `tool_engine::execute_tool` / `execute_tool_with_env` call sites are unchanged; the sandbox-allowlist sync test moved to `src/tool_catalog_sync.rs` (`workspace_exec_tool_names_match_tool_catalog`). `connection_service.rs` `ListExecTools` maps `tddy_tool_engine::ToolDef` → proto `ToolDef` at the RPC boundary; `DeleteSession`/`SignalSession` remain daemon-direct (no behaviour change) with a new contract test `delete_session_unit_accepts_daemon_direct_caller_with_valid_token`. `SessionEntry` gains `bytes_in`/`bytes_out`/`last_data_received_at` (proto fields 24/25/26) populated from the `GrpcSessionTerminal` traffic meter for claude-cli/cursor-cli/workspace sessions and zero/empty for stopped tddy-coder sessions. Feature [terminal-sessions.md § Session-scoped RPC routing & daemon-direct lifecycle](../../../../docs/ft/daemon/terminal-sessions.md#session-scoped-rpc-routing--daemon-direct-lifecycle). Cross-package [docs/dev/changesets/](../../../../docs/dev/changesets/). PR [#297](https://github.com/uppin/tddy-coder/pull/297). (tddy-daemon, tddy-tool-engine, tddy-service)
