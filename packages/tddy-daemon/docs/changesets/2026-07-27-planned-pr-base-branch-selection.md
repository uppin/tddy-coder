# 2026-07-27 — **planned-pr-base-branch-selection

**Type:** Feature

the four web-facing spawn paths honor an explicit operator-chosen `selected_integration_base_ref` over the stack-parent-resolved chain base** — each of `spawn_claude_cli_session_inner`, `start_sandboxed_claude_cli_session`, `spawn_cursor_cli_session_inner`, `start_sandboxed_cursor_cli_session` computes `chain_base_ref` as before (still seeds `Changeset.workflow.selected_integration_base_ref` and node linking) then hands `tddy_core::session_chain::select_worktree_base_ref(selected_integration_base_ref, chain_base_ref)` to `setup_worktree_for_session_with_optional_chain_base`: a non-empty trimmed request override is used as the worktree base directly, an empty/whitespace one falls through to the stack-parent resolution (today's behavior). `resolve_chain_base_for_session_spawn` is unchanged — its `stack_parent → persisted → default` precedence still governs the Telegram / workflow-recipe callers that have no request field, completing the `unified-worktree-base-resolution` precedence at the spawn seam only. Tests: `tddy-core/tests/planned_pr_base_branch_selection.rs` 6 (precedence contract); `cargo clippy --workspace --all-targets -- -D warnings` + `cargo fmt --all --check` clean. Feature folded into [git-integration-base-ref.md § Operator-chosen base at the spawn seam](../../../../docs/ft/coder/git-integration-base-ref.md#operator-chosen-base-at-the-spawn-seam-added-2026-07-27) and [pr-stacking.md § Operator-selectable base branch for a diamond node](../../../../docs/ft/coder/pr-stacking.md#operator-selectable-base-branch-for-a-diamond-node-added-2026-07-27). Cross-package [docs/dev/changesets/](../../../../docs/dev/changesets/). (tddy-daemon)
