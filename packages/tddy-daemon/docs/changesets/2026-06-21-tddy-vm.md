# 2026-06-21 — **tddy-vm

**Type:** Feature

daemon wiring + session token validation** — `VmManager` + `VmServiceImpl` (with `SessionUserResolver`) registered inside `if let Some(user_resolver)` block (gated on auth config, same as `ConnectionService`); resolver cloned before being moved into `ConnectionServiceImpl`; `vm.VmService` ServiceEntry pushed via `rpc_entries`. Acceptance tests updated: resolver required in `VmServiceImpl::new`, 5 new tests covering `ListVmImages`, auth rejection for `ListVms`/`DefineVm`/`ListVmImages`. Feature [vm/tddy-vm.md](../../../../docs/ft/vm/tddy-vm.md). (tddy-daemon)
