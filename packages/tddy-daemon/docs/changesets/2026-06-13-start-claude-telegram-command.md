# 2026-06-13 — **`/start-claude` Telegram command

**Type:** Feature

Claude Code CLI sessions seeded with first prompt** — **`claude_cli_session`**: `start(…, initial_prompt: Option<&str>)` passes positional arg to `claude`; `build_claude_argv` pure helper; `resume` never replays prompt; **`StartSessionRequest.initial_prompt`** (field 13) wired through RPC; **`ClaudeCliSessionManager`** now injected into `ConnectionServiceImpl::new` (shared `Arc`); **`TelegramWorkflowSpawn.claude_cli_manager`** field; **`telegram_session_control`**: `handle_start_claude`, `handle_telegram_claude_model_callback`, `spawn_telegram_claude_cli`, `claude_cli_branch_name_from_changeset`, model keyboard (`tcm:` callbacks, 3 models); branch callback routes to model keyboard for `session_type == "claude-cli"`, sets `new_branch_name` from `cs.name` before worktree setup; **`telegram_bot`**: `/start-claude` dispatch + `tcm:` callback. Tests: `claude_cli_session_acceptance` (6 new, 12 total), `telegram_start_claude_acceptance` (4 new). Feature [claude-cli-session.md § Seeding a first prompt](../../../../docs/ft/daemon/claude-cli-session.md), [telegram-session-control.md § /start-claude flow](../../../../docs/ft/daemon/telegram-session-control.md). (tddy-service, tddy-daemon, docs)
