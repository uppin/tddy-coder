# 2026-08-18 — **session agent roster

**Type:** Feature

admission handshake, clone provisioning, conversation routing, sandbox-IPC daemon handler** — `session_agent_roster` (new) holds the per-session roster: monotonic `rev`, idempotent-on-`agent_id` `attach`, `NOT_FOUND` `detach`, snapshot-first `subscribe`, persistence through `atomic_file`, publication on `session.agents`. `session_agent_clone` (new) provisions a remote agent's clone on its owning daemon and runs the session-worktree-sync mirror in-process; one clone per (session, owning daemon), a detached worktree under the sessions base. `connection_service.rs` gains the four roster RPCs, the three conversation RPCs **and their routing** (no separate conversation module), `roster_replacement_pairs`, the clone teardown paths, and a `DaemonRpcHandler` implementing `HostRpcHandler` by recovering the `Arc<ConnectionServiceImpl>` through a self-handle (`Arc<OnceLock<Weak<Self>>>` set in `main.rs`). The room-admission handshake is built: `provision_agent_clone` mints a scoped 5-min token and records the owning daemon in a `SessionAdmissionRegistry`; the owning daemon joins `session-{id}` with that token (no self-mint), `run_clone_mirror` runs a full-rejoin re-admit loop over `SessionAdmissionService.AdmitOwningDaemon`, and revocation (last detach, session delete) stops the mirror. `ListSubagents` stamps `daemon_instance_id`; `resolve_qualified_agent_id` fans out to a peer. AC37 cross-daemon provisioning reaches a daemon that has never seen the project over `remote_git.RemoteGitService` + `tddy-remote-git-repo`. Module [session-agent-roster.md](../session-agent-roster.md). Feature [session-agent-roster.md](../../../../docs/ft/daemon/session-agent-roster.md). Cross-package [docs/dev/changesets/](../../../../docs/dev/changesets/). (tddy-daemon)
