# Changesets Applied

Wrapped changeset history for tddy-workflow-recipes.

**Merge hygiene:** [Changelog merge hygiene](../../../docs/dev/guides/changelog-merge-hygiene.md) — prepend one single-line bullet; do not rewrite shipped lines.

- **2026-07-19** [Feature] **Create-plan prompt hands off via `spawn_conversation`** — `grill_me/prompt.rs` `create_plan_system_prompt` gains a required final "Hand off to implementation" step: once both brief files exist, commit `plans/<slug>.md`, then call `tddy-tools spawn_conversation` (kebab CLI `spawn-conversation`) with a prompt referencing the absolute session-artifact brief path + `plans/<slug>.md` and a `branch`. `grill_me/mod.rs` leaves `goal_hints("create-plan").allowed_tools` at `vec![]` (empty = allow-all; no gating change) and `goal_requires_tddy_tools_submit` at `false`. Test: Create-plan prompt names `spawn_conversation`; pr-stack acceptance green (no regression). Feature [spawn-conversation.md](../../../docs/ft/coder/spawn-conversation.md). Cross-package [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-workflow-recipes)
- **2026-07-21** [Feature] **`exploration.md` — persisted code-discovery session artifact reused across workflow steps** — plan/analyze submit schemas (`generated/tdd/{plan,analyze}.schema.json`) gain an optional `exploration` string; `PlanningOutput`/`AnalyzeOutput` carry it (blank→`None`); `writer::write_artifacts` persists `session_dir/artifacts/exploration.md` verbatim (cross-refs injected like the PRD) via a reusable `write_exploration_file`/`EXPLORATION_BASENAME`, and bugfix `apply_analyze_submit_to_changeset` writes it from the analyze submit (plan agent stays read-only). `known_artifacts()` registers `("exploration","exploration.md")` for `TddRecipe`/`TddSmallRecipe`/`BugfixRecipe`/`GrillMeRecipe` (auto-flows into `context_header_filenames()`); `prepend_context_header` extended past acceptance-tests/red to green + evaluate/demo/validate/refactor (`tdd/hooks.rs`, `tdd/hooks_common.rs`, `tdd_small/hooks.rs`, threading `manifest`). Planning prompt documents the field (Code Map file:line refs, mermaid, Documentation, Conventions & Gotchas); acceptance-tests/red/green prompts instruct read-before-explore + append-as-living-doc; grill-me `create_plan_system_prompt` writes the file directly. Feature [exploration-artifact.md](../../../docs/ft/coder/exploration-artifact.md). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-workflow-recipes, tddy-tools)
- **2026-07-03** [Feature] **PR-stack: free-prompting `orchestrate` loop + internal-status derivation + PR actions** — `PrStackRecipe::build_graph` drops the `begin-orchestrate`/`assess`/`spawn`/`merge`/`repoint` auto-loop for a single terminal `orchestrate` `BackendInvokeTask` (no successor edge → pauses for input); `next_goal_for_state[_with_changeset]` resumes into `orchestrate`; `orchestration_system_prompt` is chat-oriented; the `write-stack-plan` hook now seeds `Changeset.stack` (the removed `begin-orchestrate` was the only seeder). New `orchestrate_pr_stack::internal_status` (`derive_internal_status` from `NodeView`/`PrLiveStatus`/`effective_base_ref` + override-wins `reconcile_internal_status`) and `pr_actions` (`pr_merge_action`/`pr_close_action`/`pr_resolve_conflicts_action`, the last detect-only: probes a `--no-ff` merge, reports unmerged paths, always aborts, distinguishes a refused merge from clean). `GithubPrApi` gains `close_pr`. `OrchestratePrStackRecipe` documented as retained-but-inert (all CLI names resolve to `PrStackRecipe`). Feature [pr-stacking.md](../../../docs/ft/coder/pr-stacking.md) (2026-07-03). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-workflow-recipes, tddy-core, tddy-tools)
- **2026-07-03** [Feature] **`pr_stack::add_planned_pr_node` — append a manually-created planned PR to the stack DAG** — additive-only mutation (never touches existing nodes, unlike `reseed_stack_from_plan_if_unspawned`'s whole-plan overwrite): server-assigns the next free `"n<N>"` node id (`next_free_node_id`), rejects a dangling parent ref or a would-be cycle (`Stack::topo_order`), appends atomically via `update_stack_atomic`. Params grouped into `AddPlannedPrInput`. Feature [pr-stacking.md § Manually adding a planned PR](../../../docs/ft/coder/pr-stacking.md#manually-adding-a-planned-pr). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-workflow-recipes, tddy-core)
- **2026-07-01** [Feature] **Unified `pr-stack` recipe** — new `pr_stack` module consolidates `plan_pr_stack` + `orchestrate_pr_stack` into one session/recipe: `analyze-stack` → `write-stack-plan` → `begin-orchestrate` → `assess` loop, reusing both predecessors' tasks/hooks/prompts directly (no logic duplication). `BeginOrchestrateTask` seeds `Changeset.stack` and replays the crash-recovery journal; `reseed_stack_from_plan_if_unspawned` lets chat-driven refinement overwrite the stack until a node is spawned. `recipe_resolve.rs`/`approval_policy.rs` route `"pr-stack"` plus the two legacy CLI names to the same recipe. Feature [pr-stacking.md](../../../docs/ft/coder/pr-stacking.md#pr-stack-recipe). Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-workflow-recipes, tddy-core)

- **2026-07-01** [Fix] **`FreePromptingRecipe::plain_goal_cli_output` prints via `println!`** — was `log::info!`-only, which is invisible in the `tddy-coder` binary's plain-mode output (logs redirect to a per-session `debug.log` file, not stderr); now matches the convention every other shipped recipe's `plain_goal_cli_output` already uses (`BugfixRecipe`, `TddRecipe`). Part of a cross-package fix for plain-mode `free-prompting` crashing with `no pending questions`. Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-workflow-recipes)
- **2026-06-26** [Feature] **PR-stack orchestration engine — all stubs implemented** — `github_rest_common.rs`: `curl_github_{patch,post,get,put}_json` (curl temp-file body/response pattern, token-gated); `orchestrate_pr_stack/github.rs`: `RealGithubPrApi` 5 methods (`get_open_pr`, `merge_pr`, `patch_pr_base`, `create_pr`, `disable_auto_merge` best-effort no-op), `owner_repo_from_remote_url` (SSH+HTTPS); `git_ops.rs`: `merge_base`, `rebase_onto` (abort on conflict), `force_push_with_lease`; `assess.rs`: `effective_base_ref`, `assemble_views`, `AssessTask::run`; `bridge.rs`: `seed_orchestrator_stack_from_plan`, `execute_stack_merge` (Planned→PrMerged journal), `execute_stack_repoint` (rebase+push+patch_pr_base per dependent); `actions.rs`: `SpawnTask` (marker file), `MergeTask`, `RepointTask`. Tests: bridge acceptance 3/3, merge+repoint acceptance 2/2. Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-workflow-recipes)
- **2026-06-24** [Refactor] **`tddy-graph` extraction — lifecycle hook migration** — 9 `hooks.rs` impls updated: `agent_output_sink`/`progress_sink` trait overrides replaced with `on_enter_task` (→ `set_sinks`) / `on_exit_task` (→ `clear_sinks`) private helpers; `RunnerHooks` no longer has backend-typed sink methods. Cross-package: [docs/dev/changesets.md](../../../docs/dev/changesets.md). (tddy-workflow-recipes)
- **2026-06-21** [Feature] **PR stacking recipes** — `plan-pr-stack` recipe (analyze-stack→write-stack-plan→end pipeline, `stack-plan.yaml`+`pr-stack-plan.md` artifacts, `uses_primary_session_document=false`) and `orchestrate-pr-stack` recipe (idempotent `decide_next_action`, crash-safe `StackOpJournal`, `GithubPrApi` trait + `RealGithubPrApi`, `git rebase --onto` + `git rerere`, rollup hooks, `goal_requires_tddy_tools_submit=false`); both registered in `recipe_resolve.rs` and `approval_policy.rs`; `github_rest_common.rs` shared curl helpers.

- **2026-06-21** [Feature] **Demo goal Phase 2 — demo system prompt 7-step QEMU deploy** — `tdd/demo.rs` system prompt updated: (1) read `demo-plan.md`; (2) build qcow2 via `tddy-tools build --target <build_target>`; (3) wait for VM `Running` state by polling daemon; (4) SSH deploy steps; (5) execute `verify_command` + assert success; (6) confirm port-forward active, report `share_url`, post to Telegram; (7) submit JSON with `share_url`. Test extended with assertions on `deploy_steps`, VM-wait instructions, and `share_url` field. Feature [coder/demo-goal.md](../../../docs/ft/coder/demo-goal.md). (tddy-workflow-recipes)
- **2026-06-20** [Feature] **Demo goal Phase 1 — DemoMode/PortMap, DemoPlan extensions, JSON front-matter round-trip** — `DemoMode` (`PortForward`/`ScreenShare`, serde `snake_case`), `PortMap` (`host_port`/`guest_port`), `DemoPlan` extended with `mode`, `hostfwd`, `deploy_steps`, `verify_command`, `build_target` (all `#[serde(default)]`); `DemoOutput` gains `share_url`; `write_demo_plan_file`/`read_demo_plan_file` with JSON front-matter round-trip and legacy fallback. Feature [coder/demo-goal.md](../../../docs/ft/coder/demo-goal.md); PR [#214](https://github.com/uppin/tddy-coder/pull/214). (tddy-workflow-recipes)
- **2026-06-14** [Feature] **Remote-codebase mode — permissions** — `remote_codebase_allowlist()` returns `["AskUserQuestion"]`; dynamic `mcp__tddy-tools__*` names added by `tddy-coder` at runtime. Feature [remote-codebase-mode.md](../../../../docs/ft/daemon/remote-codebase-mode.md). (tddy-workflow-recipes)
- **2026-05-03** [Feature] **Bugfix recipe — interview-first graph** — **`BugfixRecipe`**: **`interview` → `analyze` → `reproduce` → `end`**; **`bugfix::interview`** relay, workflow states, host recovery probe; **`BugfixWorkflowHooks`** handoff merge into **analyze**. Tests **`bugfix_interview_acceptance`**, **`bugfix_interview_granular`**. Feature **[workflow-recipes.md](../../../../docs/ft/coder/workflow-recipes.md)**; **[coder/changelog.md](../../../../docs/ft/coder/changelog.md)**; **[docs/dev/changesets.md](../../../../docs/dev/changesets.md)**. (tddy-workflow-recipes, tddy-coder, docs)
- **2026-04-06** [Feature] **`github_rest_common` and GitHub-aware prompts** — **`github_rest_common`** (`GITHUB_ACCEPT`, **`GITHUB_API_VERSION`**, **`github_token_from_env`**, **`github_env_token_present`**); merge-pr curl headers and hooks; **tdd-small** **`merged_red_system_prompt`** includes GitHub PR tools section only when a token is present; **`changeset-workflow`** optional **`github_pr_tools_metadata`**. Feature docs: [github-pr-tools-mcp.md](../../../../docs/ft/coder/github-pr-tools-mcp.md). (tddy-workflow-recipes, tddy-tools)
- **2026-04-06** [Feature] **`changeset-workflow` branch/worktree intent** — **`generated/tdd/changeset-workflow.schema.json`** defines **`branch_worktree_intent`** and related workflow strings alongside post-green routing fields. Feature docs: [workflow-json-schemas.md](../../../../docs/ft/coder/workflow-json-schemas.md), [workflow-schemas.md](workflow-schemas.md). (tddy-workflow-recipes, tddy-tools)
- **2026-04-05** [Feature] **`feature_start_slash` surface** — Re-exports **`tddy_core`** helpers for **`/start-<recipe>`** parse, slash menu labels, and the post-completion recipe CLI name; **`recipe_resolve`** remains the single resolver. Feature doc: [workflow-recipes.md](../../../../docs/ft/coder/workflow-recipes.md). (tddy-workflow-recipes)
- **2026-04-04** [Feature] **TDD changeset workflow schema** — **`goals.json`** registers **`changeset-workflow`** with **`generated/tdd/changeset-workflow.schema.json`** and **`proto/changeset_workflow.proto`**; interview prompts document demo elicitation and **`persist-changeset-workflow`**. Feature docs: [workflow-recipes.md](../../../../docs/ft/coder/workflow-recipes.md), [workflow-schemas.md](workflow-schemas.md). (tddy-workflow-recipes)
- **2026-04-04** [Feature] **TDD interview before plan** — **`build_tdd_workflow_graph`** / **`build_full_tdd_workflow_graph`**: **`interview`** **`BackendInvokeTask`** and **`interview` → `plan`** edges; **`tdd::interview`** relay (**`.workflow/tdd_interview_handoff.txt`**); **`TddWorkflowHooks`** interview / plan handoff; **`TddRecipe`** start **`interview`**, **`plan_refinement_goal` → `plan`**, submit opt-out for **`interview`**. See [docs/ft/coder/workflow-recipes.md](../../../../docs/ft/coder/workflow-recipes.md). (tddy-workflow-recipes, tddy-core, tddy-coder, tddy-e2e, tddy-integration-tests)
- **2026-04-04** [Bug Fix] Bugfix **`analyze`** goal — Graph **`analyze` → `reproduce` → `end`**; start goal **`analyze`**; hooks and **`analyze`** JSON Schema in **`goals.json`**; structured submit persists **`branch_suggestion`**, **`worktree_suggestion`**, optional **`name`** / **`summary`** on **`changeset.yaml`**, with **`summary`** in **`changeset.artifacts["analyze_summary"]`** for **`reproduce`**. See [docs/ft/coder/workflow-recipes.md](../../../../docs/ft/coder/workflow-recipes.md). (tddy-workflow-recipes, tddy-tools, tddy-coder)
- **2026-04-04** [Feature] **TDD-small recipe and `post-green-review` contract** — **`TddSmallRecipe`** / **`tdd-small`** graph (**`plan` → `red` → `green` → `post-green-review` → `refactor` → `update-docs`**); **`goals.json`** registers **`post-green-review`** with schema and proto; **`tdd::hooks_common`** shared helpers for **`TddWorkflowHooks`** and **`TddSmallWorkflowHooks`**. See [docs/ft/coder/workflow-recipes.md](../../../../docs/ft/coder/workflow-recipes.md) and [workflow-json-schemas.md](../../../../docs/ft/coder/workflow-json-schemas.md). (tddy-workflow-recipes, tddy-tools, tddy-coder)
- **2026-03-29** [Feature] **Free prompting — invoke graph, hooks, submit policy** — **`FreePromptingRecipe::build_graph`**: single **`BackendInvokeTask`** for **`prompting`** (no **`EndTask`**); **`WorkflowRecipe::goal_requires_tddy_tools_submit`** override for **`prompting`**; **`FreePromptingWorkflowHooks::agent_output_sink`** for **`WorkflowEvent::AgentOutput`**; **`StubBackend`** **`prompting`** response. See [docs/ft/coder/workflow-recipes.md](../../../../docs/ft/coder/workflow-recipes.md). (tddy-workflow-recipes, tddy-core)
- **2026-03-29** [Feature] **`FreePromptingRecipe`**, **`approval_policy`**, resolver — CLI name **`free-prompting`**; initial state **`Prompting`**; **`uses_primary_session_document`** **`false`**; **`approval_policy`** lists supported CLI names and skip rules for session-document approval; **`recipe_resolve`** and **`unknown_workflow_recipe_error`** include **`free-prompting`**. See [docs/ft/coder/workflow-recipes.md](../../../../docs/ft/coder/workflow-recipes.md). (tddy-workflow-recipes, tddy-core, tddy-coder, tddy-e2e)
- **2026-03-28** [Feature] TDD graph session branching — Full graph uses **`run_optional_step_x`** and green hooks (`tddy-tools ask`, **`set-session-context`**) for post-green routing; conditional transitions use engine **`goal_conditions`**. See **`docs/ft/coder/workflow-recipes.md`**. (tddy-workflow-recipes, tddy-core, tddy-tools)
- **2026-03-28** [Feature] Plan task session id resolution — **`PlanTask`** and TDD hooks use **`tddy_core::session_lifecycle::resolve_effective_session_id`** after successful backend submit so the engine keeps the process-bound session id when the backend reports a different id. (tddy-workflow-recipes, tddy-core)
- **2026-03-28** [Feature] **`recipe_resolve`** and **`BugfixRecipe`** — **`workflow_recipe_and_manifest_from_cli_name`**, **`resolve_workflow_recipe_from_cli_name`**; **`BugfixRecipe`**: start **`reproduce`**, primary **`fix-plan.md`**, approval before **green**, **`uses_primary_session_document`** **`true`**. See [docs/ft/coder/workflow-recipes.md](../../../../docs/ft/coder/workflow-recipes.md). (tddy-workflow-recipes, tddy-coder, tddy-daemon, tddy-web)
- **2026-03-28** [Architecture] Recipe-owned session artifacts — **`SessionArtifactManifest`**; TDD hooks and **`TddRecipe::read_primary_session_document_utf8`** use **`tddy_workflow::artifact_paths`** for `artifacts/` vs legacy layout; **`before_update_docs`** builds availability from **`known_artifacts()`**; no core fallback for primary PRD basename. (tddy-workflow-recipes, tddy-workflow, tddy-core)
- **2026-03-28** [Feature] Workflow JSON Schemas — `goals.json` single registry; `schemas/` → `generated/` build; `proto/` IDL files; `schema_pipeline` and contract tests. See `docs/ft/coder/workflow-json-schemas.md` and `packages/tddy-workflow-recipes/docs/workflow-schemas.md`. (tddy-workflow-recipes, tddy-tools)
- **2026-03-22** [Feature] Workflow abstraction layer — New crate: **`TddRecipe`** (full TDD graph, hooks, parsers, permissions), **`BugfixRecipe`** stub for OCP; `WorkflowRecipe` implementation. TDD-specific graph/hooks live here instead of `tddy-core`. See [docs/ft/coder/workflow-recipes.md](../../../../docs/ft/coder/workflow-recipes.md). (tddy-workflow-recipes, tddy-core, tddy-coder, tddy-service, tddy-daemon, tddy-demo)
- **2026-04-05** [Feature] **Grill-me workflow recipe** — **`GrillMeRecipe`**: goals **`grill`** → **`create-plan`** → **`end`**; **`GrillMeWorkflowHooks`**; **`grill_me`** module and repo brief persistence per **AGENTS.md** **`plans/`**; CLI **`--recipe grill-me`** and web recipe control. See [workflow-recipes.md](../../../../docs/ft/coder/workflow-recipes.md). (tddy-workflow-recipes, tddy-core, tddy-coder, tddy-web)
- **2026-04-05** [Feature] **Review workflow recipe** — **`ReviewRecipe`**: **`inspect` → `branch-review` → `end`**; **`review/`** module (**`git_context`**, **`hooks`**, **`parse`**, **`persist`**); **`goals.json`** registers **`branch-review`** with schema and proto; **`SessionArtifactManifest`** **`review` → `review.md`**; **`approval_policy`** lists **`review`**. See [workflow-recipes.md](../../../../docs/ft/coder/workflow-recipes.md) and [workflow-json-schemas.md](../../../../docs/ft/coder/workflow-json-schemas.md). (tddy-workflow-recipes, tddy-tools, tddy-coder, tddy-daemon)
