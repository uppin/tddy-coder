# Changesets Applied

Wrapped changeset history for tddy-workflow-recipes.

- **2026-04-04** [Feature] **TDD-small recipe and `post-green-review` contract** — **`TddSmallRecipe`** / **`tdd-small`** graph (**`plan` → `red` → `green` → `post-green-review` → `refactor` → `update-docs`**); **`goals.json`** registers **`post-green-review`** with schema and proto; **`tdd::hooks_common`** shared helpers for **`TddWorkflowHooks`** and **`TddSmallWorkflowHooks`**. See [docs/ft/coder/workflow-recipes.md](../../../../docs/ft/coder/workflow-recipes.md) and [workflow-json-schemas.md](../../../../docs/ft/coder/workflow-json-schemas.md). (tddy-workflow-recipes, tddy-tools, tddy-coder)
- **2026-03-29** [Feature] **Free prompting — invoke graph, hooks, submit policy** — **`FreePromptingRecipe::build_graph`**: single **`BackendInvokeTask`** for **`prompting`** (no **`EndTask`**); **`WorkflowRecipe::goal_requires_tddy_tools_submit`** override for **`prompting`**; **`FreePromptingWorkflowHooks::agent_output_sink`** for **`WorkflowEvent::AgentOutput`**; **`StubBackend`** **`prompting`** response. See [docs/ft/coder/workflow-recipes.md](../../../../docs/ft/coder/workflow-recipes.md). (tddy-workflow-recipes, tddy-core)
- **2026-03-29** [Feature] **`FreePromptingRecipe`**, **`approval_policy`**, resolver — CLI name **`free-prompting`**; initial state **`Prompting`**; **`uses_primary_session_document`** **`false`**; **`approval_policy`** lists supported CLI names and skip rules for session-document approval; **`recipe_resolve`** and **`unknown_workflow_recipe_error`** include **`free-prompting`**. See [docs/ft/coder/workflow-recipes.md](../../../../docs/ft/coder/workflow-recipes.md). (tddy-workflow-recipes, tddy-core, tddy-coder, tddy-e2e)
- **2026-03-28** [Feature] TDD graph session branching — Full graph uses **`run_optional_step_x`** and green hooks (`tddy-tools ask`, **`set-session-context`**) for post-green routing; conditional transitions use engine **`goal_conditions`**. See **`docs/ft/coder/workflow-recipes.md`**. (tddy-workflow-recipes, tddy-core, tddy-tools)
- **2026-03-28** [Feature] Plan task session id resolution — **`PlanTask`** and TDD hooks use **`tddy_core::session_lifecycle::resolve_effective_session_id`** after successful backend submit so the engine keeps the process-bound session id when the backend reports a different id. (tddy-workflow-recipes, tddy-core)
- **2026-03-28** [Feature] **`recipe_resolve`** and **`BugfixRecipe`** — **`workflow_recipe_and_manifest_from_cli_name`**, **`resolve_workflow_recipe_from_cli_name`**; **`BugfixRecipe`**: start **`reproduce`**, primary **`fix-plan.md`**, approval before **green**, **`uses_primary_session_document`** **`true`**. See [docs/ft/coder/workflow-recipes.md](../../../../docs/ft/coder/workflow-recipes.md). (tddy-workflow-recipes, tddy-coder, tddy-daemon, tddy-web)
- **2026-03-28** [Architecture] Recipe-owned session artifacts — **`SessionArtifactManifest`**; TDD hooks and **`TddRecipe::read_primary_session_document_utf8`** use **`tddy_workflow::artifact_paths`** for `artifacts/` vs legacy layout; **`before_update_docs`** builds availability from **`known_artifacts()`**; no core fallback for primary PRD basename. (tddy-workflow-recipes, tddy-workflow, tddy-core)
- **2026-03-28** [Feature] Workflow JSON Schemas — `goals.json` single registry; `schemas/` → `generated/` build; `proto/` IDL files; `schema_pipeline` and contract tests. See `docs/ft/coder/workflow-json-schemas.md` and `packages/tddy-workflow-recipes/docs/workflow-schemas.md`. (tddy-workflow-recipes, tddy-tools)
- **2026-03-22** [Feature] Workflow abstraction layer — New crate: **`TddRecipe`** (full TDD graph, hooks, parsers, permissions), **`BugfixRecipe`** stub for OCP; `WorkflowRecipe` implementation. TDD-specific graph/hooks live here instead of `tddy-core`. See [docs/ft/coder/workflow-recipes.md](../../../../docs/ft/coder/workflow-recipes.md). (tddy-workflow-recipes, tddy-core, tddy-coder, tddy-service, tddy-daemon, tddy-demo)
