# 2026-06-20 — **tddy-build-docker

**Type:** Feature

BUILD.yaml config** — `packages/tddy-build-docker/BUILD.yaml` declares `tddy-build-docker:lib` with `srcs` glob and dep on `tddy-build:lib`. (tddy-build-docker)
