# 2026-03-13 — LiveKit ConnectRPC Transport

**Type:** Feature

LiveKitTransport implements ConnectRPC Transport over LiveKit data channels. AsyncQueue for streaming responses. Proto codegen (buf). Cypress component tests for unary, server streaming, client streaming, bidi streaming, error handling, AbortSignal. (tddy-livekit-web)
