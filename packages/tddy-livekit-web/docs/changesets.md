# Changesets Applied

Wrapped changeset history for tddy-livekit-web.

- **2026-03-13** [Feature] Ghostty Terminal Proto Codegen — terminal.proto codegen. TerminalInputSchema, TerminalOutputSchema, TerminalService. Exported from index.ts. (tddy-livekit-web)
- **2026-03-13** [Feature] LiveKit ConnectRPC Transport — LiveKitTransport implements ConnectRPC Transport over LiveKit data channels. AsyncQueue for streaming responses. Proto codegen (buf). Cypress component tests for unary, server streaming, client streaming, bidi streaming, error handling, AbortSignal. (tddy-livekit-web)
