# 2026-07-02 — **`sandbox_remote_appendix`/`create_with_subagent`

**Type:** Feature

tool-replacement enforcement text** — the managed-codebase appendix now optionally names a replacing subagent and the tools it replaces (enforcement paragraph), while the exec-tool allowlist itself is filtered at the `tddy-sandbox-recipes`/`tddy-sandbox-runner` layer. `create(dir)` unchanged (`create_with_subagent(dir, None, &[])`). Feature [managed-codebase-subagents.md § Tool replacement](../../../../docs/ft/coder/managed-codebase-subagents.md#tool-replacement-subagent-declared). (tddy-sandbox, tddy-discovery)
