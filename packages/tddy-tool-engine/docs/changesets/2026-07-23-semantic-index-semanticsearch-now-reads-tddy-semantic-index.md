# 2026-07-23 — semantic-index: `SemanticSearch` now reads `TDDY_SEMANTIC_INDEX_DB` (threaded via `extra_env`) and errors when no index is available

**Type:** Feature

the ripgrep fallback is removed (querying a populated index via the production embedder is a follow-up). Feature [semantic-index.md](../../../../docs/ft/coder/semantic-index.md). Cross-package [changeset](../../../../docs/dev/changesets/).
