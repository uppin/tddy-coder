//! Cross-package flags referenced by acceptance tests.

/// When true, loopback tunnel byte bridging uses `tddy_daemon::tunnel_streambytes_bridge` (shared with OAuth).
pub const LOOPBACK_TUNNEL_STREAMBYTES_VIA_GENERALIZED_SUPERVISOR: bool = true;
