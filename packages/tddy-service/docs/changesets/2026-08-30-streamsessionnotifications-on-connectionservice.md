# 2026-08-30 — `StreamSessionNotifications` on `ConnectionService`

**Type:** Feature

a daemon-level server-streaming RPC carrying every session's activity and attention events to one subscriber, plus `SessionNotificationEvent` and the `SessionNotificationKind` / `SessionNotificationSource` enums.
