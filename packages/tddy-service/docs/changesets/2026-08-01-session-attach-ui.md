# 2026-08-01 — **session-attach-ui

**Type:** Feature

streaming twins of `StartSession` and `ReadHostDocument`, a `STAGED_ATTACHMENT` document scope, and `WorktreeDirEntry.size_bytes`** — `proto/connection.proto` adds `rpc StreamStartSession(StartSessionRequest) returns (stream StartSessionEvent)` and `rpc StreamReadHostDocument(ReadHostDocumentRequest) returns (stream HostDocumentChunk)` **alongside** their unary counterparts, which are unchanged so `tddy-tools remote start-session`, `tddy-sandbox-app`, recipe hooks and PR-stack chain spawns need no migration; new messages `StartSessionEvent` (`oneof event { attachment_progress = 1, result = 2 }`), `AttachmentMaterializationProgress` (`basename`, `attachment_index`, `attachment_count`, `bytes_done`, `bytes_total`) and `HostDocumentChunk` (`data`, `total_byte_size` — stamped on every frame so a consumer needs no preamble); `HostDocumentScope.HOST_DOCUMENT_SCOPE_STAGED_ATTACHMENT = 5` (root `{staging_base}/{os_user}/`, `relative_path` = `<staging_id>/<file_name>`, mirroring `SESSION_UPLOAD`'s two-segment shape so it reuses that validation); `WorktreeDirEntry.size_bytes = 3`, which lets a client pre-check a worktree or project-repo file against the host's attachment cap. Also deleted two stale comment blocks that `118e0441` had made wrong — the staging RPCs' "NOT IMPLEMENTED: all three return UNIMPLEMENTED" and `StartSessionRequest.attachments`' claim that the daemon ignores the field — both of which had been mirrored into the generated `packages/tddy-web/src/gen/connection_pb.ts`. Rust regenerated via `tddy-service/build.rs`; TS regenerated with `bunx buf generate`. Docs [connection-service.md § Start-session attachments](../../../tddy-daemon/docs/connection-service.md#start-session-attachments). Cross-package [docs/dev/changesets/](../../../../docs/dev/changesets/). (tddy-service)
