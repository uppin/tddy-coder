# 2026-07-26 — start-session-attachments: `proto/connection.proto` adds `StartSessionRequest.attachments = 29` (`repeated SessionAttachment`: a `basename` plus a `oneof source` of `StagedAttachmentRef{daemon_instance_id, staging_id, file_name}` or `HostDocumentRef{daemon_instance_id, HostDocumentScope scope, session_id, project_id, relative_path}`), the `HostDocumentScope` enum (`SESSION_ARTIFACT`/`SESSION_UPLOAD`/`SESSION_WORKTREE`/`PROJECT_REPO`

**Type:** Feature

a scoped root the owning daemon resolves, deliberately **not** an absolute-host-path field), and three pre-session staging RPCs `UploadStagedAttachmentChunk{session_token,daemon_instance_id,staging_id,file_name,data,last} → {StagedAttachmentEntry entry}` / `ListStagedAttachments` / `DeleteStagedAttachment` mirroring `UploadSessionFileChunk` with `session_id`→`daemon_instance_id` and `upload_id`→`staging_id` (regenerated Rust + `tddy-web/src/gen/connection_pb.ts`). **Wire contract only — no runtime behavior; every new RPC is `UNIMPLEMENTED` and `attachments` is ignored by `start_session`.** Docs [tddy-daemon connection-service.md § Start-session attachments](../../../tddy-daemon/docs/connection-service.md#start-session-attachments-wire-contract-only). Cross-package [docs/dev/changesets/](../../../../docs/dev/changesets/). (tddy-service)
