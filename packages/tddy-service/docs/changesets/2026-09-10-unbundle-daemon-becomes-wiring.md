# 2026-09-10 — Connection proto removed

**Type:** Architecture

Deleted `connection.proto`. Added `session.proto`, `project.proto`, `demo_vm.proto`, `local_token.proto`.
Absence tests in `unbundle_service_split.rs`.
