# 2026-07-01 — **connection.proto

**Type:** Feature

`recipe` field on `SessionEntry`** — `string recipe = 22` on `SessionEntry`; surfaces the workflow recipe that created a session (e.g. `orchestrate-pr-stack`) to clients for orchestrator-only filtering in the new-session picker. (tddy-service)
