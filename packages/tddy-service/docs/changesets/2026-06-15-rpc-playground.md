# 2026-06-15 — **RPC Playground

**Type:** Feature

gRPC ServerReflection** — `reflection.proto` vendored; `reflection_service.rs` (`ServerReflectionImpl`) indexes a build-time embedded `FileDescriptorSet`; `reflection_entry_from()` helper; `build.rs` descriptor-only codegen pass. Feature [rpc-playground.md](../../../../docs/ft/daemon/rpc-playground.md). (tddy-service)
