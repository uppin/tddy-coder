# 2026-03-22 — gRPC terminal reconnect

**Type:** Fix

`UserIntent::SelectHighlightChanged` syncs Select highlight to presenter for `connect_view` snapshots; `grpc_reconnect_second_stream_receives_full_tui_render` re-enabled. (tddy-core, tddy-tui, tddy-service, tddy-e2e)
