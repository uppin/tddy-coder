# 2026-03-28 — gRPC Virtual TUI idle semantics

**Type:** Feature

`grpc_terminal_rpc` asserts frozen elapsed and ~1 Hz idle dot cadence in clarification wait; `grpc_reconnect_acceptance` threshold aligned with smaller idle frames; `pty_full_workflow` stream-order assertions. (tddy-e2e)
