# 2026-03-14 — LiveKit Token Generation

**Type:** Feature

`--livekit-api-key`, `--livekit-api-secret` (or LIVEKIT_API_KEY, LIVEKIT_API_SECRET env). Mutual exclusivity with `--livekit-token`. validate_livekit_args. Daemon and TUI use TokenGenerator + run_with_reconnect when key/secret set. Clap env feature. (tddy-coder)
