# 2026-06-21 — PR stacking CLI flags

**Type:** Feature

`--stack-parent <orchestrator-session-id>` (sets `orchestrator_session_id` on changeset) and `--stack-base <base-session-id>` (sets `previous_session_id`) added to `run.rs`.
