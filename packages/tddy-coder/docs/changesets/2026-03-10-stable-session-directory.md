# 2026-03-10 — Stable Session Directory

**Type:** Feature

When `--output-dir` omitted, run.rs uses `$HOME/.tddy` and `create_session_dir_in`; agent working_dir = current_dir(). Plan goal creates session under `$HOME/.tddy/sessions/{uuid}/`. (tddy-coder)
