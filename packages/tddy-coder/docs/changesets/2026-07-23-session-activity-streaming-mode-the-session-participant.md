# 2026-07-23 — session-activity-streaming-mode: the session-participant `StreamSessionActivity` arm honours `StreamMode` (`LIVE_ONLY` skips the persisted snapshot; the default keeps subscribe-before-snapshot ordering), and maps records to the wire via `tddy_service::agent_activity_to_proto`. Feature [agent-activity-pane.md](../../../../docs/ft/web/agent-activity-pane.md). Cross-package [docs/dev/changesets/](../../../../docs/dev/changesets/). (tddy-coder)

**Type:** Feature


