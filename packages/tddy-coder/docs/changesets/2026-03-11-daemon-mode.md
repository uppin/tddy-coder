# 2026-03-11 — Daemon Mode

**Type:** Feature

`--daemon` flag. run_daemon: headless gRPC server, sessions_base (~/.tddy/sessions or --output-dir), create_backend, DaemonService. Blocks until shutdown (SIGTERM). (tddy-coder)
