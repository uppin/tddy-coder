# 2026-03-22 — Local web dev script

**Type:** Feature

Root **`./web-dev`** runs **`tddy-daemon`** with **`dev.daemon.yaml`** (or **`DAEMON_CONFIG`**); Vite proxy uses **`DAEMON_PORT`** from that YAML. Documented under [local-web-dev.md](../../../../docs/ft/web/local-web-dev.md). (tddy-coder)
