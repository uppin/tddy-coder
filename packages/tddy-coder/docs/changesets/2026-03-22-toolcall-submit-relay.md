# 2026-03-22 — Toolcall submit relay

**Type:** Feature

`daemon_toolcall_poll_regression` documents that `run_daemon` calls `poll_tool_calls` like the TUI so `SubmitActivity` reaches the activity log; aligned with immediate submit ack in `tddy-core`. (tddy-coder)
