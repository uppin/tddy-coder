# 2026-03-21 — Interactive backend selection

**Type:** Feature

`Args.agent` / `CoderArgs.agent` as `Option<String>`; `effective_agent()`; TUI/plain paths show backend selection when omitted; `verify_tddy_tools_available` after resolved agent; config merge when `args.agent.is_none()`; `plain.rs` menu helpers. (tddy-coder)
