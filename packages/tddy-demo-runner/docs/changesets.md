# Changesets Applied

Wrapped changeset history for tddy-demo-runner.

**Merge hygiene:** [Changelog merge hygiene](../../../docs/dev/guides/changelog-merge-hygiene.md) — prepend one single-line bullet; do not rewrite shipped lines.

- **2026-06-21** [Refactor] **tddy-vm — demo-runner refactored onto tddy-vm** — `src/vm.rs`, `src/qemu.rs`, `src/mock.rs` deleted; `Cargo.toml`: `tddy-vm` dep added, `serde`/`serde_json` removed; `src/lib.rs` re-exports `tddy_vm::{Vm, QemuVm, MockVm, VmConfig, PortForward, RunningVm}`; `src/orchestrator.rs` uses `tddy_vm` types; `connection_service.rs` `DemoVmHandle` uses `tddy_vm::RunningVm`. Feature [vm/tddy-vm.md](../../../docs/ft/vm/tddy-vm.md). (tddy-demo-runner)
- **2026-06-21** [Feature] **Demo goal Phase 2 — QemuDemoVm SSH/process impl + unit tests** — `QemuDemoVm::boot` spawns `qemu-system-x86_64` (detached) and polls SSH up to 5 min; `deploy` runs each step via `ssh root@127.0.0.1 -p <port>`, exits early on non-zero; `verify` captures stdout+stderr; `forward` validates TCP reachability on host port (1 s timeout) and returns `ForwardHandle { share_url: "http://localhost:<port>" }`; `shutdown` sends `system_powerdown` via monitor socket; `wait_for_ssh_port(host, port, timeout)` TCP-polls every 100 ms; `send_monitor_command(socket, cmd)` async Unix-socket write; `QemuVmArgs::monitor_socket_path(port)` → `/tmp/tddy-demo-monitor-{port}.sock` for per-instance sockets; unit tests for all helpers and `forward`. Feature [coder/demo-goal.md](../../../docs/ft/coder/demo-goal.md). (tddy-demo-runner)
- **2026-06-20** [Feature] **Demo goal Phase 1 — DemoVm trait, QemuVmArgs, MockDemoVm, DemoOrchestrator** — new crate; `DemoVm` trait (boot/deploy/verify/forward/shutdown, mockable VM boundary); `QemuVmArgs::build/hostfwd_spec/netdev_arg` (pure QEMU argv builder, unit-testable without process spawning); `MockDemoVm` recording test double; `DemoOrchestrator::run(recipe, RunningVm)` (validate PortForward → deploy → verify → forward → Telegram notify; never calls boot — VM lifecycle is UI/daemon-owned). `QemuDemoVm` concrete SSH/boot implementation deferred. Feature [coder/demo-goal.md](../../../docs/ft/coder/demo-goal.md); PR [#214](https://github.com/uppin/tddy-coder/pull/214). (tddy-demo-runner)
