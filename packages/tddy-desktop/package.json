{
  "name": "tddy-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run scripts/electrobun-dev.ts",
    "build": "electrobun build",
    "test": "bun run --cwd ../tddy-livekit-web build && bun test src/bun test/acceptance test/e2e",
    "prebuild": "bun run --cwd ../tddy-livekit-web build"
  },
  "dependencies": {
    "@bufbuild/protobuf": "^2.2.0",
    "@connectrpc/connect": "^2.0.0",
    "@connectrpc/connect-web": "^2.0.0",
    "@livekit/rtc-node": "0.13.24",
    "electrobun": "1.16.0",
    "tddy-livekit-web": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
