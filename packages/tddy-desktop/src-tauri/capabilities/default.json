{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the dashboard window may call: the three RPC commands that open, feed and release an addressed envelope-frame connection, plus the core plugin set the @tauri-apps/api helpers are built on.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "allow-tddy-rpc-connect",
    "allow-tddy-rpc-send",
    "allow-tddy-rpc-disconnect"
  ]
}
