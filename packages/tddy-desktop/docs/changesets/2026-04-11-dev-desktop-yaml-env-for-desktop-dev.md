# 2026-04-11 — dev.desktop.yaml + .env for desktop dev

**Type:** Feature

Repo-root `dev.desktop.yaml` default when `TDDY_DAEMON_CONFIG` unset; `loadRootDotEnv` + `CURRENT_USER` temp config like `./web-dev`; `desktop-dev.sh` loads `.env` and exports default config path. (tddy-desktop)
