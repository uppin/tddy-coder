# 2026-07-22 — reusable-lsp: register the `TddyLspExecutor` before spawning the jail (macOS `run_macos`) + set `TDDY_LSP_TOOLS` in the jail env (`spawn.rs`) when `is_available(&params.repo)`. Cross-package [changeset](../../../../docs/dev/changesets/). PR [#310](https://github.com/uppin/tddy-coder/pull/310).

**Type:** Feature


