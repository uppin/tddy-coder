# 2026-07-30 — pr-stack-full-control: seven new `pr_*` `#[tool]` methods (`pr_update_planned`, `pr_delete_planned`, `pr_set_parents`, `pr_read`, `pr_search`, `pr_comments`, `pr_adopt`) with their `Parameters` input structs and matching `call_tool_by_name` arms, bringing the PR-stack surface to 15. The bodies **delegate only**

**Type:** Feature

env via `orchestrator_dir`/`real_gh`/`repo_slug`/`default_branch`/`addressed_pull_number`, logic in `tddy_workflow_recipes::{pr_stack, orchestrate_pr_stack::pr_insight}`, wire shape via `pr_read_json`/`pr_comments_json`/`pr_search_json` built with `serde_json::json!` (the view types carry no `Serialize` derive: the JSON is the tool's contract, not a projection of a Rust type). `pr_read` omits `files` entirely rather than emitting `null` when not requested, and a comment thread carries no `resolved` key. `pr_update_planned`'s `sync_pr` resolves the PR reference, the credential **and** the payload before the local write, so a refused call leaves the plan untouched — a partial write found in review, where the edit landed and *then* the push was rejected. `PrSetParentsInput::parents` deliberately has **no** `#[serde(default)]`, unlike every other parent list on this surface: an omitted field would parse as `[]`, which on a branch-owning node rebases onto the default branch, force-pushes with lease and re-targets the PR — a forgotten field must not be a destructive git operation. `UNKNOWN_TOOL_REJECTION` is a named `pub const` so the cross-crate reachability test binds to a contract instead of prose; `to_wire` replaces eight copies of the `{"error": …}` result envelope. Tests: 220 (+10) — `pr_stack_tool_dispatch_acceptance` derives its expectations from `PR_STACK_TOOL_NAMES` (with a cardinality guard, so a naming change fails loudly instead of passing vacuously) and asserts both MCP advertisement and by-name dispatch; inline unit tests pin `addressed_pull_number`'s two rejection arms, all three wire shapes, and `pr_state_name` over every `PrState`. Feature [pr-stacking.md § PR-management tools](../../../../docs/ft/coder/pr-stacking.md#pr-management-tools). Cross-package [docs/dev/changesets/](../../../../docs/dev/changesets/). (tddy-tools)
