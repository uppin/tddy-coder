# 2026-07-05 — `session-hook` Cursor stdin + `list-models cursor-cli`

**Type:** Feature

`session-hook` maps `hook_event_name` from stdin (Cursor + Claude); `list-models --agent cursor-cli` returns curated catalog. PRD [cursor-cli-session.md](../../../../docs/ft/daemon/cursor-cli-session.md). Cross-package: [docs/dev/changesets/](../../../../docs/dev/changesets/). (tddy-tools)
