//! The error types live in [`tddy_session_store::error`]. Re-exported at its old path for existing
//! callers; new code should name `tddy_session_store::error` directly.

pub use tddy_session_store::error::*;
