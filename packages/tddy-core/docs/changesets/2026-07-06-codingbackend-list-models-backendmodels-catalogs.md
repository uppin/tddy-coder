# 2026-07-06 — `CodingBackend::list_models()` + `BackendModels` catalogs

**Type:** Feature

`backend/mod.rs` adds `BackendModel{id,label}`/`BackendModels{models,default_model}` and an async `CodingBackend::list_models() -> Result<BackendModels, BackendError>` (default impl = curated list for the backend's name), with `AnyBackend`/`SharedBackend` dispatch, `curated_models_for_agent` (claude=opus/sonnet/haiku, codex=gpt-5), a free `claude_cli_models()` (full Claude ids, `claude-opus-4-8` default), and pure `acp_models_from_session_state` mapping `SessionModelState.available_models` (`current_model_id` = default; **errors** when no `SessionModelState` — no empty-list fallback). `backend/cursor.rs` spawns `<bin> --list-models` and parses `id - label` lines treating `(current, default)` as the default (marker stripped); `acp.rs`/`codex_acp.rs` run an ephemeral `initialize`+`new_session` and read `available_models` (stop discarding `.models`); `claude.rs`/`codex.rs`/`stub.rs`/`mock.rs` return curated/static lists. Tests: cursor parser 2, curated catalogs 3, ACP mapping 2 (incl. no-fallback) — suite 236/236. Feature [tool-session-model-selection.md](../../../../docs/ft/web/tool-session-model-selection.md). Cross-package [docs/dev/changesets/](../../../../docs/dev/changesets/). (tddy-core)
