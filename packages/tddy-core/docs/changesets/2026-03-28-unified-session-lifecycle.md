# 2026-03-28 — Unified session lifecycle

**Type:** Feature

**`session_lifecycle`**: **`materialize_unified_session_directory`**, **`unified_session_dir_path`**, **`resolve_effective_session_id`**, **`validate_session_id_segment`** / **`SessionIdValidationError`**, **`UnifiedSessionTreeBootstrap`** (**`SessionLifecycleBootstrap`**). Presenter **`workflow_runner`**: shared **`insert_engine_io_and_demo_flags`**; explicit error when **`session_dir`** cannot be resolved from engine context (no anonymous directory fallback). Feature doc: [session-layout.md](../../../../docs/ft/coder/session-layout.md). (tddy-core)
