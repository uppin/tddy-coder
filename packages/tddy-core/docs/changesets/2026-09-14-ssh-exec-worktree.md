# 2026-09-14 — Remote worktree materialization for SSH exec

**Type:** Feature

Added `ssh_config_host` on session metadata and `setup_worktree_for_session_over_ssh` to create the
session worktree on an OpenSSH Host alias target.
