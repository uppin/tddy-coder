# 2026-07-24 — activities-as-acp: `stream::claude::tool_use_detail` is now `pub` and appends an inclusive line range to the Read/Write basename (`main.rs L10-49`) when the input carries numeric `offset`+`limit` (whole-file reads stay basename-only). Reused by `tddy-service`'s ACP tool-frame title. Feature [agent-activity-pane.md](../../../../docs/ft/web/agent-activity-pane.md). Cross-package [docs/dev/changesets/](../../../../docs/dev/changesets/). (tddy-core)

**Type:** Feature


