# 2026-03-07 — Claude Stream-JSON Backend

**Type:** Feature

Replaced plain-text backend with NDJSON stream processing (`--output-format=stream-json`), session management (`--session-id`/`--resume`), structured question extraction from AskUserQuestion, structured-response and delimited PRD/TODO parsing, progress callback. (tddy-core)
