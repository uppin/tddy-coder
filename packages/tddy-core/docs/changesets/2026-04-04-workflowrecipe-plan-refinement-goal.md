# 2026-04-04 — `WorkflowRecipe::plan_refinement_goal`

**Type:** Feature

Default **`start_goal()`**; recipes with a pre-plan elicitation step override so PRD refinement targets the structured planning goal (e.g. TDD **`plan`**, grill-me **`create-plan`**). **`skip_failed_resume_transition`** default; **`start_goal_for_session_continue`** / TDD resume alignment. See [workflow-recipes.md](../../../../../docs/ft/coder/workflow-recipes.md). (tddy-core, tddy-workflow-recipes, tddy-coder)
