# 2026-08-30 — one session-display-label rule

**Type:** Feature

new `session_label` module deriving a session's human name as repo basename → workflow goal → short id, mirroring `tddy-web`'s `sessionDrawerLabel` case for case (em-dash display placeholder counts as absent), so a Telegram message and a drawer row name the same session identically.
