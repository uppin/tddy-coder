# 2026-07-22 — reusable-lsp: add the `LspExecutor` trait + `LspQuery` + process-global `OnceLock` registry (`register_lsp_executor`/`lsp_executor`) in `toolcall/lsp.rs`, mirroring `BuildExecutor`; re-exported from `toolcall`. Cross-package [changeset](../../../../docs/dev/changesets/). PR [#310](https://github.com/uppin/tddy-coder/pull/310).

**Type:** Feature


