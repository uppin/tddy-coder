# 2026-07-23 — **bsp-build-server

**Type:** Feature

mode-aware lowering** — `RustPlugin::lower_mode` emits `cargo test`/`cargo run` actions for `BuildMode::Test`/`Run` (Compile stays `cargo build`); unsupported modes are rejected. Cross-package [docs/dev/changesets/](../../../../docs/dev/changesets/). Feature [bsp-build-server.md](../../../../docs/ft/coder/bsp-build-server.md). (tddy-build-rust)
