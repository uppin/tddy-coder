# 2026-07-23 — **bsp-build-server

**Type:** Feature

mode-aware lowering** — `TypeScriptPlugin::lower_mode` emits configurable bun `test`/`start` scripts for `BuildMode::Test`/`Run` (Compile stays the build script); unsupported modes are rejected. Cross-package [docs/dev/changesets/](../../../../docs/dev/changesets/). Feature [bsp-build-server.md](../../../../docs/ft/coder/bsp-build-server.md). (tddy-build-typescript)
