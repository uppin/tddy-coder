# Changelogs and changeset histories are directories of one-file-per-entry, so parallel
# branches add distinct files and never share an append-point. No union merge driver is
# needed — and none should be added back.
# See docs/dev/guides/changelog-merge-hygiene.md
